# VoidCube 全项目核心逻辑与架构检查计划

## 0. 审查目标与边界

本次审查从项目核心逻辑、运行时拓扑和数据所有权出发，对整个仓库做一次可复核的细致检查。目标不是只找语法问题，而是确认：

- 实际代码是否遵守 `ARCHITECTURE.md` 的依赖方向、进程边界和数据库 owner 规则；
- API-A、Supervisor、Memory/MemAI、API-B 员工执行、CLI 与 Desktop 的主链路是否闭合；
- 启动、关闭、重启、故障、重试、幂等、并发、权限和降级行为是否与契约一致；
- 技能发现/registry、模型/鉴权/请求协议、配置和 wheel 是否存在失效入口、重复实现或退役逻辑残留；
- 测试是否覆盖关键契约，且测试结果能够作为架构结论的证据。

不在本次审查中替代产品设计评审，也不以重构为目的。发现问题时优先记录证据和影响；只有为完成审查所必需的最小修复才直接修改实现。

## 1. 审查原则

1. 以 `ARCHITECTURE.md` 为架构规范，以当前代码和测试为实现证据；冲突必须单独记录。
2. 先画依赖和数据流，再深入局部实现，避免被大量单元测试或单个模块带偏。
3. 每条问题必须包含位置、触发条件、实际行为、规范期望、影响、严重级别和验证方式。
4. 区分真实缺陷、设计差距、测试缺口和文档漂移，不把它们混为一类。
5. 使用项目虚拟环境执行测试；命令失败、环境缺失或测试未覆盖都要如实记录。
6. 不回退用户已有改动；审查记录本身是本次任务的主要产物，代码修改需另行说明。

## 2. 严重级别

| 级别 | 判定标准 |
|---|---|
| P0 | 可导致数据损坏/越权/秘密泄露/不可恢复的核心运行失败，或规范架构的关键安全边界被绕过 |
| P1 | 主流程在常见或可预见故障下错误、丢数据、重复执行、状态失真或无法恢复 |
| P2 | 次要流程、边界条件、可维护性或一致性问题，存在明确影响但有可用绕过 |
| P3 | 文档漂移、冗余代码、诊断性不足或低风险测试缺口 |

## 3. 分阶段检查计划

### 阶段 1：仓库地图与架构基线

- [x] 读取 `AGENTS.md`、`ARCHITECTURE.md`、`README.md`、构建配置和测试配置。
- [x] 核对包目录、启动入口、进程/服务、数据库文件、插件和技能目录。
- [x] 使用静态搜索绘制依赖方向、SQLite 访问、Gateway 代理、旧包名/兼容入口和动态导入。
- [x] 记录架构文档与代码目录的第一处偏差。

证据命令：`rg --files`、`rg`、`scripts/python_architecture.py`、包元数据检查。

### 阶段 2：启动、请求和回合主链路

- [x] 从 `voidcube`/`vc` 入口追踪配置加载、服务编排、Agent 初始化、一次对话回合和退出流程。
- [x] 检查 API-A、Supervisor、Gateway、Memory Service 和 Desktop/CLI 的职责是否越界。
- [x] 检查同步/异步边界、任务取消、超时、关闭顺序、异常传播和状态终态。
- [x] 对照相关 CLI、runtime、application、supervisor 测试，找出未被测试保护的关键分支。

### 阶段 3：数据 owner、Memory 与并发恢复

- [x] 盘点所有 SQLite 打开点、repository、owner lease、事务和写入队列。
- [x] 确认客户端不直接持有 Memory 数据库或 Memory repository，不经 Gateway 走数据面。
- [x] 检查 `accepted`/`durable`/`committed` 语义、outbox drain、幂等键、重试和死信/最终失败。
- [x] 检查跨进程并发、busy、重启恢复、迁移、备份、integrity check 和 shutdown drain。
- [x] 运行针对 Memory、outbox、owner lease、并发和恢复的项目测试。

### 阶段 4：鉴权、请求协议和外部边界

- [x] 检查 Provider/model 配置、凭据解析、API key 注入、请求构造、错误映射和日志脱敏。
- [x] 检查 Memory scope、owner/workspace/domain、actor capability 和服务 token 是否由服务端校验。
- [x] 检查 HTTP/loopback、命名管道/本地 socket、Gateway 管理面和插件工具边界。
- [x] 搜索任意路径代理、客户端可控 actor、SQL/RPC 注入、裸环境变量信任和失效兼容分支。
- [x] 按仓库规则运行退役集成扫描及相关集成测试。

### 阶段 5：技能发现、插件、配置和打包

- [x] 追踪技能发现、索引、缓存、增量变更统计和 registry owner；确认文件系统仍是内容权威。
- [x] 检查插件 manifest、加载入口、工具 schema、权限声明和失效插件/旧入口。
- [x] 检查配置默认值、环境变量覆盖、版本迁移、生产/测试配置漂移。
- [x] 检查 wheel 包内容、顶层兼容包、数据文件、入口点和可加载技能是否出现双入口。
- [x] 若涉及 registry，固定运行 `pytest tests/test_skill_registry.py`，核对 `added/reparsed/reused/removed`。

### 阶段 6：测试、静态质量与收口

- [x] 运行项目虚拟环境中的相关分层测试和必要的全量测试。
- [x] 运行 `git diff --check`、架构检查、文档契约、wheel 契约和退役入口扫描。
- [x] 对每个问题确认复现证据、影响范围、是否已有测试、建议修复优先级和是否需要代码修改。
- [x] 汇总未覆盖风险、环境限制和合理下一步建议。

## 4. 跟踪记录

状态值：`未开始`、`进行中`、`已完成`、`阻塞`。每一步完成后更新状态、时间、命令/文件范围、发现和证据。

| 编号 | 阶段/步骤 | 状态 | 发现摘要 | 证据/位置 | 后续动作 |
|---|---|---|---|---|---|
| 1.1 | 读取规则与架构基线 | 已完成 | 已读取仓库规则、架构规范、README、构建与测试配置 | `AGENTS.md`、`ARCHITECTURE.md`、`README.md`、`pyproject.toml`、`pytest.ini` | 继续建立代码地图 |
| 1.2 | 统计目录与测试规模 | 已完成 | `src` 约 610 个文件/16.6 万行；Mem 约 70 个文件/2.6 万行；桌面端独立存在 | `rg --files`、目录统计 | 静态追踪入口和边界 |
| 1.3 | 建立依赖/数据流地图 | 已完成 | 确认 Gateway 是请求/注册边界；Memory/Session/Action Journal 分别拥有各自 SQLite；活跃代码未发现普通客户端直接打开 `memory.db` 的入口 | `scripts/python_architecture.py`；`rg` 对 `sqlite3.connect`、`memory.db`、Gateway client 和入口点的静态追踪 | 在阶段 3 复核 owner、事务和恢复语义 |
| 2.1 | 启动与退出 ownership | 已完成 | 根启动器无条件设置 daemon ownership；即使没有实际启动服务也会让下游注册全量退出清理 | `root_launcher.py:108-124`；`launcher.py:196-203`；`daemon_runtime.py:66-78`；最小复现见问题 VC-001 | 修复时应让 ownership 只由实际启动结果驱动，并区分服务集合 |
| 2.2 | 服务健康、端口和注册 | 已完成 | 未知端口占用在 `ensure_running()` 中只要 HTTP 200 就可能被视为健康服务；服务身份校验未贯穿该分支 | `service_launcher.py:731-747,1131-1143`；最小复现见问题 VC-002 | 统一返回启动结果类型，并在复用前强制身份校验 |
| 2.3 | Supervisor 并行启动与 Gateway registration | 已完成 | 冷启动时 Supervisor 可与 Memory 并行；后续仍检查 `supervisor`/`executor` registration。已有失败重启测试，未发现可单列的确定性状态错误；返回 `None` 的多义性是共因 | `service_launcher.py:1064-1110`；`tests/test_serve_config.py::test_ensure_running_restarts_supervisor_when_executor_registration_is_missing` | 与 VC-002 一并处理启动结果语义 |
| 2.4 | 一次回合线程、取消与异常传播 | 已完成 | Scheduler 的取消/终态覆盖较好；但 worker 线程未保存异常对象，真实线程异常会返回 `result=None`，原始异常只进入线程 stderr | `turn/execution.py:35-58`；`turn/agent_executor.py:134-146`；真实线程复现见 VC-003 | 增加异常 holder 和直接测试，评估是否需要将诊断带入失败结果 |
| 3.x | 数据 owner、Memory 与并发恢复 | 已完成 | 正常路径分层测试通过；确认 repository 关闭竞态和 outbox 过期租约后的陈旧 worker 写回风险已分别修复并回归；直接 SQLite 查询仅见于可复用查询对象/CLI pipeline，未证明为 Memory Service 热路径 | `Mem/src/memai/repository/sqlite_repository.py`、`plugins/memory/mem/outbox.py`、`Mem/src/memai/query.py`；`94 passed`、`105 passed`，并发最小复现与回归见 VC-004/VC-005 | 进入鉴权、请求协议和外部边界检查 |
| 4.x | 鉴权、请求协议和外部边界 | 已完成 | 初审确认 Gateway 管理面、Memory 单 token actor 绑定、命名 custom endpoint 凭据回退三项问题；本轮已分别修复并补回归测试。请求构造、错误映射和退役模型保护保持通过；退役集成扫描与相关测试已重新运行 | `internal_gateway.py:280-305,424-438`；`Mem/src/memai/transport/http_adapter.py:137-150`；`providers/runtime.py:264-282`；`providers/auxiliary_client.py:741-778`；本轮定向测试 `91 passed`，协议/鉴权测试 `88 passed`，退役/技能/打包集合 `82 passed` | 继续保留其他阶段结论，进入修复后收口 |
| 5.x | 技能、插件、配置和打包 | 已完成 | registry 热路径、变更统计、插件 manifest、wheel 内容和技能加载契约通过；未发现新的双入口或失效加载点 | `tests/test_skill_registry.py` `14 passed`；`tests/test_plugin_manifest_contract.py`、`tests/test_packaging_contract.py`、`tests/test_skills_sync_contract.py` `13 passed`；`scripts/python_architecture.py`；`scripts/build_wheel.py` | 进入最终测试与静态收口 |
| 6.x | 测试、静态质量与收口 | 已完成 | VC-001/VC-002/VC-003/VC-004/VC-005/VC-006/VC-007/VC-008 已完成修复验证，当前问题清单已闭环 | 本轮 `41 passed`、`6 passed`；`scripts/python_architecture.py` 通过；`scripts/build_wheel.py` 成功并验证 `dist/voidcube_agent-1.0.0-py3-none-any.whl`；`git diff --check` 通过 | 继续保留回归测试，收束最终结论和后续建议 |

## 5. 问题清单

| 编号 | 级别 | 状态 | 问题 | 位置 | 触发条件/影响 | 复现或验证 |
|---|---|---|---|---|---|---|
| VC-001 | P1 | 已修复 | 根启动器仅在实际启动过 daemon 时才宣称 ownership | `src/voidcube/interfaces/cli/root_launcher.py:108-128`；下游 `src/voidcube/interfaces/cli/launcher.py:196-203`；清理 `src/voidcube/infrastructure/gateway/daemon_runtime.py:66-78` | 仅在 `ensure_running()` 真的启动了至少一个服务时写入 `VOIDCUBE_DAEMONS_STARTED=1`；全部已运行时不再错误接管退出清理 | 新增 `tests/test_voidcube_launcher.py::test_auto_start_daemons_claims_ownership_only_after_real_start`；`tests/test_cli_daemon_runtime.py` 保持通过 |
| VC-002 | P1 | 已修复 | 未知端口占用不再被误判为 VoidCube 健康服务 | `src/voidcube/infrastructure/gateway/service_launcher.py:1136-1148`；`_health_endpoint_is_service()` 仍只在已知 owner 上参与复用 | `ensure_running()` 对未知 owner 直接返回 `running=False, healthy=False`，不会把只回 200 的陌生进程当成 VoidCube 服务 | 新增 `tests/test_serve_config.py::test_ensure_running_rejects_unknown_process_that_only_answers_http_200`；`tests/test_serve_config.py` 与 `tests/test_startup_performance.py` 通过 |
| VC-003 | P2 | 已修复 | 回合 worker 线程异常会在清理后回传到主线程 | `src/voidcube/interfaces/cli/turn/execution.py:23-64` | worker 异常被线程内捕获并在 join 后重新抛出，避免静默返回 `result=None`；cleanup/flush 仍按既有顺序执行 | 新增 `tests/test_cli_turn_execution.py::test_turn_execution_propagates_worker_exception_after_cleanup`；回归测试通过 |
| VC-004 | P1 | 已修复 | `SQLiteMemoryRepository.close()` 与写入入队没有原子 ownership 边界，关闭期间可能把 `_STOP` 放到仍未完成的写请求之前 | `Mem/src/memai/repository/sqlite_repository.py:225-276,529-546` | 新增 closing 阶段和在途入队计数，`close()` 先阻断新写入并等待在途入队结束，再投递 `_STOP`；不再让停止标记抢到未完成请求前面 | 新增 `test_sqlite_repository_close_waits_for_inflight_enqueue_before_stop`：关闭不会在请求入队前抢先投递 `_STOP`，请求和 close 均完成 |
| VC-005 | P1 | 已修复 | Outbox 旧租约 worker 在租约过期并被新 worker 接管后，仍可执行 `mark_delivered()`/`mark_failed()` 影响新 worker 的行 | `plugins/memory/mem/outbox.py:229-329` | 每次 `next_due()` 现在返回 `_outbox_lease_token`，settle 需要该 token；旧 worker 的迟到 settle 无法再匹配已接管的行。项目内 worker loop 已统一携带 token | 新增 `test_memory_outbox_stale_settle_token_cannot_delete_reclaimed_row` 和 token 化的 lease/交付回归；`tests/test_mem_memory_provider.py`、`tests/test_memory_service_boundaries.py`、`tests/test_gateway_activity.py` 通过 |
| VC-006 | P1 | 已修复 | Gateway 配置了 `auth_token` 时，管理面和健康写接口仍允许匿名访问/修改 | `src/voidcube/infrastructure/gateway/internal_gateway.py:280-305,424-438`；控制面调用方 `gateway/presence.py`、`service_launcher.py`、CLI status、Supervisor runtime | 新增统一控制面路径判定与 root token 校验，覆盖 `/register`、`/admin/*`、`/health/{service_id}`、`/v1/sessions*` 和 improvement report；根健康探针与 `/api/*` 数据代理保持原边界。注册处理器中的重复鉴权分支已删除；CLI、启动器和 Supervisor 已统一补发 Gateway token | 新增 `test_gateway_control_plane_requires_root_token_when_configured`：匿名/错误 token 返回 401，正确 token 可访问控制面，根健康返回 200；Gateway activity、application services、UI adapter 和 Supervisor verification 测试通过 |
| VC-007 | P1 | 已修复 | 单一 Memory `service_token` 被默认复制给所有 MemoryActor，凭据不能约束 actor capability | `Mem/src/memai/transport/http_adapter.py:137-150`；组合根 `Mem/src/memai/application/memory_service.py:5645-5650` | 单 token 现在只绑定 `service_actor`，不会再为所有 `MemoryActor` 自动生成 token 映射；按 actor 的 `service_tokens` 多 token 路径保持不变 | 新增 `test_memory_http_adapter_single_token_is_bound_to_configured_actor`：`api_a` token 可用，`governor` 和 `memory_maintenance` 使用同 token 返回 401；Memory service boundary 集合通过 |
| VC-008 | P1 | 已修复 | 命名 custom endpoint 缺少自身凭据配置时，会把全局 Provider 密钥作为该 endpoint 的 Authorization 凭据 | `src/voidcube/infrastructure/providers/runtime.py:264-282`；`src/voidcube/infrastructure/providers/auxiliary_client.py:741-778` | runtime 删除 `OPENAI_API_KEY`/`OPENROUTER_API_KEY` 的命名 provider 回退；auxiliary client 改为复用同一 named custom runtime resolver，并将缺凭据转换为不可用结果。显式 custom URL 的兼容回退仍与命名 provider 分离 | 新增 `test_named_custom_provider_does_not_fall_back_to_global_api_key`：全局 key 存在时 runtime 抛出 `AuthError`，auxiliary 返回 `(None, None)`；provider registry 集合通过 |

## 6. 最终结论模板

- 架构符合项：Gateway 管理面、skill registry、wheel 契约和大部分数据 owner 边界都与文档一致。
- 已修复 P1 问题：VC-006, VC-007, VC-008。
- 仍待修复 P0/P1 问题：无。
- P2/P3 问题：无。
- 测试与静态检查结果：本轮定向修复测试 `91 passed`；协议/鉴权测试 `88 passed`；退役、skill registry、插件 manifest、wheel 集合 `82 passed`；`scripts/python_architecture.py`、`scripts/build_wheel.py`、`git diff --check` 均通过。
- 环境或范围限制：原 `test_supervisor_room_uses_rest_scene_only_for_daily_companion` 已删除。经程序链路复核，该测试直接修改 `_service_runtime.stellar_mode` 后立即读取 UI 的旧缓存，绕过了 `_start_autonomous_chain_gate()` / `_stop_autonomous_chain_gate()` 正式切换入口，因此不再作为程序基线。
- 建议下一步：以当前程序运行链路为唯一基线，后续先审查正式模式切换入口、缓存失效和真实 API/UI 调用；再单独更新失效测试与文档，避免反向驱动主逻辑。

## 7. 修复跟踪记录（2026-08-30）

| 问题 | 实施 | 验证 | 状态 |
|---|---|---|---|
| VC-006 | Gateway 新增集中式控制面 root token middleware，删除注册处理器的重复鉴权 | `tests/test_gateway_activity.py`；匿名、错误 token、正确 token 矩阵 | 已完成 |
| VC-007 | Memory 单一 `service_token` 仅绑定 `service_actor`，保留显式 `service_tokens` 映射 | `tests/test_memory_service_boundaries.py`；actor/token 矩阵 | 已完成 |
| VC-008 | runtime 与 auxiliary named custom provider 统一使用 provider 自身凭据，删除全局 key 回退 | `tests/test_provider_registry_contract.py`；runtime/auxiliary 双路径 | 已完成 |
| 修复后总体验证 | 运行退役集成、协议、技能 registry、插件、wheel、架构和 diff 检查 | `88 passed`、`82 passed`；架构通过；wheel 已验证 | 已完成 |
| 控制面调用方适配 | CLI presence/status、service launcher、Supervisor 活动/trace/gate/UI 请求统一携带 Gateway token | `tests/test_application_services.py`、`tests/test_ui_memory_status_adapters.py`、Supervisor verification 回归 | 已完成 |
| VC-001/002/003 终轮修复 | 根启动器 ownership、服务端口身份复用、回合 worker 异常传播一并收束 | `tests/test_voidcube_launcher.py`、`tests/test_serve_config.py`、`tests/test_cli_turn_execution.py` | 已完成 |
| 最终静态收口 | 重新跑架构、打包和 diff 契约 | `scripts/python_architecture.py`、`scripts/build_wheel.py`、`git diff --check` | 已完成 |
| 过期测试清理 | 删除绕过正式模式切换入口、与当前 UI 状态缓存语义冲突的独立测试；不修改主程序 | `tests/test_supervisor_runtime_wiring.py`；`service_runtime.py` 正式切换入口；`ui_runtime.py:_stellar_mode_status()` | 已完成 |
