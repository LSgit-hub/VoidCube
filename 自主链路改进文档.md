# 自主链路改进跟踪

## 范围

本文件跟踪 API-B 经 Supervisor、员工调度器、员工 Agent 执行，再回写到任务状态的完整链路。覆盖两种入口：

- Assist（辅助模式）：用户语义由 API-B 判断并委派给员工。
- Auto（自动模式）：内生驱动/API-B 产生自学习、自改进任务并委派给员工。

## 当前基线

- [x] API-B 可创建员工 scheduled task。
- [x] scheduled task 使用 SQLite 持久化，完成回写有 outbox。
- [x] Auto chain 有 `AutonomousChainTask`、审批和 reconcile 投影。
- [x] Assist 与 Auto 共用 `AutonomousChainTask` canonical 状态机，并通过 `autonomous_task_id` 关联员工 scheduled run。
- [x] Auto gate 停止后，已排队 Auto task 会被 claim 端过滤。
- [x] Auto gate 停止会取消已启动的 Auto scheduled run，并释放 chain lease。
- [x] scheduled worker claim 后会取得并续租 autonomous execution lease。
- [x] 自改进 side effect 已接入 lease 校验；commit lineage/验证证据已在员工回写边界强制校验。
- [x] 过期 scheduled run 的迟到 finish 会被拒绝。
- [x] 员工执行器由主 CLI 持有；没有 CLI claim 时，队列快照明确报告 `waiting_for_employee_executor`。

## 修复计划与验收标准

### P1-A：停止门控与取消

状态：已完成第一版（P1-B 的 commit 证据项转入 P3）

验收：

1. Auto gate 关闭后，`requested_via=autonomous_worker` 的 queued task 不得被 claim；Supervisor 端也强制执行该过滤。
2. 已运行的 Auto scheduled run 在 gate 关闭时被取消，后台 Agent 通过 lease/heartbeat 失败路径收到 interrupt/cancel。
3. Assist（`companion_delegate`、`companion_media`、`companion_voice`）不受 Auto gate 过滤影响。
4. 停止后的任务状态、scheduled run 状态和最终回写保持一致。

### P1-B：执行租约与自改进边界

状态：已完成第一版

验收：

1. 员工 claim 后获得对应 `AutonomousChainTask` 的 generation/attempt lease。
2. Agent 的副作用工具能够调用可信 lease validator，并由 scheduler heartbeat 续租。
3. stale lease 或 task/generation 不匹配时，副作用和最终回写均被拒绝。
4. body improvement 的结果必须包含可验证的 task、commit lineage 和验证证据。

### P1-C：过期回写防护

状态：已完成第一版

验收：

1. `lease_expires_at` 已过期的 run 不能被 finish 为 completed/failed，恢复结果会持久化为 failed。
2. 过期 run 只能由恢复流程标记失败并释放调度槽位。
3. 重复 finish 保持幂等，不能覆盖新一代 run 的结果。

### P2：状态统一与运行依赖

状态：已完成第一版

验收：

- Assist 和 Auto 都能关联 canonical task、员工 run、结果和审计事件。
- 没有 CLI employee executor 时，系统明确报告“等待执行器”，而不是静默停留在已派发。

实现要点：

1. `companion_delegate`、`companion_media` 和 `companion_voice` 均创建 Assist canonical task，任务完成后由 employee reconcile 回写。
2. canonical task id 不再决定 Auto/Assist gate；claim source `requested_via=autonomous_worker` 才属于 Auto，避免 Assist 被 Auto gate 误过滤。
3. daily companion 周期即使关闭可选 observation，也会继续运行 employee reconcile。
4. Assist canonical task 标记 `assist_mode`，不会被 endogenous Auto review 二次判断。
5. scheduled-task 与 worker execution 快照提供 `employee_executor.status`、队列/运行计数和 claim 能力说明。
6. Auto gate 停止只取消 `autonomous_worker` 员工 run，不取消正在执行的 Assist run；recurring Assist schedule 每次完成后轮换下一代 canonical task。

### P3：自改进证据闭环

状态：已完成第一版

- body improvement 的 employee 结果必须绑定 canonical task、commit lineage、changed files 和验证证据。
- 证据缺失或与 lease generation 不匹配时，结果只能进入 failed/reconcile，不得形成 completed 投影。

实现要点：

1. body improvement 员工结果必须以 JSON（允许 code fence）返回 `task_id`、`lease_generation`、`attempt_id`、baseline/candidate commit、changed files 和 verification。
2. 回写会校验结果与当前 autonomous execution lease、任务预授权 lineage/changed files 一致；缺失或不一致统一拒绝 completed 投影。
3. 员工任务指令现在包含 execution lease 和结构化结果契约，减少自由文本回写造成的证据丢失。
4. 员工回写通过后再调用 `BodyImprovementReviewService.review()`，对目标槽位、容器执行环境、真实 commit、worktree 干净度、祖先关系、changed files 和治理授权做二次核验；评审异常或拒绝同样只能形成 `failed`。

## 测试要求

- 运行环境：项目 `.venv`。
- 增加 API-B → claim → employee → finish → reconcile 的端到端覆盖。
- 增加 gate 停止竞态、过期 lease、重复 finish、Assist/Auto 隔离测试。
- 涉及技能 registry 的改动才运行 `pytest tests/test_skill_registry.py`；本轮不修改 registry。

## 变更记录

### 2026-08-21

- 建立跟踪文档。
- 已确认：Auto 停止后的 claim 过滤缺失、员工 lease 未接入、过期 finish 可接受。
- 完成第一版 gate claim 互斥、员工 autonomous lease claim/validate/renew、gate 停止取消和过期 finish 防护。
- 新增 `tests/test_autonomous_chain_execution_boundaries.py` 覆盖 claim 隔离、lease 绑定和过期回写。
- P2 新增 Assist canonical handoff、等待执行器、recurring successor 和 API-B → claim → finish → reconcile 端到端覆盖。
- P3 新增 body improvement 证据完整性、lease mismatch 和拒绝 completed 投影测试。
- P3 新增真实 body review 回写门、评审拒绝不得完成测试；针对性边界与打包集合 `22 passed`。
- 修复 Auto gate 停止时尚未生成 employee assignment 的直接运行任务未被失败回写的问题；Assist canonical task 仍按 `assist_mode` 保留。
- 验证：自主链路与打包集合 `48 passed`；P3 针对性集合 `22 passed`；Ruff、compileall、架构退役扫描、wheel contract、`git diff --check` 通过。
- 现场故障复盘：Auto 任务曾因 Gateway `6000` 短时不可用，CLI 员工 claim 轮询对网络错误静默放弃，持续显示“待认领”但没有实际执行。已增加 claim 失败后的 Gateway/Memory/Supervisor 自动恢复并重试一次；HTTP 业务拒绝不重试。
- 现场故障复盘：Supervisor 重启后 Auto gate 已恢复为 `daily_companion/false`，CLI 本地仍残留 AUTO 激活标记，造成界面与服务状态分裂。已增加 Supervisor 模式同步，发现本地过期 AUTO 标记时清除并停止本地 autonomous scheduler。
- 可观测性修复：自主链路有已派工任务但 Gateway 没有有效 CLI employee executor、或 executor lease 已过期时，UI 现在显示“执行器失联”，不再伪装成普通“待接手”；状态会携带重启 CLI employee executor 的恢复提示。
- 本次验证：调度器 Gateway 恢复、CLI 状态同步和 UI 失联投影测试 `6 passed`。

## 当前现场状态（2026-08-21）

- Gateway 已恢复监听 `6000`，Supervisor Auto gate 已重新激活为 `auto_evolution`，review loop 与 drive loop 正常运行。
- 故障复核时旧 CLI employee executor session `20260821_194905_5b0a63` 已 stale，Gateway 当前没有可用 CLI employee executor；故障时遗留的 approved/reconciling 任务随后已由恢复流程落为 `completed/failed`，不会继续等待其自动回写。
- 若后续 Auto 重新产生员工任务，需要重启当前 CLI，或重新打开一个 CLI employee executor；新 executor 启动后才具备 claim 能力。
