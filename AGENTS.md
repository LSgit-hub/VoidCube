# VoidCube Repository Rules

1. 完成阶段任务后给出合理的下一步建议。
2. 修改文件主逻辑后，删除已经失效的代码、参数和冗余兼容分支，不要让后续会话把旧兼容逻辑重新当成主逻辑。
3. Anthropic、Claude 和 Codex 已明确退役。不得新增、恢复或保留它们的 API 调用、Provider、OAuth、适配器、协议兼容、别名、回退、技能示例、市场缓存或隐藏入口，除非用户以后明确撤销本条约束。
4. 涉及模型、鉴权、请求协议、技能或打包的改动完成前，必须运行退役集成扫描和相关测试；活跃代码、可加载技能与 wheel 应保持零入口。
