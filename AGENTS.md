# VoidCube Repository Rules

1. 完成阶段任务后给出合理的下一步建议。
2. 修改文件主逻辑后，删除已经失效的代码、参数和冗余的兼容分支，不要让后续会话把旧兼容逻辑重新当成主逻辑（如果兼容为回退拖地方案可保留）。
3. 涉及模型、鉴权、请求协议、技能或打包的改动完成前，必须运行退役集成扫描和相关测试；活跃代码、可加载技能与 wheel 应保持零入口。
4. 测试使用项目虚拟环境。
