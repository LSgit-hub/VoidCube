# NOTE: This file is maintained for convenience only.
# The canonical dependency list is in pyproject.toml.
# Preferred install: pip install -e ".[all]"

# Core dependencies
openai
python-dotenv
fire
httpx
rich
tenacity
prompt_toolkit
pyyaml
requests
jinja2
pydantic>=2.0
PyJWT[crypto]
debugpy

# Web tools
firecrawl-py
parallel-web>=0.4.2
beautifulsoup4
lxml

# Image generation
fal-client

# Text-to-speech (Edge TTS is free, no API key needed)
edge-tts

# Optional: For cron expression parsing (cronjob scheduling)
croniter

# Optional: For messaging platform integrations (gateway)
python-telegram-bot[webhooks]>=22.6
discord.py>=2.0
aiohttp>=3.9.0

# ─── VoidCube-web-ui (Python FastAPI backend) ───────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
aiofiles>=24.0.0
pyyaml>=6.0.2
httpx>=0.28.0
websockets>=14.0
qrcode>=8.0
pillow>=11.0
markdown>=3.7
psutil>=6.1
python-multipart>=0.0.12

# ─── VoidCube-desktop (PyWebView 桌面壳) ──────────────────────
pywebview>=5.0
mss>=9.0
pytesseract>=0.3
Pillow>=11.0
