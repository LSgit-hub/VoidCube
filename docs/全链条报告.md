# 全链条报告：自主链路与用户链路边界审查

更新日期：2026-07-04

> 本报告按本轮最新语义重新生成，不沿用已删除的历史全链条报告或历史问题清单作为判断来源。

## 1. 审查基准

本报告按最新《voidcube架构基线》审查全执行链路。当前最高优先级语义如下：

- **自主链路**：API-B 内生驱动自主感知、判断并生成治理投影 / 任务；Web 监控展示 API-B 的状态、反馈、任务治理和自主任务回报；API-A 自主执行面（当前表现为由 `/auto` 临时门控的最小迷你 CLI / 子代理通道）拉取并执行 `self_learning` 与 `body_improvement`；成果写入 Mem；下一轮 API-B 内生驱动读取、管理和再判断。
- **用户链路**：用户使用主 CLI 调用 API-A 主 Agent 运行，走 `user_chat` 泳道，服务用户当前请求。
- **硬边界**：自主链路和用户链路必须互不抢占、互不覆盖、互不解释成同一个 CLI 模式。`/auto` 与 `/auto-q` 只是当前自主链路的临时启停开关，不是主 CLI 的用户交互模式。Web 监控只监控展示 API-B 的动作、状态、反馈、任务治理和自主任务回报，不成为用户聊天入口。

## 2. 结论

设计方向基本符合要求，但此前文档存在三个语义偏差：

1. **链路命名不够硬**：已有 `user_chat / supervisor_task` 双泳道和 `/auto` 临时门控说明，但没有把“自主链路 / 用户链路”作为一等架构边界，容易让后续实现把 `/auto` 开关继续理解成主 CLI 的一种模式。
2. **执行面表述偏粗**：多处写成“活跃 Agent 执行学习任务”，容易误解为用户主 CLI / 主 Agent 被后台任务复用。最新语义应写成 API-A 自主执行面执行，由最小迷你 CLI / 子代理通道承载，并与用户主 CLI 隔离。
3. **Web 监控边界偏宽**：旧表述说 Web UI 是“所有任务统一可观测窗口”，容易扩张成监控用户链路或聊天内容。最新语义应限定为 API-B 观测面，只展示 API-B 动作状态与自主任务回报。

本轮已将这些语义同步到：

- [voidcube架构基线.md](./voidcube架构基线.md)
- [内生驱动核心设计.md](./内生驱动核心设计.md)
- [项目文件架构说明.md](./项目文件架构说明.md)
- [CLI展示与gateway双槽设计.md](./CLI展示与gateway双槽设计.md)

## 3. 对最新语义的链路对齐

### 3.1 自主链路

```text
API-B / Supervisor 内生驱动
  → 自主感知用户、系统、记忆、身体、治理状态
  → 形成 judgement / need / intent / governance output
  → 将适合 API-A 执行的创造类投影放入任务列表
  → API-A 自主执行面（最小迷你 CLI / 子代理通道）pull 任务
  → 执行 self_learning / body_improvement
  → 将学习成果、替身改进报告、任务 outcome 写入 Mem
  → 下一轮 API-B 读取 Mem 与任务历史继续判断
```

符合点：

- 任务生产者归 API-B / Supervisor。
- 任务执行者归 API-A 自主执行面，不归 API-B。
- 成果闭环归 Mem，再被 API-B 读取。
- Web 监控只展示 API-B 侧状态和自主任务回报。

### 3.2 用户链路

```text
用户
  → 主 CLI
  → API-A 主 Agent (`user_chat`)
  → 工具 / 临时上下文 / 必要记忆访问
  → 主 CLI 输出
```

符合点：

- 用户链路不依赖自主任务队列。
- 用户主 CLI 不承担自主任务执行槽。
- `/auto` 开关不得影响主 CLI 输入或主 Agent 交互。

## 4. 全链路问题清单

### P0：必须立即守住的边界

| # | 问题 | 风险 | 现状 / 处理 |
| --- | --- | --- | --- |
| P0-1 | 文档未把“自主链路 / 用户链路”定义成一等边界 | 后续实现继续把 `/auto` 开关当成主 CLI 模式，或把后台任务接到用户链路 | 已在基线新增 §2.1，并同步相关文档 |
| P0-2 | “Agent 执行自主任务”表述过宽 | 误导为活跃用户主 Agent / 主 CLI 直接承担后台学习和改代码 | 已改为“API-A 自主执行面 / 最小迷你 CLI / 子代理通道” |
| P0-3 | Web 监控职责边界过宽 | Web 小屋可能被扩张成用户聊天入口或用户链路监控面 | 已改为“只观测 API-B 与自主任务回报” |
| P0-4 | `/auto` 门控语义容易回退 | `/auto` 开关可能再次被实现成独占主 CLI、阻断用户输入或占用用户展示 | 已统一写成“自主链路临时启停开关”；后续代码和 UI 文案仍需持续检查 |

### P1：实现与文档需要继续核对的点

| # | 问题 | 风险 | 建议 |
| --- | --- | --- | --- |
| P1-1 | `cli.py` 中由 `/auto` 门控的自主执行面仍与主 CLI 代码体量耦合很深 | 新会话维护时容易把最小迷你 CLI 当成主 CLI 的一部分继续扩张 | 本轮已清理高风险文案；后续再抽出“自主执行面”命名层或独立模块，避免 UI/状态/执行逻辑和用户交互混写 |
| P1-2 | dashboard / status / Web 小屋三类观测面的命名仍有历史残留 | 使用者不清楚哪个看 API-B、哪个看 `supervisor_task`、哪个看主 CLI | 本轮已统一关键文案：Web 小屋 = API-B 观测；最小 CLI dashboard = supervisor_task 子代理观测；主 CLI status = 用户链路状态栏 |
| P1-3 | `systems/self_learning/` 仍作为过渡实现存在 | 容易被误当成独立“自学系统”服务 | 本轮已降级标注为 legacy conclusion contract/store 与 Supervisor payload builder；生产归监督者，执行归 API-A 自主执行面，存储归 Mem；executor health 中的旧执行广告已删除 |
| P1-4 | `user_chat / supervisor_task` 双泳道是实现层术语，但“自主链路 / 用户链路”是架构层术语 | 文档和代码讨论容易混用层级 | 在新增文档、注释和 UI 文案中优先使用“用户链路 / 自主链路”，再括注 lane 名称 |
| P1-5 | 旧的大写自动门控词残留在注释、测试和内部哨兵中 | 后续会话可能把临时 `/auto` 开关误认为主 CLI 的模式或自主链路本体 | 本轮已清理为 autonomous-chain gate / autonomous executor；`/auto` 仅保留为命令和临时开关 |
| P1-6 | 自主链路开关仍沿用旧治理模式命名 | 容易把 Supervisor 自主链路门控误读成身体 Governor 或用户 CLI 模式 | 本轮已破坏性改名为 autonomous-chain-gate，不保留旧路由和旧状态键 |
| P1-7 | Gateway 仍可把 self_learning 注册成独立服务路由 | 旧“自学服务执行器”可能从 Gateway 自动路由侧复活 | 本轮已删除 Gateway 的 self_learning 独立服务统计、自动路由和代理 activity 分支 |
| P1-8 | 学习结论 store 仍通过包级 / 顶层导出暴露 | 新代码容易从 `systems` 或 `systems.self_learning` 把它当主服务依赖 | 本轮已删除 `systems` 顶层 self_learning 导出，并让 store 只能从明确的 conclusion_store 模块显式导入 |
| P1-9 | 学习结论 store 类名仍带 Service | 容易继续暗示存在独立自学运行服务 | 本轮已破坏性改名为 `SelfLearningConclusionStore`，文件改为 `conclusion_store.py` |
| P1-10 | `systems` 顶层包仍重导出大量运行对象 | 新代码可能绕过模块边界，从总入口拿服务 / adapter / governor 对象 | 本轮已清空顶层运行对象重导出，要求从所属子模块显式导入 |
| P1-11 | 身体生命周期旧控制器别名仍指向 executor | 容易把身体生命周期机械执行面重新理解成控制器 / 决策器 | 本轮已删除旧别名，统一使用 `BodyLifecycleExecutor` |

### P2：后续验证项

| # | 验证项 | 目标 |
| --- | --- | --- |
| P2-1 | 主 CLI 用户会话运行时开启 / 关闭 `/auto` 开关 | 用户输入、主 Agent 输出、主 CLI 子代理展示不被自主链路覆盖 |
| P2-2 | 自主链路执行 `self_learning` 并产生子代理 | 最小 CLI / dashboard 只显示 `supervisor_task` 子代理，主 CLI 不混入 |
| P2-3 | Web 小屋刷新 API-B 状态 | 只展示监督者状态、任务治理、反馈、Mem 状态和自主任务回报，不展示用户聊天内容 |
| P2-4 | 自主任务完成并写入 Mem | 下一轮 API-B 内生驱动能读取学习成果 / 改进报告，形成闭环 |
| P2-5 | Gateway lane 同时存在 `user_chat` 与 `supervisor_task` | 两个 lane 互不覆盖，不能退回 last-writer-wins 单槽解释 |

## 5. 当前对齐后的架构判断

按最新语义，VoidCube 的全执行链路应被理解为“双链路共享底座”：

- 用户链路保证用户当前交互稳定、直接、优先。
- 自主链路负责 API-B 内生判断、API-A 自主学习/改进、Mem 闭环和 Web 可观测。
- 两者共享 API-A 能力域和 Mem，但必须通过 `user_chat / supervisor_task`、主 CLI / 最小迷你 CLI、用户输出 / API-B Web 观测这三层边界隔离。

因此，当前设计在目标方向上是成立的；主要问题不是缺组件，而是旧文档和部分命名仍可能把“自主链路”误读成“主 CLI 的另一种运行模式”。本轮已经把这层语义写入基线，后续应优先清理代码和 UI 文案中的旧叫法，避免兼容逻辑再次长成主逻辑。

## 6. 2026-07-03 跟进收口

本轮继续按 P1 清理高风险命名残留：

- `cli.py`：将自主链路面板、`/auto` / `/auto-q` 提示、任务回写 reason、应急退出提示中的历史执行面叫法改为“自主链路 / API-A 自主执行面 / autonomous executor”。
- `VoidCube_cli/ops/dashboard.py`：明确最小 dashboard 观测自主链路的 `supervisor_task` lane，不再写成临时 `/auto` 门控视角。
- `systems/supervisor/service_runtime.py`、`supervisor.py`、`planning_runtime.py`、`ui_runtime.py`：将注释、日志和审计 reason 中的历史门控 / 执行面叫法收口为 “autonomous-chain gate” 与 “API-A autonomous executor / autonomous-executor tasks”。
- `docs/voidcube架构基线.md`：同步更新历史进度条目，避免继续写“活跃 Agent 拉取自主任务”。

### P1-3 跟进：self_learning 兼容层降级

- `systems/self_learning/conclusion_store.py`：明确 `SelfLearningConclusionStore` 只是历史学习结论的兼容记录层和 Supervisor payload builder，不是自主任务执行服务。
- `VoidCube_cli/ops/serve.py`、[项目文件架构说明.md](./项目文件架构说明.md)、[voidcube架构基线.md](./voidcube架构基线.md)：同步说明 `systems/self_learning/` 不是独立自学系统，学习任务执行归 API-A 自主执行面。
- `systems/execution/service.py`：不再宣称存在 `self_learning` executor 执行入口；真实 `self_learning` 执行走 API-A pull path。
- `systems/architecture.md`：修正旧入口文档，不再指向已不存在的 `docs/architecture-integration.md`，并移除“自提升只在时间窗口内”与“自学系统只研究”的旧约束。
- `tests/test_gap_coverage.py`：测试说明改为“学习结论兼容层”，明确其只是历史学习结论兼容层。

### P1-4 跟进：body_improvement 与 body_switch 不再混同

- `systems/runtime_task_profile.py`：`body_improvement` 继续归入 `body_upgrade` 执行族；`body_switch` 保持独立 task family / execution kind，避免替身改进和身体切换在治理、审计、Mem lineage、用户同意门上被合并。
- [endogenous-drive-body-upgrade-gap.md](./endogenous-drive-body-upgrade-gap.md)：同步修正旧说明，不再建议把 `body_switch` 归入 `body_upgrade`。
- `tests/test_mem_governor_bridge.py`：更新旧断言，使 Mem Governor 审计记录保留 `body_switch`，与 queue / lifecycle / body registry 的最新边界一致。
- `tests/test_runtime_task_profile.py`：新增纯函数回归，锁住 `body_improvement -> body_upgrade`、`body_switch -> body_switch`，以及两者 broad task 仍属于 `self_evolution`。

### P2-5 跟进：Gateway 双 lane 回归增强

- `tests/test_gateway_activity.py::test_gateway_agent_lanes_keep_supervisor_and_user_chat_separate` 已覆盖 `supervisor_task` 与 `user_chat` 同时存在时互不覆盖。
- 本轮补充 top-level 对照断言：后写的 `user_chat` 仍会覆盖旧 top-level `agent.scene`，但 `lanes.supervisor_task` 必须保留学习任务状态。这明确证明下游必须读 lane，不能退回 last-writer-wins 单槽解释。

### P2-1 跟进：`/auto` 开关不改变用户回合身份

- `cli.py`：`_current_cli_agent_role()` 不再仅因 `_autonomous_gate_active=True` 就返回 `supervisor_task`；只有当前存在自主任务，或当前 `chat()` 回合明确绑定自主任务 run id，才进入自主 lane。
- `cli.py`：`chat()` 在每个模型回合开始时记录 `_active_chat_agent_role`，普通用户输入即使发生在 `/auto` 开关开启期间，也按 `user_chat` 上报。
- `cli.py`：`_current_gateway_presence_snapshot()` 在用户回合期间不暴露自主任务 `task_id / execution_kind`，避免主 CLI 用户链路覆盖或污染 `supervisor_task` 观测。
- `tests/test_cli_autonomous_gate.py`：新增回归，确认 `/auto` 开启且存在自主任务时，普通用户回合仍推送 `agent_role=user_chat`；同时保留自主任务 presence 推送 `agent_role=supervisor_task`。

### P2-2 跟进：自主子代理观测与主 CLI 状态分离

- `VoidCube_cli/ops/dashboard.py`：最小 dashboard 的 API-A 段继续优先读取 `lanes.supervisor_task`，作为自主链路观测面，不被主 CLI 的 `user_chat` 子代理覆盖。
- `VoidCube_cli/status.py`：主 CLI status 的 API-A 段在 Gateway 提供 lanes 时优先读取 `lanes.user_chat`，避免自主链路 self_learning 子代理混入用户链路状态栏。
- `tests/test_scene_status_observability.py`：新增双向回归：dashboard 只展示 `supervisor_task` 的学习子代理；主 CLI status 只展示 `user_chat` 的用户回合子代理。

### P2-3 跟进：Web 小屋只展示 API-B / 自主链路观测

- `systems/supervisor/trace_runtime.py`：Web 小屋读取 Gateway timeline 时新增过滤边界，排除 `user_request` 与 `agent_scene`，避免用户链路请求、用户聊天子代理焦点或 prompt preview 出现在 API-B 监控面。
- Gateway 仍可记录用户链路活动，用于 idle 判断、审计和双 lane 聚合；但 Web 小屋 timeline 只吸收 API-B 治理与自主任务相关事件，例如 `self_learning`、`self_evolution`、`memory_task`、`body_upgrade`、`body_switch`。
- `tests/test_supervisor_runtime_wiring.py`：新增回归，向同一 trace 注入 `self_learning`、`user_request`、`agent_scene` 三类 Gateway 事件，断言 Web timeline 只保留自主链路事件，且用户聊天内容不会渲染。

### P2-4 跟进：自主任务完成结果进入 Mem 与下一轮 API-B 判断

- `systems/gateway/internal_gateway.py`：完成回写时保留 API-A 自主执行面的 `final_response` 并转发给 Supervisor；同时继续把该文本作为 `autonomous_task_finding` 写入 Mem Tier1，Mem 写入失败不影响任务完成状态。
- `systems/supervisor/planning_runtime.py`：`completed / failed` 决策会把 `final_response` 收进 decision context，并在 `endogenous_drive_history.outcomes` 中记录 `autonomous_executor_final_response` 与 `outcome_summary`。
- 这样下一轮 `evaluate_endogenous_drive()` 注入 `drive_history` 时，API-B 不只知道“任务完成了”，也能读到“API-A 自主执行面学到 / 改进了什么”。
- `tests/test_gateway_activity.py`：增强回归，确认 Gateway 写 Mem Tier1 的同时也把 `final_response` 转发给 Supervisor。
- `tests/test_supervisor_self_evolution_queue.py`：新增闭环回归，完成一个 endogenous `self_learning` 任务后，确认结果文本进入 history outcome，并能出现在下一轮 drive context 中。

### P2-6 跟进：自主执行面逻辑从主 CLI 中抽出第一层

- `VoidCube_cli/autonomous_executor.py`：新增 API-A 自主执行面辅助模块，承载自主任务执行类型识别、任务 prompt 生成、run id 绑定与 prompt-to-run 匹配。
- `cli.py`：主逻辑已不再直接拼接自主任务 prompt，内部 run id / prompt 临时键已收口为 `_autonomous_*` 命名。
- `tests/test_autonomous_executor.py`：新增模块级回归，锁住 `self_learning` 与 `body_improvement` 的自主 prompt、body baseline 捕获和 run id 匹配。

### P2-7 跟进：自主执行面生命周期从主 CLI 中抽出第二层

- `VoidCube_cli/autonomous_executor.py`：新增 `AutonomousExecutorRuntime`，承载自主任务恢复、pull、running 回写、completed / failed 回写、timeout、interrupt、body improvement report 提交等生命周期逻辑。
- `cli.py`：`_find_owned_running_autonomous_task()`、`_poll_autonomous_workflow()`、`_post_autonomous_task_decision()`、`_interrupt_current_autonomous_task()`、`_report_current_autonomous_task_timeout_if_needed()`、`_submit_body_improvement_report()` 均收口为 runtime wrapper；主 CLI 不再内联自主任务生命周期状态机。
- Gateway activity 的自主任务起始来源从历史门控视角收口为 `autonomous_pull`。
- `tests/test_cli_autonomous_gate.py` 与 `tests/test_autonomous_executor.py` 覆盖迁移后的 wrapper 和模块行为，确保用户链路不受影响。

### P2-8 跟进：内部执行面命名收口为 autonomous

- `cli.py`：内部状态、panel、Gateway 状态缓存、执行事件、当前任务、run id、timeout/writeback/interrupt 方法均从 `_auto_*` 收口为 `_autonomous_*` 或 `_autonomous_gate_*`；`/auto` 只保留为用户命令和临时门控名称。
- `VoidCube_cli/autonomous_executor.py`：任务 prompt 与 run id 临时键统一使用 `_autonomous_*` 命名。
- `systems/gateway/internal_gateway.py`：Mem Tier1 finding source 统一使用 `autonomous_task_finding`。
- `tests/test_cli_autonomous_gate.py`：测试文件与测试函数名同步使用“autonomous gate”主语。
- 文档统一使用“`/auto` 开关 / 自主链路临时门控”，避免后续把 `/auto` 理解成用户链路模式。

### P2-9 跟进：广义自动门控残留清理

- `cli.py`：内部唤醒 / 超时哨兵改为 `__AUTONOMOUS_*__`，避免控制流名称继续暗示旧的 CLI 模式。
- `systems/gateway/internal_gateway.py`、`systems/supervisor/config_models.py`、相关测试与文档：历史大写门控、模式和队列表达已收口为 “autonomous-chain gate” 或 “`/auto` 临时开关”。
- `docs/内生驱动核心设计.md`：`/auto` 明确为自主链路临时启停门控；内生驱动内部的 `auto` 仅指姿态自动选择，不等于 CLI 开关。
- 破坏性改名：单次同步触发路由改为 `/self-evolution/autonomous-cycle`，运行时入口改为 `run_autonomous_cycle()`，UI 活动事件改为 `autonomous_cycle_completed`；旧路由不再注册。
- 保留项：`/auto`、`/auto-q` 是当前兼容命令；环境变量里的自动阈值 / 自动打开前缀表示通用配置，不作为自主链路主语。
- 回归对齐：测试已改为验证 active sessions 作为软感知信号而非硬阻断；Web 小屋窗口展示“执行模式 / 全天候自动执行中”，不再回到旧时间窗语义。

### P2-10 跟进：自主链路开关破坏性改名

- Supervisor 开关路由改为 `/autonomous-chain-gate/activate`、`/autonomous-chain-gate/deactivate`、`/autonomous-chain-gate/status`，旧开关路由不再注册。
- Gateway admin 通知路由改为 `/admin/autonomous-chain-gate`，返回键改为 `autonomous_chain_gate_active`。
- Supervisor service runtime、内生驱动 perception、idle window、Web 小屋状态和 CLI `/auto` 调用均改用 `autonomous_chain_gate_active`。
- Web 小屋治理状态显示“自主链路”，不再使用旧治理模式标签。
- 测试新增防回退断言：Supervisor 与 Gateway 均只暴露 autonomous-chain-gate 路由。

### P2-11 跟进：删除 self_learning executor 健康广告

- `systems/execution/service.py`：health payload 删除学习任务 executor route 和相关兼容说明，executor 不再宣称自己拥有学习任务执行入口。
- `tests/test_execution_service.py`：新增反向断言，确认 health payload 不包含 `self_learning` executor route，也不包含兼容说明。
- 保留边界：`systems/self_learning/` 仍可作为历史学习结论 contract / store；它不承担自主任务执行，自主任务执行继续由 API-A 自主执行面通过 Gateway `/v1/tasks` pull 完成。

### P2-12 跟进：删除 Gateway self_learning 独立服务路由

- `systems/gateway/internal_gateway.py`：health `registered_services` 不再统计独立 `self_learning` 服务；自动路由表不再把该服务类型映射成学习服务代理路径。
- `systems/gateway/internal_gateway.py`：通用 `/api/...` 代理不再根据 `service_type` 把学习服务请求记录成 `self_learning` activity；学习活动只来自明确的 activity touch、自主任务 pull/writeback 和 Supervisor/Gateway 任务事件。
- `tests/test_gateway_activity.py`：新增防回退断言，即使有旧服务类型注册，Gateway 也不发布学习服务代理路由；访问旧代理路径返回 404，且不会产生学习 activity。

### P2-13 跟进：self_learning 导出面收口

- `systems/__init__.py`：删除 self_learning contract / store 的顶层导出，避免顶层系统命名空间成为新代码默认导入路径。
- `systems/self_learning/__init__.py`：只导出历史学习结论 contract 模型，不再导出结论 store。
- `systems/supervisor/planning_runtime.py` 与测试：需要模型时从 `systems.self_learning.models` 或 `systems.self_learning` contract 导入；需要兼容 store 时显式从 `systems.self_learning.conclusion_store` 导入。
- 这样结论 store 的使用点必须显式承认自己在访问历史结论记录层，而不是自主任务执行服务。

### P2-14 跟进：学习结论 store 破坏性改名

- 学习结论记录模块改名为 `systems/self_learning/conclusion_store.py`。
- 学习结论记录类改名为 `SelfLearningConclusionStore`，类注释删除“保留旧名兼容”的说法。
- 相关测试文件改名为 `tests/test_self_learning_conclusion_store.py`，测试函数和显式导入同步改为 conclusion store 语义。
- 保留边界：`systems/self_learning/__init__.py` 继续只导出 contract 模型，不导出 store 类。

### P2-15 跟进：systems 顶层导出收口

- `systems/__init__.py`：删除 body registry、execution facade/service、governor、lifecycle、probe 等运行对象的顶层重导出。
- 顶层包现在只保留薄说明和空 `__all__`，要求运行对象从所属模块显式导入。
- 扫描确认当前代码没有依赖顶层 systems 包获取运行对象；现有调用点均使用具体模块路径。

### P2-16 跟进：删除身体生命周期旧控制器别名

- `systems/lifecycle.py`：删除旧控制器别名，统一只保留 `BodyLifecycleExecutor`。
- `tests/test_lifecycle.py`、`tests/test_probe.py`：导入和局部变量统一改为 `BodyLifecycleExecutor` / `executor`。
- 语义边界：身体生命周期模块只负责执行 Governor 批准后的确定性状态变化，不承担控制器或决策器命名。

### P2-17 跟进：执行适配器状态与记忆压缩旧路径收口

- `systems/execution/adapters.py`：`WatchWindowExecutionAdapter` 删除旧 `runtime_state` 注入参数，watch-window task / outcome 状态只由适配器内部 `WatchWindowState` 持有。
- `systems/supervisor/runtime_assemblers.py`：Supervisor 装配 watch-window executor 时不再传入外部 runtime state，避免 API-B / 执行适配器之间继续形成旧共享状态协议。
- `systems/execution/adapters.py`：记忆维护结果键统一为 `rule_compression`，只调用 `/compressed/run-all-rules`，不再 fallback 到旧记忆压缩公开入口。
- `systems/memory/memory_service.py`：旧记忆压缩公开入口不再注册，保留内部方法仅用于既有健康信号逻辑复用。
- `tests/test_execution_adapters.py`：新增回归确认执行链路只请求 canonical memory rule compression endpoint，并且不会重新暴露旧平铺压缩结果键。

### P2-18 跟进：内生驱动 prompt 压缩层旧主语命名收口

- `systems/supervisor/endogenous_drive_prompts.py`：参考对齐条目压缩 helper 从历史主语命名收口为中性 `reference_alignment` 命名。
- `systems/supervisor/endogenous_drive_prompts.py`：认知评估与自迭代趋势压缩只读取 canonical 主字段，不再从历史聚合列表补主字段或补计数。
- `systems/supervisor/planning_runtime.py`、`systems/supervisor/endogenous_drive.py`：同步把 common 字段读取 helper 的局部命名收口为 secondary/common field 语义，去掉运行时主路径中的历史兼容主语。
- `systems/supervisor/planning_runtime.py`、`systems/supervisor/endogenous_drive.py`：破坏性删除历史聚合列表读取；proposal cognition、LM generation context、meta cognition profile、self-iteration hypotheses 证据都只接受 canonical 主字段。
- `tests/test_supervisor_self_evolution_queue.py`：删除专门验证历史聚合字段 fallback 的分支，相关用例改为主字段输入。

### P2-19 跟进：无 LM proposal 时的 canonical 认知闭环补强

- `systems/supervisor/planning_runtime.py`：非 planned 任务写回如果没有 LM cognitive assessment，会从 `drive_judgement` 派生 canonical `llm_cognitive_assessment`，让完成/失败/决策结果能进入下一轮 proposal cognition。
- `systems/supervisor/planning_runtime.py`：派生 assessment 只用于终态/执行写回，不用于单纯 planned 事件，避免刚计划完的静态任务立刻制造重复候选。
- `systems/supervisor/endogenous_drive.py`：当没有 LM proposals、没有具体活动 topic，但历史中已有 canonical cognitive assessment 时，生成 bounded self-learning cognition review 候选，保持“写回记忆 → 下一轮读取管理记忆”的自主链路闭环。
- `systems/supervisor/endogenous_drive.py`：用户活动 topic 提取支持 `recent_metadata.user_request.topic/title`，确保 active sessions 只作为软信号，不阻止学习候选进入队列。
- `tests/test_supervisor_self_evolution_queue.py`：整份长周期回归重新通过，覆盖多轮写回、历史乱序回放、active sessions 下学习候选规划和重复队列防抖。
