# 全链条报告：API-B 内生驱动 → API-A 代理执行 → Mem 记忆写入

> 审查范围：监督者(API-B)内生驱动产出 → 任务队列/治理/派发 → API-A 拉取并执行(含子代理) → 执行成果回写 → Mem 双层记忆写入。
> 方法：4 个子代理分段静态审查源码，逐项定位 `文件:行号` 并确认触发条件，未运行系统、未改动代码。
> 结论先行：**链条在三个关键接缝处断裂或会静默丢数据**——成果回写、状态所有权、记忆落地。下面先给最危险的跨段故障链，再分段列问题，最后给修复优先级。
>
> **2026-06 （全天候执行语义）**：监督者改为全天候自动执行，时间窗口(原 00:00-06:00)与"等用户空闲"硬门控已移除（详见《voidcube架构基线》§6）。受此影响的条目已就地标注 `[已随全天候改造修订]`：执行窗口跨夜问题(2-4.2)作废；时区 bug(2-4.1)不再影响 gating（用户代理已废、`last_user_request_at` 无活 feeder，该 gate 本就失效），idle 时间改为任务产出时间，不再保留弃用代码。AUTO 模式不再限制主 CLI 的用户输入与使用；在当前全天候改造下，监督者可以软感知用户状态，且 CLI 已采用双槽设计，不会再影响用户使用主 CLI 与主 Agent 交互。AUTO 模式现在只相当于监督者的一个手动开关，待完善后会完全移除对监督者的限制（无需使用 AUTO 命令来启停监督者）；但目前暂时保留 AUTO 模式作为监督者门控。

---

## 0. 执行摘要

整条链路能在"理想路径"(无并发、无抖动、LLM 健康、不重启)下走通。但一旦引入真实环境的并发、网络抖动、进程重启、LLM 降级，会暴露三类系统性缺陷：

1. **成果不落地**：API-A 执行学习/改造任务后，产出的 findings / 实现摘要**既不回写 supervisor、也不进记忆**——回写 payload 只有成败标志，记忆钩子又不覆盖主回合(段3 H4 + 段4 1.1)。自进化闭环的"成果"环节实际是断的。
2. **所有权/状态脱节**：任务的 owner 判定依赖 gateway 的"全局唯一 active session"，而它是 last-writer-wins，双 CLI 并存或 gateway 抖动时会误判孤儿、误恢复、回写撞上非法状态转换，导致**双执行 + 回写静默丢失**(段2 2.1/2.2/3.1 + 段3 H1/H2/H3)。
3. **假活跃 / 假成功**：`memory_active` 只看"压缩规则跑过"、派发结果"非已知错误即成功"——多个健康/成功信号反映的不是真实状态(段4 6.1 + 段2 6.2)。（注：原列的 idle 时区 bug 2-4.1 已随全天候改造降级，见上方修订说明）

按数量：高severity 14 项、中 20 项、低/观察 9 项。其中**段2(派发交接)与段4(记忆写入)是重灾区**。

---

## 1. 最危险的跨段故障链（复合风险）

这些是单段问题叠加后放大的真实事故场景，优先级最高。

### 故障链 A：双 CLI + gateway 抖动 → 任务双执行且成果丢失

```text
段2-2.2  gateway _active_cli_session_id = 非idle最后写入者（last-writer-wins）
   │     user_chat CLI 推 scene → 抢走 active，supervisor_task CLI 的 owner≠active
   ▼
段2-3.1  恢复逻辑发现 owner≠active（或 gateway 不可达时 active="") → 误判孤儿
   │     → 任务重置 approved → 被重新拉取
   ▼
段3-H2   原 CLI 其实还在执行/刚完成，回写 completed 撞上已被改写的状态
段2-1.4  approved→completed 非法转换抛 ValueError（500）→ 回写静默失败
   ▼
结果：同一任务两个 CLI 执行；先完成者的成果回写丢失；最终可能被记 failed
```

**触发**：常态——只要"用户在用主 CLI" + "AUTO 在跑监督者任务" + 任一次 gateway 5xx/超时。
**根因**：active-session 与 task-owner 耦合(段2-2.2)、恢复在不确定时采取激进策略而非保守跳过(段2-3.1)。

### 故障链 B：执行成功 → 成果蒸发

```text
段3-H4   AUTO 任务由主 agent 经 _pending_input 直接执行，不走 delegate_task
   │     → delegate_tool 的记忆钩子(on_delegation)不触发
   ▼
段3-H4   回写 supervisor 的 payload 只含 decision/reason/context，不含 final_response
   ▼
段4-1.1  gateway 的 _record_agent_interaction_to_tier1 只在 response_text 非空时写
   │     而该值恒为空 → agent 成果从不进 tier1
   ▼
结果：findings/实现摘要只留在 conversation_history，supervisor 拿不到、记忆里没有
```

**触发**：每一个正常完成的 learning / body_improvement 任务。
**根因**：成果回流路径压根没接上(段3-H4 与段4-1.1 是同一个洞的两端)。

### 故障链 C：看起来在学习，其实空转

```text
段4-1.1  执行成果未进 tier1（见故障链 B）
   ▼
段4-2.2  tier1 溢出路径仍按龄过滤 → 近期 turn 撑爆 max_turns 也不下沉
段4-6.1  memory_active 只看"压缩规则被调用过"，no-op 也刷新 last_run
   ▼
段4-3.1  LLM 健康只在启动检查一次，降级后不回升
   ▼
结果：UI 显示"记忆活跃 ✅"，实际成果没进来、压缩在空跑、LLM 可能早已降级
```

**触发**：LLM 启动时不可用 / 高频写入 / 长时间运行。
**根因**：多个健康信号反映"代码在跑"而非"数据真的写成功了"。

### 故障链 D：CLI 崩溃 → 任务永久悬挂

```text
段3-H3   崩溃重启后恢复只"认领"owner，不重新注入 prompt → 任务不再执行
段3-M1   30 分钟超时在真正长回合/卡死时不触发（poll 被 _agent_running 挡住）
段2-4.3  agent_pull 任务 metadata 写的是 execution_started_at，兜底查 executed_at → 兜底跳过
段2-3.2  恢复只比对 session 相等，不消费 gateway 已算出的 lease stale 标志
   ▼
结果：owner CLI 死了但 session 未过 1h TTL 时，任务卡死 running，无人清理
```

**触发**：CLI 静默退出/断网/卡死。
**根因**：三套兜底机制(30min 超时、孤儿恢复、lease stale)各有盲区且互不补位。

## 2. 分段问题清单

### 段 1 · API-B 内生驱动（drive 评估 → 候选 → 可执行任务）

文件：`systems/supervisor/planning_runtime.py`、`systems/supervisor/endogenous_drive.py`

| # | 严重度 | 问题 | 位置 | 触发条件 |
|---|---|---|---|---|
| 1-H1 | 高 | [2026-07 已收口] UI 只读评估改走 `persist_evaluation=False`，不再写 history/governance/cognition；持久评估仍显式保留 | `planning_runtime.py`、`ui_runtime.py` | UI 刷新无缓存候选时不再追加 judgement、自增 judged |
| 1-H2 | 高 | [2026-07 已收口] runtime drive cycle 改为先纯评估、再按姿态/预算 gate，最后只对实际保留并准备入队的候选落 judgement/governance/cognition；入队失败会回滚本次持久化 | `planning_runtime.py` | observation 主轴多候选 / budget 不足时，被 defer/trim 的候选不再增加 judged |
| 1-M1 | 中 | [2026-07 已收口] self-regulation boost 触发二轮候选重算时复用首轮 LM task proposals，并禁止二轮辅助 LM | `planning_runtime.py`、`endogenous_drive.py` | underdelivery/observation 等使 boost 非零的常见路径 |
| 1-M2 | 中 | [2026-07 已收口] observation gate 的允许集已和 engine 对齐，不再额外放行 `memory_maintenance` fallback | `planning_runtime.py`、`endogenous_drive.py` | engine 与 runtime 的 observation 判定分歧时 |
| 1-M3 | 中 | [2026-07 已收口] 静态 stable_key(memory_maintenance/queue_hygiene)已加入近期完成冷却，避免刚完成即重建同键任务 | `endogenous_drive.py` | 治理候选仍 eligible 且 urgency 达标 |
| 1-M4 | 中 | [2026-07 已收口] evaluate 持久化段在后续落盘失败时回滚 history/governance/cognition 快照，避免 judgement 半落盘 | `planning_runtime.py` | LM/队列/IO 在 annotate 之后异常 |
| 1-L1 | 低 | [2026-07 已收口] `_apply_scheduled_for_to_candidate_items` 改按 prepared 下标分配缺失 token，非 dict 项不会再导致后续候选错位或漏写 | `planning_runtime.py` | candidate_items 含非 dict 元素 |
| 1-L2 | 低 | [2026-07 已收口] 后端候选 queue item 不再人工叠加顶层 `stable_key/value_tags/utility` 副本，UI 改为优先读 metadata 主口径、旧顶层仅作 fallback | `endogenous_drive.py`、`planning_runtime.py`、`ui_runtime.py` | 一致性/可维护性 |
| 1-L3 | 低 | [2026-07 已收口] 候选 queue item 增加一等 `rationale`，并同步写入 metadata；来源仍复用既有 judgement/intent/need，不新增解释层 | `endogenous_drive.py`、`planning_runtime.py` | 下游依赖稳定 rationale 字段时 |
| 1-L4 | 低 | [2026-07 已收口] `_immediate_first_drive` 已提成 `_run_immediate_endogenous_drive_once` 并补 try/except，首跑异常只记录 warning，不再成为未处理 task exception | `service_runtime.py` | 首个 drive cycle 抛异常 |

与 `docs/内生驱动问题清单.md` 的对应：§3.7(纯评估不放大 history)已由 `persist_evaluation=False` 与 runtime gate 后持久化收口；§3.25(observation 下 memory_maintenance 不兜底)已由 engine/runtime 允许集统一收口；§4 问题1/2 的候选命名/理由口径已完成第一轮收口，剩余厚度继续归入“兼容层偏厚”长期项。

2026-07 收口状态：1-H1 的 UI 只读刷新路径已改为 `persist_evaluation=False`，不再写 judgement/governance/cognition；1-H2 的 runtime cycle 已改成 gate 后持久化，只统计实际保留并入队的候选，规划失败时回滚 history/governance/cognition；1-M1 的自调节二轮候选重算已复用首轮 LM task proposals，并禁止二轮辅助 LM；1-M2 的 runtime observation gate 已和 engine 对齐，不再放行 `memory_maintenance` fallback；1-M3 的 memory/queue hygiene 静态治理候选已加入近期完成冷却；1-M4 的持久化段已统一到 `_persist_endogenous_evaluation_for_candidates`，在后续落盘失败时回滚 history/governance/cognition 快照；1-L1/L2/L3 已完成小口径收口，避免兼容投影字段继续伪装成主结构。

2026-07 追加收口：`proposal_cognition.meta_cognition_profile` 不再外露任务形态辅助提示，`priority_signals` 也不再记录候选投影偏向；LM prompt 内只保留 `decision_core.secondary_task_shape_hint/score` 作为二级辅助提示，并且不再从旧 profile 输入回流。`proposal_cognition.summary` 也不再携带候选数量，只保留 posture / drift 辅助状态，候选数量降到 `current_candidates.count` 内读取。

2026-07 prompt / LM 状态削薄：`task_type_priors` 不再作为 prompt-facing 独立块进入 LM payload，也不再放入 prompt memory brief / 默认 attention priority；最新 LM generation context 也不再保留整块 priors，并移除顶层 `task_shape_hint` 与 `raw_candidate_kinds` 候选调试残留。任务形态只通过 prompt 的 `decision_core.secondary_task_shape_hint/score` 作为二级辅助提示表达。proposal drift 在 prompt 中也不再展开历史条目，只保留 drift state、质量分布、alignment signal/missing count 与 health；`recent_reference_alignment` 也只保留主缺口和计数，且源头已直接产 `entry_count / primary_missing_* / missing_*_count`，避免旧候选反馈明细继续伪装成主判断。prompt compact helper 对旧 `recent_entries` 只做 legacy fallback，不再与薄字段叠加 weak/partial 计数，避免兼容输入双倍放大 grounding pressure。

2026-07 继续削薄：runtime `proposal_cognition` 中的 `recent_reference_alignment`、`proposal_drift_memory`、`recent_cognitive_alignment`、`self_iteration_hypotheses`、`self_iteration_trend_memory`、`switch_*`、`post_task_effect_*`、`cognitive_assessment_memory` 已折入 `auxiliary_memory`，且辅助记忆前台只保留状态/方向/质量均值/计数，不再暴露 common_* 历史文本列表、hypotheses 明细列表或策略目标列表，保留纠偏信号但不再与 posture / assessment trace 并排扩张。默认 LM 任务生成主链不再构造 `cognitive_feedback_memory` / `evidence_attention` / `cognitive_strategy_delta` / `cognitive_evolution_draft`，也不再抓取深层 `memory_context`；旧 attention/evolution helper、辅助 learning/body LLM fallback、memory HTTP fallback、generic learning fallback 以及对应 env/config policy 入口已删除，不再只是“默认跳过”。LM prompt 内的 `cognitive_assessment_memory` / `self_iteration_trend_memory` 也同步改成 dominant 文本 + signal count + summary，`self_iteration_hypotheses` 改成 dominant/count/top evidence，不再展开 common_* 或 hypotheses 明细列表；最新 LM generation context 也同步降为 dominant/count 口径，并优先读取薄字段、旧 `common_*` 仅作 fallback，避免运行态快照继续保存历史明细或在削薄后丢失纠偏计数。`proposal_cognition` fallback 也已改为直接生成 dominant/count 的 self-iteration hypotheses，不再临时构造旧式 `hypotheses` 明细列表；薄字段 `why_not_improvement_now` 也会直接进入 self-iteration hypotheses，不再依赖旧 `common_why_not_improvement_now`。随后 `cognitive_assessment_memory` 与 `self_iteration_trend_memory` 的 engine/runtime 源头也改为直接产 dominant + unique signal count + entry_count，不再先产 `entries/common_*` 后再压薄；prompt compact helper 也已改为优先读取 `target_count/hypothesis_count/stay_or_switch_count/switch_reason_count`，旧 `common_*` 只作 legacy fallback，避免薄字段进 prompt 后计数归零；`self_iteration_hypotheses` 的 prompt compact 和最新 LM context 也改为薄 `hypothesis_count/suggested_task_types/top_priority` 优先，旧 `hypotheses` 明细只在薄字段缺失时补空，不再用旧明细长度放大纠偏权重；engine `post_task_effect_memory` 也对齐 runtime，跳过 planned-only outcome，直接产均值、方向、主 effect 与 entry_count。旧快照中的 `common_*` 单字符串 fallback 现在按完整文本读取，不再被拆成字符参与 dominant 或 count，避免兼容层制造假信号。

2026-07 候选反馈层继续削薄：runtime `proposal_cognition.auxiliary_memory.recent_reference_alignment`、`proposal_cognition.auxiliary_memory.proposal_drift_memory` 和 `proposal_cognition.auxiliary_memory.recent_cognitive_alignment` 不再外露 common_* 文本列表、entries 明细或 `dominant_top_priority_task_type` 旧口径；前台改为主缺口、`dominant_task_shape`、alignment signal count、missing count、entry count 等状态/计数字段。LM prompt 的 proposal drift 证据也同步为计数/健康口径，runtime 和 prompt 都不再把候选反馈历史展开成第二套主结构。随后源头也继续收窄：`proposal_drift_memory` 与 `recent_cognitive_alignment` 的 summary 构建不再先临时组装历史 entry 明细，而是直接用计数、分数累积和主信号统计生成薄字段。

2026-07 当前候选态收口：runtime `proposal_cognition.current_candidates` 不再外露候选条目明细，也不再暴露 `task_type_counts` 或 `quality_counts` 这类候选分布面板；前台只保留候选数量、LM 生成数量和平均 alignment 分数，避免当前候选列表重新成为前台解释中心。

2026-07 主议程 / 主治理层去候选化：runtime `attention_agenda.entries` 不再携带 `candidate_kind/candidate_count`，`governance` 顶层也不再重复输出 `candidate_count`；议程条目只表达 need/topic/objective/priority/urgency/confidence/persistence 等主认知信息。候选投影数量仍可从治理 channel count 或 `proposal_cognition` 聚合态读取，不再混入 agenda entry 或主治理层。

2026-07 meta-governance 收口：runtime `meta_governance` 不再输出单条重复 `entries` 列表，改为保留 `mode/confidence/stability/drivers/guardrails` 和一个轻量 `context` 指针，避免治理姿态被重复展开成第二份状态包。

2026-07 UI 可观察性修正：监督者 UI 读取内生 cognition snapshot 时已改为先解开 `{state: ...}` 包装层，并从薄口径 `proposal_cognition.lm_trace` 读取 LM generation 状态/模型角色/提案计数；不再依赖旧的 `proposal_cognition.lm_reasoning_state` 大包，也修正 LM 生成开关读取到 `service_runtime` 的实际配置位置。

2026-07 outcome 写回幂等收口：`_record_endogenous_drive_outcome` 的重复检测从最近 24 条扩展到完整保留窗口，避免长链运行中同一 task/status/event 排到 24 条之后被重复记录，从而重复放大 `focus_stats/contextual_focus_stats` 的 completed/failed/dragging 计数。

2026-07 治理事件幂等收口：`_trim_endogenous_governance_events` 现在按 `event_id` 去重，避免同一治理事件重复落盘后在 self-evolution cycle 中被重复消费，从而双倍推高 `dynamic_candidate_throttle_boost`、`dynamic_observation_bias_boost` 或 truthfulness 纠偏强度；无 `event_id` 的事件仍按原样保留，避免误合并。随后继续补上生成端的未消费同义事件合并：同一 `event_type/channel/context/preferred_focus/message/rationale/payload` 的未消费治理事件不会因为重复评估换新 UUID 后继续增殖；已消费事件不参与语义去重，保留审计并允许后续真实复发。

2026-07 长链恢复补证：新增“近期连续 self-learning completed 序列释放旧脏历史 observation carryover”的回归，覆盖旧 `paused / retry / awaiting_review / deferred / failed` 历史与陈旧 strategy memory 并存时，不再继续判为 `historical_underdelivery`；同时新增“恢复后再次 relapse + 陈旧 strategy memory + 衰减 self-regulation”的回归，确保最近 `retry / deferred` 拖拽能重新进入 observation 并收紧 candidate budget；再补“连续 strategy writeback 后恢复/relapse 双向切换”的回归，证明同一批写回记忆不会把系统锁在单一姿态；继续补“治理事件消费 + drive 入队失败回滚 + context switch 重入队”的回归，确认已消费治理事件不会被撤销，失败候选也不会留下 judgement / strategy 污染。追加多轮交织回归：连续两批治理事件消费、两次 drive plan 失败回滚、两次不同 context 的成功重入队后，旧消费时间不倒退、失败候选不进入 judgement/strategy、truthfulness 与 queue_hygiene 只各记录一次成功 judgement。再补真实 plan/writeback 后的打乱顺序回放回归，确认混合 outcome 历史会按 `recorded_at` 恢复时间窗口，reflection / adaptive policy / primary need 不被列表顺序污染。最新再补同一批连续 writeback strategy memory 下的长脏 history 双态回放：planned-only outcome 不作为恢复/复发事实放大，近期 `retry/deferred` 会重新收紧 observation，近期 completed 链即使乱序回放也能释放 observation / candidate budget。

### 段 2 · 任务队列 → 治理 → 派发 → API-A 拉取（交接段，重灾区）

文件：`systems/supervisor/planning_runtime.py`、`task_queue.py`、`service_runtime.py`、`systems/gateway/internal_gateway.py`、`cli.py`

> **AUTO 临时门控说明**：最新基线倾向最终移除对 AUTO 命令的依赖，让监督者按全天候语义自运行；当前阶段暂时保留 `/auto` / `/auto-q` 作为监督者链路启停门控，但它不限制主 CLI 输入，也不影响用户与主 Agent 交互。

| # | 严重度 | 问题 | 位置 | 触发条件 |
|---|---|---|---|---|
| 2-1.1 | 高 | [2026-07 已收口] `_run_self_evolution_cycle` 已加进程内 asyncio 锁；review loop / `/auto` / 首跑并发命中时，后进入者返回 `cycle_already_running`，不再重复 review/dispatch 同一批任务 | `planning_runtime.py` | governor review 周期 + `/auto` 手动并发 |
| 2-1.4 | 高 | [2026-07 已收口] agent-pull 任务在 owner 匹配时允许 late completed/failed 回写先从 approved 恢复 running，再进入终态；不再因 approved→completed 竞争抛 ValueError | `planning_runtime.py` | 恢复/重试与 CLI 回写时序交错 |
| 2-2.1 | 高 | [2026-07 已收口] 恢复判定不再依赖"owner 是否等于全局 active"，而是查询 owner session 自身 lease；running 写 owner 与 scene 抢 active 的顺序窗口不再导致刚拉任务被误恢复 | `planning_runtime.py`；`internal_gateway.py` | CLI 拉取瞬间与 review/recovery 重叠 |
| 2-2.2 | 高 | [2026-07 已收口] 双 CLI 下 user 端 scene 抢走 `_active_cli_session_id` 不再影响 supervisor_task owner 判定；恢复只看 owner session missing/stale | `planning_runtime.py`；`internal_gateway.py` | 用户对话 CLI + AUTO 执行 CLI 同时活动 |
| 2-3.1 | 高 | [2026-07 已收口] 恢复查询 gateway 失败时保守 no-op；当前恢复主路径改为 owner session 查询，失败不会把 active 当空导致全量误恢复 | `planning_runtime.py` | gateway 短暂 5xx/超时/重启恰逢 cycle |
| 2-4.1 | 中（降级） | [2026-07 已收口] idle now 与 gateway naive 时间戳统一按 UTC 比较；aware `now` override 也会归一到 naive UTC。全天候改造后它不再是 gating 致命问题，但展示/派生判定的 8 小时偏移已消除 | `planning_runtime.py`；`internal_gateway.py` | 监督者主机非 UTC(本项目 UTC+8) |
| 2-4.3 | 高 | [2026-07 已收口] stale running 兜底同时识别 `executed_at` 与 agent_pull 实际写入的 `execution_started_at`；owner 仍 active 但任务超过 30 分钟时会被置 failed，不再永久 running | `planning_runtime.py`、`cli.py` | owner CLI 崩溃/卡死且 session 仍存活 |
| 2-5.1 | 高 | [2026-07 已收口] supervisor 恢复不依赖 gateway 主动通知；owner session 查询返回 missing 时会恢复 running agent-pull 任务到 approved，下一轮可重新认领 | `planning_runtime.py`；`internal_gateway.py` | CLI 静默退出/断网 |
| 2-6.1 | 高 | [2026-07 已收口] `_start_governor_mode` 在本地已 active 时会重推 gateway governor-mode=true，不再因幂等早返回导致 gateway 重启后长期丢失 governor 状态 | `internal_gateway.py`；`service_runtime.py` | governor 期间 gateway 重启 / 通知失败 |
| 2-1.2 | 中 | [2026-07 已收口] agent-pull `running→running` no-op 进入 supervisor 前会校验 owner session；第二个 CLI 以不同 `session_id` 刷 running 会 409，且不会覆盖原 `owner_session_id` | `planning_runtime.py`；`internal_gateway.py` | 两个 AUTO CLI 各拉到同一 approved |
| 2-1.3 | 中 | [2026-07 已收口] `awaiting_review`/`retry` 已进入显式合法转换表；未知状态/目标会直接拒绝，completed/failed/cancelled 仍保持终态，不再被"缺表=放行"误开 | `task_queue.py` | 任何路径置这两个状态后再转换 |
| 2-2.3 | 中 | [2026-07 已收口至 agent-pull 主路径] gateway 转发前读取任务快照并校验 CLI session；supervisor decision 侧也校验 owner，running 会补齐 owner metadata，completed/failed 非 owner 直接 409 | `internal_gateway.py`；`planning_runtime.py` | 误报或并发 session 串号 |
| 2-3.2 | 中 | [2026-07 已收口] gateway `/v1/sessions/{session_id}` 暴露 owner lease，恢复逻辑识别 `is_stale` / `lease_status=stale`，owner stale 时恢复 approved | `planning_runtime.py`；`internal_gateway.py` | owner CLI 卡死但 session 仍 active |
| 2-3.3 | 中 | [2026-07 已收口] owner_session_id 缺失的 running agent-pull 任务不再按 owner≠active 自动恢复；记录 `owner_session_missing_seen_at` 后保持 running，交给 stale running 兜底处理 | `planning_runtime.py` | metadata 写入失败 / 自派发 running 命中 agent_pull |
| 2-4.2 | ~~中~~ 作废 | `[已随全天候改造修订]` ~~执行窗口不支持跨夜~~ —— 执行窗口机制已彻底移除(全天候执行)，此条不再适用 | ~~`planning_runtime.py:4848`~~ | —— |
| 2-6.2 | 中 | [2026-07 已收口] 派发结果改为只认明确成功状态才 completed；`status=None`、未知状态和明确错误状态都会进入失败重试/失败路径，不再假完成 | `planning_runtime.py` | executor/facade 返回非标准 status |
| 2-6.3 | 中 | [2026-07 已收口] orphan recovery 查询 gateway 失败时保守 no-op 并 warning；CLI 拉取/写回路径保留 supervisor 明确 HTTPException 状态，只有真实转发/网络异常包 502，不再把 supervisor 503 误改成 502 | `planning_runtime.py`；`internal_gateway.py` | gateway 故障 |
| 2-7.1 | 中 | [2026-07 已收口] CLI 在 current AUTO task 未完成/超时时直接返回，不再继续拉取新任务；supervisor 增加 `self_evolution_dispatch_limit_per_cycle`，默认每轮只 dispatch 1 个非 agent_pull formal execution request，并返回预算耗尽计数 | `planning_runtime.py`；`config_models.py`；`cli.py` | 队列积压（原"窗口开启瞬间"触发已随全天候改造作废，但常态全天候下积压仍可触发） |
| 2-7.2 | 中 | [2026-07 已收口] body_improvement 的 self_learning 前置改成公平门控：running self_learning 仍硬挡；planned/approved self_learning 只触发一次前置 defer，后续 review 若仍未 running 会释放 body_improvement，避免长期饥饿 | `planning_runtime.py` | self_learning 因恢复反复滞留 |
| 2-5.2 | 中 | [2026-07 已收口] 恢复逻辑识别 owner session `is_stale` / `lease_status=stale`，不再因全局 active 滞留而跳过恢复 | `planning_runtime.py`；`internal_gateway.py` | CLI 死亡后 1 小时内 |
| 2-4.4 | 低 | [2026-07 已收口] idle/window、active CLI lease stale、session TTL 默认值集中到运行时阈值入口；Gateway 可配置 `active_cli_stale_after_seconds` / `session_ttl_seconds`，Supervisor idle window 可配置 user/memory/workflow 阈值，并在 idle 评估中透出 active CLI stale 阈值，避免 90s/600s/3600s 被误读成同一语义 | `systems/runtime_thresholds.py`；`internal_gateway.py`；`config_models.py`；`planning_runtime.py` | 边界时刻 |
| 2-7.3 | 低 | [2026-07 已收口] schedule_token 防重前移到候选准备阶段：活跃队列已占用的 token 会进入 `requested_scheduled_for` 并重新分配下一空槽；review 仍保留冲突 defer 作为显式/人工重复指定的兜底 | `planning_runtime.py` | 并发/相邻 cycle |

### 段 3 · API-A 代理执行（拉取 → auto/pull 执行 → 子代理 → 回报）

文件：`cli.py`、`tools/delegate_tool.py`、`agent/run_agent.py`、`agent/subagent_display.py`

| # | 严重度 | 问题 | 位置 | 触发条件 |
|---|---|---|---|---|
| 3-H1 | 高 | [2026-07 已收口] `/auto-q` 普通/fast-path 退出会先把当前 AUTO 任务按 interrupted failed 回写；回写成功才清 `_current_auto_task`，force-quit 复用同一清理口径 | `cli.py` | AUTO 执行中用户输入 `/auto-q` |
| 3-H2 | 高 | [2026-07 已收口] completed/failed/timeout/enqueue-failed 回写统一走 `_post_auto_task_decision`；回写失败会记录事件并保留 `_current_auto_task` 与 `_last_agent_turn_result`，等待后续重试，不再静默清空导致成果丢弃 | `cli.py` | 完成时刻 gateway 短暂不可达 |
| 3-H3 | 高 | [2026-07 已收口] 恢复 owned running 任务后会重建并重新注入 AUTO prompt，成功重放后本轮直接返回，避免同轮立刻按旧 started_at 超时；重放失败则主动回写 failed | `cli.py` | 拉取标记 running 后崩溃/重启 |
| 3-H4 | 高 | [2026-07 已收口] AUTO completed 回写携带 `final_response` + `session_id`，gateway 成功转发后写入 Mem Tier1；body_improvement 另提交 improvement report 供监督者评分 | `cli.py`；`internal_gateway.py` | 每个 AUTO 任务正常完成路径 |
| 3-M1 | 中 | [2026-07 已收口] `chat()` 的 agent thread 监控循环会复用 AUTO timeout helper；长回合超过 30 分钟会立即回写 failed 并尝试 interrupt，不再等 poll 空闲后才判定 | `cli.py` | 模型回合自身运行超过 30 分钟 |
| 3-M2 | 中 | [2026-07 已收口] AUTO prompt 注入时绑定本地 `auto_task_run_id` 与 prompt 文本；`chat()` 只把匹配 run_id 的结果写入当前 AUTO 任务，后台/其它 chat 不再覆盖 `_last_agent_turn_result` | `cli.py` | AUTO 任务期间有后台消息注入 |
| 3-M3 | 中 | [2026-07 已收口] 批量子代理构建失败会将已建 child 从 `_active_children` 移除、标记展示 error 并 close，避免残留到下次中断或进程退出 | `delegate_tool.py` | 批量 tasks 中某 child 构建失败 |
| 3-M4 | 中 | [2026-07 已收口] AUTO 完成/超时/force-quit 回写失败会记录事件并保留本地任务状态；Tier1 记忆写入失败会 warning 并进入 gateway `memory_write_failure_count` / recent metadata；scene push 仍为非关键 best-effort，不再承载任务正确性 | `cli.py`；`internal_gateway.py`；`delegate_tool.py` | gateway/supervisor/记忆后端抖动 |
| 3-L1 | 低 | [2026-07 已收口] AUTO 激活时预登记 `cli_auto_executor` session；auto-resume 会优先拾取未关闭的 AUTO executor session，即使 message_count=0，避免 supervisor-pull 任务没有用户消息行时重启后换 session | `cli.py` | 崩溃重启 |
| 3-L2 | 低 | [2026-07 已收口] `delegate_task` 暴露并校验已存在的 `worktree_path`，child prompt / `terminal_cwd` / `cwd` / subdirectory hints 均绑定到该路径；delegate 不再假装负责创建/清理 worktree，生命周期仍由 body slot 管理 | `delegate_tool.py` | 并发/多槽改造时 |
| 3-L3 | 低 | [2026-07 已收口] CLI agent role 改为由 `_auto_mode_active or _current_auto_task` 判定；AUTO 完成/超时/恢复失败/退出/force-quit 的 idle scene 均显式标注 `agent_role=supervisor_task`，快速退出收尾期不再翻成 user_chat lane | `cli.py` | 3-H1 触发后 |
| 3-L4 | 低 | [2026-07 已收口] delegate display manager 改为按 invocation 存入 `_subagent_display_managers` map，旧 `_subagent_display_manager` 仅作为兼容视图；CLI `/tasks` 与 gateway observability 聚合全部 active managers，构建失败和完成路径都会清理 map entry | `delegate_tool.py`；`cli.py` | 同 agent 并发 delegate |

### 段 4 · Mem 记忆系统写入（成果/对话 → tier1 → tier2 压缩/长期，重灾区）

文件：`systems/gateway/internal_gateway.py`、`systems/memory/memory_service.py`、`systems/memory/tier1_to_tier2_bridge.py`、`Mem/src/memai/{extraction,schema,model_config}.py`、`plugins/memory/mem/governor_bridge.py`

| # | 严重度 | 问题 | 位置 | 触发条件 |
|---|---|---|---|---|
| 4-1.1 | 高 | [2026-07 已收口至 AUTO 成果主路径] self_learning completed 回写中的 `final_response` 会由 gateway 写入 Tier1；不复活已废弃 agent/query 代理，直接接通 AUTO finding → Mem | `cli.py`；`internal_gateway.py` | AUTO self_learning 正常完成路径 |
| 4-1.2 | 高 | [2026-07 已收口] tier1 写入失败不再静默吞异常，会 warning 记录 session/speaker/异常，并刷新 gateway `last_memory_write_failure_at` / `memory_write_failure_count` / `recent_metadata.memory_write_failure`；仍保持 best-effort 不阻断用户链路 | `internal_gateway.py` | Mem 服务重启/慢/抖动 |
| 4-2.1 | 高 | [2026-07 DB 写入阶段已收口] pipeline 计算仍在事务外，但 archive、`compressed_memories` 写入和 `compressed_to_tier2` 标记已同事务提交/回滚；失败重试依靠稳定 compressed memory ID 幂等覆盖，不再生成随机重复 Event | `memory_service.py`；`tier1_to_tier2_bridge.py` | 压缩中途崩溃 / archive 阶段异常 |
| 4-2.2 | 高 | [2026-07 已收口] max_turns 溢出路径显式传 `force_oldest=True`，`tier2_compress` 和 standalone bridge 会按最旧未压缩 turn 取批次，不再被 retention 窗口挡成 no_candidates | `memory_service.py`；`tier1_to_tier2_bridge.py` | 高频写入，30 天内 turn 数超 tier1_max_turns(默认 10000) |
| 4-3.1 | 高 | [2026-07 已收口] LLM 健康由启动一次改为压缩循环周期复检，`rules_status` 暴露最近检查时间；provider 恢复后不再必须重启 | `memory_service.py` | 启动早于 key 配置 / 启动时 provider 抖动 |
| 4-3.2 | 高 | [2026-07 已收口] bridge 结果没有 Event 时不归档、不写 compressed_memories、不标 `compressed_to_tier2=1`；API 与 standalone bridge 都返回/记录 no-events 路径，turn 留在 Tier1 等下轮压缩 | `memory_service.py`；`tier1_to_tier2_bridge.py`；`Mem/src/memai/extraction.py` | LLM 降级 + turn 文本不匹配关键字 |
| 4-5.1 | 高 | [2026-07 SQLite 投影层已收口] Mem pipeline 内部 ID 仍为随机 uuid4，但写入 `compressed_memories` 和 archive back-reference 时统一映射为基于 memory_type / 内容 / source/evidence/child refs 的稳定 ID；同一语义压缩重放会覆盖同一行 | `memory_service.py`；`tier1_to_tier2_bridge.py`；`Mem/src/memai/schema.py:18,203` | flag 更新失败 / 并发压缩 / 崩溃重试 |
| 4-6.1 | 高 | [2026-07 已收口] `memory_active` 改看最近有效写入活动，规则 no-op 只推进 last_run 不刷新 effective activity；UI 不再把空跑显示成记忆活跃 | `memory_service.py`；`ui_runtime.py` | 压缩循环在跑但无实际写入 |
| 4-2.3 | 中 | [2026-07 已收口] 过期 turn 的 retention 优先级高于 relevance gate；若按 `min_relevance` 查不到候选，API 与 standalone bridge 都会回退选择最旧过期 turn，并在 API 结果中标记 `low_relevance_fallback` | `memory_service.py`；`tier1_to_tier2_bridge.py` | turn 长期未被压缩 |
| 4-2.4 | 中 | [2026-07 已收口] 后台压缩循环、单规则异常、tier2 bridge cycle 异常都会 warning 并保留结果错误结构，不再只 debug/静默 | `memory_service.py` | pipeline/LLM/DB 持续异常 |
| 4-3.3 | 中 | [2026-07 已收口] `_call_llm_for_summary` 返回结构化结果；无 LLM / 空摘要 fallback 会标记 `summary_degraded=true`、`summary_method=truncation_fallback`，并写入 memory metadata | `memory_service.py` | `memories/compress` / summarize 在 LLM 不可用时被调 |
| 4-1.3 | 中 | [2026-07 已收口] Mem `add_turn` 会在同一 SQLite 事务中自动创建缺失 session；gateway Tier1 记录改为单次 POST `/sessions/{session_id}/turns`，不再先建 session 再写 turn | `internal_gateway.py`；`memory_service.py` | 第二次请求超时/失败 |
| 4-4.1 | 中 | [2026-07 已收口] 压缩写库实现收敛为 `tier1_to_tier2_bridge._write_compressed_memories_to_db` 单一函数，MemoryService 直接复用；scene `event_kind` 同步按 child event 派生，避免 API/standalone 形态漂移 | `memory_service.py`；`tier1_to_tier2_bridge.py` | 分别经 API 与 standalone bridge |
| 4-4.2 | 中 | [2026-07 已收口] archive 与 compressed_memories 使用同一套稳定 compressed memory ID；DB 写入阶段同事务提交/回滚，重复压缩不再因随机 ID 新增或部分写库造成形态分裂 | `memory_service.py`；`tier1_to_tier2_bridge.py` | 重复压缩 / 部分失败 |
| 4-4.3 | 中 | [2026-07 已收口] lifecycle escalation 父记忆 ID 改为基于 child/level 的稳定 UUID；`parent_id` 指回被升级的子记忆，`source_turns` 保留原始 turn 列表，旧子记忆通过 `superseded_by` 指向父条目 | `memory_service.py` | 记忆进入 Scene/Arc/Epoch 升级 |
| 4-5.2 | 中 | [2026-07 已收口] Tier1 `turns` 增加 `dedup_key` 与 `(session_id,dedup_key)` 唯一索引；写入优先使用显式 `turn_dedup_key`/`idempotency_key`，带 `task_id`/`trace_id` 等重试标识时自动派生 key，重复写返回原 turn | `memory_service.py` | 写入重试/重复触发 |
| 4-6.2 | 中 | [2026-07 已收口] gateway memory activity 改按方法/path 区分读写；GET/HEAD/OPTIONS 与 search/query/timeline/stats/status/health/usage 类 POST 不刷新 `memory_task`，写入/压缩/规则执行仍打点 | `internal_gateway.py`；`planning_runtime.py` | endogenous_drive 频繁读 compressed/search |
| 4-7.1 | 中 | [2026-07 已收口] governance repository 镜像仍保持 best-effort、JSONL 仍是权威，但 append 失败会 warning 记录 record_id/异常栈，不再静默造成查询层空白难排查 | `governor_bridge.py` | repository 写入异常 |
| 4-7.2 | 中 | [2026-07 已收口] MemoryService 与 standalone bridge 的 SQLite 连接统一走 `open_memory_sqlite`，默认设置 `timeout=30s`、`PRAGMA busy_timeout=30000`、`journal_mode=WAL`；并发写不再使用零等待默认连接 | `memory_service.py`；`tier1_to_tier2_bridge.py` | 后台压缩与 API 写/手动 bridge 同时 |
| 4-3.4 | 低 | [2026-07 已收口] embedding 不可用时不再用 hash 向量伪装 semantic；`semantic_search` 退回 keyword search，返回 `method=keyword_fallback`、`semantic_degraded=true`、`ignored_min_similarity` | `memory_service.py` | LLM embedding 不可用 |
| 4-7.3 | 低 | [2026-07 已收口] supervisor UI Tier1 stats 在 gateway services 不可用或 memory service 未注册时返回 `memory_unavailable=true`、reason 与 `memory_active=false`，不再用空对象伪装"无历史记忆" | `ui_runtime.py` | Mem 未注册/不可达 |

## 3. 修复优先级建议

按"影响面 × 修复成本"排序。前 4 项是杠杆最高的——小改动、堵大洞。

### P0（先修，多为小改动堵大洞）

1. **2-4.1 时区统一**（已修复）：supervisor idle-window 侧将默认 `now` 改为 UTC，并把 aware override 归一到 naive UTC，和 gateway naive UTC 活动时间戳保持同一时钟域。
2. **段3-H4 + 段4-1.1 成果回流断裂**（接通一条链）：回写 payload 带上 `final_response`(截断)，并在 AUTO 完成分支显式写记忆。这是自进化闭环"成果"环节缺失的根因，不修则整套学习/改造产出蒸发。
3. **2-3.1 恢复在不确定时保守化**：`_fetch_gateway_active_cli_executor` 失败时**跳过恢复**，而非把 active 当空。一行判断之差，杜绝"gateway 抖动→全量误恢复→双执行"。
4. **4-6.1 + 4-3.1 健康信号说真话**：`memory_active` 基于"最近周期实际写入条数>0 且 llm_healthy"；LLM 健康改周期复检。否则所有人(含 UI、内生驱动)被"假活跃"误导。

### P1（所有权与状态机解耦）

5. **2-2.1 / 2-2.2 active-session 与 task-owner 解耦**：恢复判定基于 owner session 自身的 lease 存活，不依赖"全局唯一 active session"。
6. **2-1.4 回写冲突口径**（已修复）：agent-pull owner 匹配的 late completed/failed 回写会先恢复 running 再落终态；owner 不匹配返回 409，不再抛 500。
7. **段3-H1 / H2 / H3 回写可靠性**（已修复）：快速退出复用 force-quit 的回写+清理；回写成功后才清本地状态、失败保留本地状态待重试；恢复时重放 prompt 或主动回写 failed。
8. **2-6.1 governor 重通知**：health-check 重注册时一并重推 governor 模式。

### P2（记忆写入的事务性与幂等）

9. **4-5.1 确定性 ID**（SQLite 投影层已修复）：写入 `compressed_memories` 与 archive back-reference 时用稳定 ID，使重复压缩成为幂等 upsert（同时收口 4-4.2 的重复写问题）。
10. **4-2.1 压缩 DB 写入单事务**、**4-2.2 溢出路径放宽按龄过滤**已修复：pipeline 计算在事务外保持不变，archive/压缩投影/turn 标记在一个 SQLite 事务内提交或回滚；超限时按最旧未压缩批次处理。
11. **4-3.2 降级模式不丢数据**（已修复）：产不出 Event 的 turn 不标记已压缩，保留在 Tier1。
12. **4-1.2 / 4-2.4 / 段3-M4 停止静默吞异常**（已修复）：关键写入失败至少 warn；AUTO 回写失败不清空任务状态；Tier1 写入失败有 gateway 计数和 recent metadata。

### P3（一致性与可维护性，非正确性）

13. 段1 的 judged/outcome 作用域对齐(1-H2)、只读评估副作用收敛(1-H1)、observation 允许集两层统一(1-M2)。
14. 段4 压缩写库、SQLite WAL+busy_timeout、gateway 读写活动区分已收口；剩余 P3 主要是层级回溯与写入去重等一致性细节。
15. 时间常数对齐(2-4.4)已收口：运行时阈值入口集中，Gateway lease/TTL 与 Supervisor idle window 语义分开暴露。（原"命名收敛 1-L2/L3"已完成第一轮收口；原"执行窗口支持跨夜 2-4.2"已随全天候改造作废）

## 4. 一句话结论

链路的"控制流"基本成立，但"数据流"在三处接缝漏：**成果回不来、所有权对不齐、健康信号不可信**。其中性价比最高的是 P0 的几项——一条成果回流链 + 一处保守恢复 + 两个健康信号修正（+ 已降级的时区统一），能消掉本报告里大半高危故障链。建议先做 P0 并各自补回归测试，再按 P1→P2 推进；P3 与既有的内生驱动收敛工作合并节奏。

## 附：方法与边界

- 本报告由 4 个子代理分段静态审查源码生成，所有条目均带 `文件:行号` 且经 read/grep 确认，但**未运行系统验证**。标注的"触发条件"是基于代码路径的推断，实际触发概率需运行时观测确认。
- 严重度是审查视角的相对排序(高=可致数据丢失/双执行/闭环断裂，中=特定条件下错误行为，低=潜伏/一致性)，非正式风险评级。
- 行号基于审查时的工作区快照，后续改动可能漂移。
- **全天候执行语义对齐（2026-06）**：本报告已按"监督者全天候自动执行、用户状态走 `active_sessions` 软感知、时间窗口移除"修订相关条目（见顶部修订说明与 §2-4.1/2-4.2/2-7.1/2-7.2 标注）。本报告审查范围是"内生驱动→代理执行→记忆写入"主链，不含身体切换路径；身体切换的"仅 activate 需用户同意"目标语义记录在《voidcube架构基线》§7.5，属于另一条待实现项，不在本报告问题清单内。

## 5. 修复进度（2026-06）

按 P0 优先级推进。每项均补回归测试并跑绿；行号以修复时工作区为准。

### ✅ P0-2 成果回流闭环（段3-H4 + 段4-1.1）— 已修复

`self_learning` 与 `body_improvement` 两条成果路径都从 CLI 接通，全部 best-effort（成果回流失败不阻断任务回写）：

- **self_learning → Mem Tier1**：`cli.py` 保留 agent `final_response`（原在 `_run_agent_turn` 算出后即丢弃），completed 回写 payload 带 `final_response`(截断 4000)+`session_id`；网关 `_forward_task_decision` 转发成功且 `decision==completed` 时调活体 `_record_turn_to_tier1` 写入。复用已有 helper，不复活已 410 的 `_record_agent_interaction_to_tier1`。
- **body_improvement → 监督者评分**：`cli.py` 新增 `_git_head_commit`/`_git_improvement_diff`，body_improvement 派发前捕获基线 HEAD、完成后比对 commit/diff 组装 `BodyImprovementReport` 提交（CLI 此前从不提交该报告 → slot health_score 永不更新）；网关新增 forward 端点 `/v1/body/improvement-report` 转发到监督者。未检测到提交则跳过报告、仍正常回写 decision。
- **改动文件**：`cli.py`、`systems/gateway/internal_gateway.py`
- **测试**：`tests/test_gateway_activity.py`（completed 写 tier1 / failed 不写 / 报告转发 / 无监督者 503，4 例）、`tests/test_cli_improvement_diff.py`（真实临时 git repo 验证 commit/diff 提取，5 例）

### ✅ P0-3 恢复保守化（段2-3.1）— 已修复

`_recover_orphaned_agent_pull_tasks` 区分"网关说没有 active 执行器"与"网关不可达"：拉取**失败时直接 no-op 返回 0**（原先吞异常置 `active_session_id=""`，导致每个 running 任务 `owner==active` 恒 False → 全量误恢复 → 双执行）。成功但 active 为空的情形不在本次收口范围（见 P1 2-2.x 所有权解耦）。

- **改动文件**：`systems/supervisor/planning_runtime.py`
- **测试**：`tests/test_supervisor_self_evolution_queue.py::test_recovery_skipped_when_gateway_active_executor_fetch_fails`（拉取抛错 → 0 恢复、任务保持 running）；既有成功路径恢复测试仍绿

### ✅ P0-4 健康信号说真话（段4-6.1 + 段4-3.1）— 已修复

- **4-6.1 假活跃**：新增 `_last_effective_activity_at`，仅当某轮规则**实际写改行数 >0**（`_rule_effective_count` 跨四种返回结构汇总）才打点；`last_run` 仍逐轮推进但不再等同"活跃"。UI `memory_active` 改用 `effective_activity_at`（2 周期窗口），降级/空跑/链路断裂不再显示"记忆活跃 ✅"。`_tier1_decay_cycle`/`_tier2_bridge_cycle` 改为返回实际处理行数。
- **4-3.1 LLM 假健康**：`_check_llm_health` 由"仅启动一次"改为**压缩循环每周期复检**，瞬时抖动/迟配 key 后可回升；新增 `_last_llm_health_check_at` 暴露最近探测时刻。
- **改动文件**：`systems/memory/memory_service.py`、`systems/supervisor/ui_runtime.py`
- **测试**：`tests/test_memory_health_signals.py`（`_rule_effective_count` 多形态 / 空库 no-op 不打点 / 有 turn 衰减写行则打点 / rules_status 暴露新字段，4 例）

### 进度小结

- **2026-07 补充收口**：P0-1 idle 时区统一已修复；1-L4 immediate first drive 异常保护已修复；2-6.1 governor-mode 已 active 时重通知 gateway 已修复；2-1.1 cycle 加锁、2-4.3 agent_pull stale 时间字段、2-6.2 非标准执行状态不假完成已修复。均补轻量回归，避免全链条跑通前被 idle 秒数偏移、首轮后台异常、gateway 重启后 governor 状态丢失、并发 cycle、agent_pull 永久 running 或执行器假完成卡住。
- **2026-07 本轮全链条补洞**：2-1.4 late writeback 已收口；2-1.2/2-2.1/2-2.2/2-3.2/2-5.x 恢复逻辑改为 owner session lease/missing 判定并阻止非 owner running no-op 抢占；2-1.3 状态机补齐 `awaiting_review`/`retry` 显式转换；2-2.3 agent-pull running/completed/failed 所有权校验已在 gateway 与 supervisor 双侧落地；2-3.3 无 owner running 不再反复恢复；2-6.3 gateway 故障状态口径、2-7.1 CLI 单执行槽与 supervisor dispatch budget、2-7.2 body_improvement 公平释放已收口；3-H1/H2/H3 CLI 回写可靠性与恢复重放已落地；3-M1/M2 长回合 timeout 与 turn-result run_id 归因已收口；3-M3/3-M4 关键失败可见性已补；4-1.2/4-1.3/4-2.4 写入可见性与 Tier1 单调用记录已补；4-2.1 DB 写入事务、4-2.2 max_turns 溢出候选路径、4-2.3 过期低 relevance 候选回退已收口；4-3.2 no-event 降级不丢 turn、4-3.3 摘要降级显式标记已收口；4-4.1 压缩写库单实现、4-4.3 层级升级回溯链、4-5.1/4-5.2/4-4.2 的重复写问题已在 SQLite 投影层用稳定 ID 与 Tier1 dedup_key 收口；4-6.2 gateway memory 读写活动拆分、4-7.1 governance 镜像失败 warning、4-7.2 Memory SQLite 连接 busy_timeout/WAL 已收口。
- **已修复/收口**：P0-1、P0-2、P0-3、P0-4、1-L4、2-1.1、2-1.2、2-1.3、2-1.4、2-2.1、2-2.2、2-2.3(agent-pull 主路径)、2-3.2、2-3.3、2-4.3、2-5.1、2-5.2、2-6.1、2-6.2、2-6.3、2-7.1、2-7.2、3-H1、3-H2、3-H3、3-M1、3-M2、3-M3、3-M4、4-1.2、4-1.3、4-2.1(DB 写入阶段)、4-2.2、4-2.3、4-2.4、4-3.2、4-3.3、4-4.1、4-4.3、4-5.1(SQLite 投影层)、4-5.2、4-4.2、4-6.2、4-7.1、4-7.2。
- **待办**：报告内显式问题清单已清零；剩余可选工作是全量端到端压测与配置文档化。
- **2026-07 低优先级收口验证**：本轮补跑 `py_compile`（CLI/Gateway/Supervisor/Memory/Delegate 相关文件）、`git diff --check`、主链路回归 `tests/test_cli_auto_mode.py tests/test_gateway_activity.py tests/test_memory_health_signals.py tests/test_delegate_display.py tests/test_mem_governor_bridge.py tests/test_cli_tasks_command.py tests/test_supervisor_activity_window.py`，结果 **117 passed / 1 warning**；Supervisor 定向补跑 body_improvement 公平门控、owner 恢复/校验、状态机、schedule_token、Tier1 stats 不可用信号共 **11 passed**。完整 `tests/test_supervisor_self_evolution_queue.py` 中仍有若干 `stability_longcycle`/内生驱动策略历史断言稳定失败，未归入本轮全链条问题清单收口范围。
- **2026-07 跑通验证补充**：本轮针对全链条阻断点补跑 `planning_runtime.py` / `service_runtime.py` 编译、`git diff --check`，以及 12 个定向/轻量端到端用例：idle UTC、首轮 drive 异常保护、governor 重通知、周期 drive、formal dispatch、未知执行状态重试、孤儿恢复保守化、cycle 锁、agent_pull `execution_started_at` 超时、phase1 approved dispatch 与 duplicate dispatch。结果：**12 passed / 270 deselected**，耗时约 35.8s。
- **2026-07 当前验证**：`tests/test_supervisor_self_evolution_queue.py` 追加治理事件幂等、writeback 时间顺序回放、LM generation context 薄字段读取、proposal cognition fallback 薄 hypotheses、recent reference alignment 薄源头、proposal drift / recent cognitive alignment 薄源头、auxiliary memory 薄源头、post-task planned 过滤、长脏 history 双态回放、薄 why_not hypothesis 与 legacy 字符串 fallback 用例后当前收集 **224 items**；上一轮已按 node id 分片跑完旧基线：**200 passed / 6 skipped**。本轮新增后已补跑治理事件生成/消费/视图聚焦组（5 passed、6 passed），writeback 时间顺序回放单例（1 passed）和恢复/复发/排序组合（5 passed），LM context / prompt / proposal cognition 薄字段组合（5 passed），recent reference alignment / grounding focus 组合（4 passed），auxiliary/post-task/proposal drift/recent cognitive alignment/posture reasoning 组合（15 passed），long dirty history / recovery / relapse / replay 组合（4 passed），prompt compact 薄计数组合（3 passed），prompt reference alignment fallback 防叠加组合（4 passed），薄字段 auxiliary/hypotheses 组合（7 passed），以及本轮 legacy 字符串 fallback / 长链组合（6 passed、10 passed）。`tests/test_gap_coverage.py` 为 **31 passed / 1 xfailed**。旧的 endogenous_drive 失败记录已过时。
