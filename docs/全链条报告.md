# 全链条报告：自主链路与用户链路边界审查

更新日期：2026-07-07

> 本报告只保留当前有效基线、当前问题清单和历史别名对照；不再把长迁移历史留在主文，避免退场术语反向污染后续判断。

## 1. 审查基准

本报告按最新《voidcube架构基线》审查全执行链路。当前最高优先级语义如下：

- **自主链路**：API-B 内生驱动自主感知、判断并生成治理投影/任务；Web 小屋只展示 API-B 的状态、反馈、任务治理和自主任务回报；API-A 自主执行面（当前仍由 `/auto` 临时门控的最小迷你 CLI / 子代理通道承载）拉取并执行 `self_learning` 与 `body_improvement`；成果写入 Mem；下一轮 API-B 再读取并继续判断。
- **用户链路**：用户通过主 CLI 调用 API-A 主 Agent，走 `user_chat` 泳道，服务当前用户请求。
- **硬边界**：自主链路与用户链路必须互不抢占、互不覆盖、互不被解释成同一个 CLI 模式。`/auto` 与 `/auto-q` 只是当前自主链路的临时启停开关，不是主 CLI 的用户交互模式。Web 小屋不是用户聊天入口，只是 API-B 主视角的观测面。

## 2. 当前结论

当前设计方向基本成立，且本轮已经完成以下关键收口：

1. **双链路主语已固定**：架构讨论优先使用“自主链路 / 用户链路”，不再把 `/auto` 误写成主 CLI 模式。
2. **执行者边界已固定**：任务生产者归 API-B / Supervisor；任务执行者归 API-A 自主执行面；成果回流归 Mem。
3. **Web 小屋职责已固定**：Web 小屋只观察 API-B 判断、治理反馈、Mem 回流，以及 API-A 回报给 API-B 的自主任务状态。
4. **主读模型已固定**：`autonomous_observation` 当前有效主树为 `board / chain / loop / runtime / counts`。
5. **链路分段已固定**：Web / dashboard / CLI 自主观察 helper 统一围绕 `autonomous_observation.chain.segments`，顺序为 `api_b_candidates -> api_b_backlog -> api_a_ready -> mem_recent`。
6. **创造类任务归属已固定**：未被 API-B 放行的 `self_learning / body_improvement` 仍属于 `api_b_backlog` 治理段，不会因为类型属于 API-A 能力域就自动出现在 `api_a_ready`。
7. **API-A 待认领窗口语义已进一步收口**：`api_a_ready` 现在只承载真正“已放行、待 API-A 认领”的链路项；已经进入 `running` 的 API-A 任务只留在 `loop.stages.api_a_execution` 与最近事件/轨迹里观察，不再混回待认领窗口。

## 3. 当前链路对齐

### 3.1 自主链路

```text
API-B / Supervisor 内生驱动
  → 自主感知用户、系统、记忆、身体、治理状态
  → 形成 judgement / need / intent / governance output
  → 生成候选并管理治理在途项
  → API-A 自主执行面 pull 已放行任务
  → 执行 self_learning / body_improvement
  → 将结果、替身改进报告、任务 outcome 写入 Mem
  → 下一轮 API-B 读取 Mem 与任务历史继续判断
```

符合点：

- 任务生产归 API-B。
- 任务执行归 API-A 自主执行面。
- Mem 是结果回流位，不是执行槽。
- Web 小屋只展示这条闭环的 API-B 主视角观测。

### 3.2 用户链路

```text
用户
  → 主 CLI
  → API-A 主 Agent (`user_chat`)
  → 工具 / 临时上下文 / 必要记忆访问
  → 主 CLI 输出
```

符合点：

- 用户链路不依赖自主链路治理存储。
- 用户主 CLI 不承担自主任务执行槽。
- `/auto` 开关不得阻断用户输入或主 Agent 交互。

## 4. 当前协议与实现对齐点

- `read_model_version=10`。
- Web / 最小 dashboard / 主 CLI 自主观察 helper 统一消费 `autonomous_observation.chain.segments`。
- `loop.stages.api_a_execution` 直接承载 `status_label / chain_reason / activity_text / reason_style / focus_task`。
- `api_a_ready` 只统计 `approved / retry` 的放行待认领项；`running` 任务属于执行中阶段，不再算作“待认领窗口”。
- 内生判断层这一轮继续把这条边界正式抬进 perception：`deliberation.perception` 已新增 `api_a_ready_count / api_a_running_count`，Supervisor 不再只能从 backlog 侧面猜测“API-A 是在等认领，还是已经执行中”。
- `board` 当前稳定承载 `headline / summary / hero_summary / primary_focus / hero_pills / observation_notes / recent_activity`；Web 小屋不再把 `board.current_cards` 作为主投影层，而是直接消费这组 API-B 主视角读模型。
- Web 小屋这一轮继续把说明层从前端 helper 下沉回 Supervisor：`board.hero_summary / hero_pills / observation_notes` 现已成为正式读模型，hero 顶栏、观测说明卡与 dashboard 不再需要各自从 `counts/runtime/focus` 临时拼协议解释。
- segment `read_rule / next_step` 与 stage `read_rule / transition_hint` 已加入读模型；同时 Web 小屋这一轮继续把 `board.protocol_notes -> 前端 helper 再拼说明` 这层旧兼容撤掉，Supervisor 现在直接稳定投影 `board.hero_summary / hero_pills / observation_notes / recent_activity`。
- 主 CLI 自主执行面、监督事件镜像、最近执行摘要与 `/auto` 入口提示，已统一回到“链路项 / 链路裁决 / 执行焦点 / 自主链路”语义。
- Web 小屋已删除旧 `autonomous_observation.queue`、顶层 `api_b/api_a/mem/reread` 兼容镜像，以及 `presentation` 展示镜像层。
- Web 小屋这一轮继续去掉旧“队列台 / dispatch / LM 观察 / Perception → Intent”展示味：面板主语改回“API-B 判断输入 / API-B 当前判断 / 自主闭环分段观察”，用户链路信号只再作为“会话数 / 让路参考阈值”一类软感知说明出现。
- `ui_runtime` 内部围绕 API-A 段的旧 `dispatch` 命名也已继续收口到“execution lane / 待认领窗口”语义，避免后续会话把自主链路观测层重新理解成旧分发队列。
- 自主判断层这一轮也补上了同样的语义边界：`evaluate_endogenous_drive()` 现在会把 `governance_backlog_tasks` 与 `api_a_execution_lane_tasks` 分开投给内生驱动；`governance_backlog_count / stale_backlog_count / pending_review_count` 只再统计 API-B 治理段，不再把已经进入 API-A `approved/running/retry` 的链路项误算成治理拥堵。
- 同一轮里，学习主题去重与 `body_improvement` 冷却仍继续读取整条自主链路的 live tasks，因此这次收口只减少“误报治理压力”，不会因为 API-B backlog 变窄就放出重复学习候选或重复替身改进候选。
- 内生驱动候选载荷入口与 backlog 计数主语已继续收口：候选载荷入口已统一到 `to_backlog_item()`，高频 backlog 压力/恢复/计数字段也已回到 `backlog` 语义，减少旧“总队列管理器”心智继续反向污染 Web 小屋与自主链路。
- 执行服务对外入口已进一步收口到自主链路主语：executor 侧正式执行入口改为 `/executor/autonomous-chain/execute`，返回状态改为 `autonomous_chain_execution_executed / autonomous_chain_execution_recorded`，不再继续把执行面叫成 `formal_self_evolution`。
- 最小迷你 CLI 中 API-A 子执行面的本地事件主语也已继续收口：`enqueue/prompt_queue` 一类命名已改成“自主执行已起跑 / 首个执行回合 / 等待回写”这组更贴近链路阶段的说明，避免把 API-A pull/claim 执行面重新理解成旧本地消息队列。
- 监督者 scene 与循环摘要这一轮进一步统一为 `handoff / handed_off`，CLI、gateway、dashboard、Web 小屋不再同时混用“执行交接”文案与 `dispatch/dispatched` 旧键名。
- supervisor runtime / config 中最后一组还在充当正式主语的 `self_evolution_review_*` 已完成破坏性收口：现统一为 `autonomous_chain_review_*`，Web 调度视图同步读 `autonomous_chain_review_interval_seconds`，环境变量也切到 `SUPERVISOR_AUTONOMOUS_CHAIN_REVIEW_INTERVAL`。
- self-learning 结论回推 Supervisor 的入口已从 `submit_to_supervisor_queue()` 收口到 `submit_to_supervisor_backlog()`，避免把“学习结论转治理任务”继续误读成旧总队列写入。
- gateway / ops dashboard 这一轮继续削掉一层旧兼容：dashboard 对 API-A 观测现在只读取 `supervisor_task` 泳道，不再回退到顶层 agent 场景；gateway 活动状态恢复时也不再迁就旧 `self_evolution_*` 持久化键。
- Web 小屋抽屉与回合细节这一轮继续去掉“trace/source/execution family”实现味：前端现优先展示“回合 / 记录侧 / 治理动作 / 执行动作”语义，后端 trace 聚合也补上了对应的人类可读标签，房间舞台与动画骨架保持不变。
- `governance_preview` 这一轮也不再只是原始上下文字段透传：Supervisor 序列化阶段已补上 `summary / action_label / priority_label / merge_into_title` 等可读投影，Web 卡片与抽屉直接消费这些监督者说明，不再自行硬猜 shadow review 结构。
- `governance_preview` 这一轮继续摆脱 `lm_governance_*` 内部键名：Web/CLI 现优先读取 `review_outcome / followup_suggestion / priority_adjustment` 这组稳定投影，把“监督裁定 / 保留建议 / 优先级重排”作为正式展示协议，LM 仅留在决策上下文内部。
- Web 小屋的认知/溯源抽屉这一轮继续从“原始 cognition 快照直出”改成稳定投影协议：UI state 已新增 `cognition.judgement / cognition.uncertainty` 两层，先把 `dominant_constraint / why_not_improvement_now / recommended_probe / observation_target` 等内部字段折成 Supervisor 可解释中文语义，再交给前端展示。
- `renderProvenanceDrawer` 与认知面板也因此不再把不确定性账本当原始诊断对象硬摊开：现在主语回到“当前判断 / 为什么暂不直接改替身 / 当前最该补哪侧证据 / 建议探针是什么”，候选卡片同时补齐“候选类型 / 信号来源 / 学习分支 / 浮出原因”。
- Web 小屋的面板标题与空状态文案这一轮也继续从实现视角收口：`LM 输入 / 认知状态 · 感知到意图 / Prompt 预览` 已改成 `API-B 判断输入 / API-B 当前判断 / 判断输入预览`，同时把 `LLM 健康` 一类外显文案收薄为更中性的“判断模型健康 / 记忆模型健康”。
- Web 小屋这一轮继续把“临时门控 / 驱动可用”这类运行时内部信号从前台主说明里降权：hero pill 已改成“待认领窗口不含执行中”，stats 面板的“治理状态”也收回“观测状态”，主语变成“当前焦点 / 用户软感知 / 观测快照 / 最近动作”，避免房间再次长成控制台或旧队列台。
- 同一轮里，前端读取用户软感知也不再优先钻 `runtime.activity_guards.user_chain_signal` 这层旧嵌套，而是先消费 `runtime.user_chain_signal` 正式分支；抽屉底部的辅助说明也从“临时开关已开启/关闭”改成“观测快照来源”，进一步把 Web 小屋收回只读观察角色。
- 这一轮又继续做了一次破坏性收口：Web 小屋 `runtime` 已不再暴露 `activity_guards / eligibility / drive_available / autonomous_chain_gate_active` 这组旧控制台兼容壳，前端也删除了 `schedule` 这层空转调度读取；房间观测协议现在只保留 `user_chain_signal + snapshot_source + chain/loop/board`，把“用户软感知输入”与“自主闭环分段投影”作为唯一正式前台读法。
- Web 小屋输入链这一轮也开始脱离旧 guard 聚合器直连：Supervisor 新增 `runtime/observation-input` 正式入口，UI state 现在先读取这条只含 `activity / user_chain_signal / snapshot_source` 的窄快照，再回退到本地缓存或默认值；`evaluate_activity_guards()` 继续留在自主判断层与运行时决策层使用，不再充当前台观测输入的直接协议名。
- 自主判断主干这一轮也开始同步收口主语：`EndogenousDriveEngine` 的 `build_deliberation_report() / generate_candidates() / resolve_cognitive_posture_state()` 已新增 `drive_input` 正式入口，并先在内部统一通过 `drive_input -> drive_context / perception / LM evidence packet` 传递；旧 `activity_guards` 目前仅保留为兼容参数，避免测试和长尾调用一次性失稳。
- 同一轮里，候选 evidence 与 LM 证据包里会外显给后续判断/排障视角的旧 guard 名字也继续下沉：`activity_guard_checks` 已收回 `observation_checks`，truthfulness 候选的 `signal_source` 也改为 `runtime_observation_snapshot / raw_counts`，不再把 `evaluate_activity_guards` 直接暴露成上层判断材料。
- 测试心智层这一轮也开始跟着收口：`test_supervisor_autonomous_chain_store` 的高频样例构造器已从 `_endogenous_activity_guard_payload` 改成 `_endogenous_drive_input_payload`，近期补过的 LM evidence / drive context 断言块也开始用 `drive_input` 局部变量和 `drive_input=` 入口，减少搜索结果继续把“自主判断输入”误读成旧 guard 模式。
- 随后的测试收口又继续往 mock 命名和调用入口推进了一层：`test_supervisor_autonomous_chain_store` 里高频 `fake/stable/strained/replay *_activity_guards` 已改成 `*_drive_input`，`test_gap_coverage` 里直打 `EndogenousDriveEngine` 的高频入口也已从 `activity_guards=` 收回 `drive_input=`；运行时仍保留的 `request["activity_guards"] / decision_context["activity_guards"]` 则暂作为正式协议位保留，避免把 review/runtime 接口一次性扯乱。
- Web 小屋内部围绕链路排序的旧调度兼容这一轮也继续退场：`ui_runtime` 现只再读取 canonical `scheduled_for` 作为链路投影排序信号，不再在观测层吸收 `scheduled_at / run_at / execute_after / window` 这组历史队列别名，避免 Web 小屋再次长成旧任务台兼容器。
- 同一轮里，内生驱动也开始用这组 lane 计数主动约束成长节奏：当 `api_a_ready_count / api_a_running_count` 大于 0 时，`prepare_body_growth` 不再提前冒出，`body_growth_quota` 会被压回 0，学习扩张也会额外增加约束惩罚，优先等待 API-A 执行回流沉淀后再继续扩张。
- `board.observation_notes / hero_pills` 这一轮补上“待认领窗口边界”正式说明，明确 `api_a_ready` 不含执行中项、执行中留在 `loop.stages.api_a_execution`、写回结果留在 `mem_recent`，减少后续会话把三段重新混成旧执行队列。
- Web 小屋的 `cognition.judgement` 与 `board.observation_notes` 这一轮也开始直接解释“为什么这轮暂不继续推出替身改进 / 学习扩张”：当 `api_a_ready_count / api_a_running_count` 大于 0 时，前台会明确提示先等 API-A 认领、执行、写回并由 API-B 再读取沉淀，而不是继续把这类停顿误解成旧 gate 或旧队列阻塞。
- 测试与专题 gap 文档这一轮也继续清掉一批容易误导新会话的旧主语：`queue item / task queue / dispatch` 一类测试标题、skip reason 与专题说明，已收回 `backlog / execution handoff / 自主链路分段投影` 语义。
- supervisor runtime 这一轮也继续把正式配置与测试名往当前主语收口：`self_evolution_handoff_limit_per_cycle` 已改成 `autonomous_chain_handoff_limit_per_cycle`，相关 review-cycle 测试名和结论回推测试名也不再沿用 `self_evolution_cycle / into_queue` 旧说法。
- 同一轮里，planning runtime 的 cycle lock 内部属性也已改成 `autonomous_chain_cycle_lock`，dashboard 的 `handoff` 场景外显文案收成“自主交接”，继续减少代码搜索结果与状态栏里的旧 `self_evolution / 执行交接` 回声。
- gateway activity 这一轮开始补“可读标签层”而不是只暴露 canonical runtime 值：`recent_metadata` 与 `task_identity` 现在会附带 `*_label / display_label`，把 `self_evolution / body_switch / body_improvement / memory_maintenance` 等运行时值同步投影成人可读的“自主改进 / 身体切换 / 替身改进 / 记忆维护”语义，便于 dashboard、日志和排障视角直接站在自主链路主语上。
- gateway / trace 这一轮继续削掉一层宽泛摘要：`task_identity` 中无人消费的 `labels / label_texts` 已移除，trace 的网关活动摘要也开始优先使用 `task_identity.summary / title / display_label` 指向当前链路项本身，而不是再回退到更粗的 `task_type` 族语义。
- 最小 ops dashboard 这一轮继续破坏性收口：总览标题改为 `API-B 主视角自主闭环总览`，并直接消费 `board.hero_summary / primary_focus / hero_pills / recent_activity`；“最近自主动作”不再从 gateway `recent_metadata` 本地再拼一份兼容摘要，而是只认 Supervisor 已投影出的闭环读模型。
- Web / CLI 这一轮继续把“焦点是谁”这件事彻底交回 Supervisor：前端 hero 与 CLI 摘要都开始优先直接读取 `board.primary_focus`，不再先从 `loop.stages` 反推一版临时焦点。
- planning runtime 这一轮也继续把内部治理主语和外显协议对齐：LM review 返回与批量复核上下文里的 `shadow` 内部名已收回 `followup_suggestion`，治理在途快照 prompt 也不再把当前主协议描述成泛化“任务列表”。
- planning / endogenous drive 这一轮继续把“自主判断”从旧候选器口吻里剥离出来：监督者活动里的 `Endogenous drive found no new governance candidates` 与“新增治理候选”已分别收回“本轮未形成新的治理在途投影 / 新增治理在途链路项投影”，内生驱动 backlog 快照里的 `queued_body_titles` 这类实现味变量也已改回 `body_improvement_backlog_titles`，减少后续维护把 API-B 判断层再理解成旧队列生成器。
- 同一轮里，`endogenous_drive` 内部也继续把“canonical runtime 值”和“判断层内部命名”拆开：`self_evolution` 仍作为稳定治理类型保留在输出协议里，但 `self_evolution_plan` 这类内部变量已回收到更贴近语义的“自主改进判断计划”口径，避免后续维护把内部 deliberation 结构误读成必须处处跟 runtime 枚举同名。
- 同一轮里，进入 `evidence_packet / governance_backlog_snapshot / decision_core.summary` 的中层判断材料也开始统一回中文：高频 backlog 摘要、避免重复提案的 guidance、若干 need rationale 与 decision core summary 已不再混用英文键值串，进一步降低“自主判断本体已经收口，但中间材料还像旧实验层”的割裂感。
- Web 小屋前端的长尾动作标签也继续和 gateway label 层对齐：卡片类型与治理/执行动作提示现在优先使用 `task_identity.display_label`，`body_switch` 外显语义收回“身体切换”，`self_evolution` 则回到“自主改进”，减少前后端同一动作双翻译的情况。
- Web 小屋这一轮继续减少前端“各自拼活动摘要”的做法：`supervisor ui state` 已新增 `autonomous_observation.board.recent_activity` 稳定投影，直接把 gateway 最近一次自主规划、执行回报、自主学习或写回异常收口成“阶段 / 标题 / 摘要 / 来源侧 / tone”读模型；原先那份 `runtime.recent_activity` 重复镜像也已删除，链路总览 hero、观测面板和抽屉统一只消费 board 投影，而不是继续散落地读原始 activity 快照。
- 同一轮里，hero 顶栏也不再把 `protocol_notes` 再拼成一层额外 pills；Web 现在只消费 Supervisor 正式投影出的 `hero_pills` 与 `observation_notes`，减少前端自行加戏。
- 同一轮里，Supervisor 侧也不再先塞 `protocol_notes` 再让 helper 兜底；board 已直接成为完整观测板投影，进一步减少 Web 小屋重新长出“兼容解释层 / 队列台说明层”的概率。
- Web 小屋这一轮继续把“房间 scene 自己回头查原始 backlog 行”这层旧中间态撤掉：`ui_runtime` 现在通过 `list_chain_projection_tasks()` 先拿自主闭环投影，再直接基于 `autonomous_observation.chain / loop` 判定房间 scene；Web 房间状态不再额外依赖一份“治理积压原始行 -> 再判 scene”的旧队列式装配。
- 同一轮里，Web 小屋认知面板与最小 dashboard 的展示主语也继续收口：对外标签里的 `候选池 / 治理积压 / 清理治理积压 / 治理积压阻塞` 已回收到 `候选形成段 / 治理在途 / 疏通治理在途 / 治理在途阻塞`，尽量避免展示层继续把自主闭环解释成旧队列管理器。
- 同一轮里，`endogenous_drive` 内部会直接流入治理信号、观察建议和候选标题的外显文本也开始同步收口：真实性观察、阻塞/准备度偏弱、对齐后再推出候选、成长窗口观察，以及 `复核治理在途卫生` 这类候选标题，已不再继续混用旧英文句式或“治理积压”旧主语。
- 随后的收口又进一步下沉到 reflection / governance signal / truthfulness alert 这层判断材料：`governance backlog state... / Truthfulness pressure... / recent learning yield is ...` 这类会直接进入自主判断与治理事件的英文句式，也已回到当前中文链路口径，减少“展示层已收口，但判断原料还像旧实验层”的割裂感。
- 同一轮里，`endogenous_drive_prompts.py` 的中层压缩包也继续做了同语义收口：`queued_body_titles` 这类旧内部名已退场，`governance_backlog_snapshot / grounding_focus` 的 summary 与 guidance 已改回中文链路口径，prompt 不再持续把旧 backlog/queue 叙事喂回自主判断。
- 随后的收口又继续覆盖到 trace/runtime 与 phase1/gap 这批高频搜索入口：轨迹标题、时间线样例、监督者活动摘要、学习结论回推样例和若干测试主题，已进一步从英文 demo 文本与“治理积压/queue”旧口吻收回当前中文链路语义，减少新会话把测试样例误读成主设计说明的概率。
- 这一轮继续把“旧队列台口吻”从高可见入口往外迁：`项目文件架构说明` 已把自主链路主干改回“治理在途投影 -> API-A 自主执行面 -> Mem -> API-B 再读取”，`ui_runtime` 的 scene 说明和 phase1 端到端测试步骤也统一强调“API-B 只判断/治理/交接，API-A 才执行”，进一步压低 Web 小屋和测试样例把系统误导成旧任务队列管理器的概率。
- 同一轮里，`trace_runtime` 的 fallback 标签层也补了统一归一化：即便内部事件名带空格、连字符或缺少显式摘要，轨迹视图仍会落回“批量复核 / 监督者活动 / 治理记录”这类人类可读主语，而不是把原始 event code 直接暴露给 Web/CLI 排障视角。
- `trace_runtime` 与 Web 抽屉里的回合细节这一轮也继续做了同语义收口：gateway timeline 摘要现在优先使用 `task_identity.display_label / *_label`，`body_switch` 收回“身体切换”，`execution_handoff_*` 外显语义收口成“自主交接*”，`gateway_activity_log` 记录侧文案也统一回“网关回报”，减少 timeline / 抽屉 / 最近动作三套中文各自翻译。
- planning / trace runtime 这一轮也继续清掉一批会直接落到活动摘要、裁决原因和异常提示里的英文句式：内生驱动新增候选、治理放行/延后、自主交接完成、网关活动暂不可用等摘要现已统一收回中文口径，排障视角不再在同一条自主链路里混杂中英文。
- Web / trace 展示层也继续加了一层旧状态吸收护栏：即便底层长尾数据仍出现 `queued`，当前 UI 状态与 trace 标签也只会投影成“待审核”，不再把旧 `queue` 主语重新暴露给 Web 小屋或排障视角。
- 这层旧状态吸收护栏这一轮进一步从“主展示字典兼容”收成“统一归一化入口”：`queued` 现在只作为历史输入被折叠成 `planned -> 待审核`，不再继续占据 Web 小屋与 trace 的一等状态枚举位。
- 这一轮又继续把这层吸收护栏往实现底部压了一格：Python 侧 `ui_runtime / trace_runtime` 已不再各自维护一份 `queued -> planned` 和状态中文映射，而是统一收进 `observation_status.py`；`build_observation_card()` 也会先做状态归一化，`board.pop("protocol_notes")` 这类只剩展示层防御味的残影已删掉，Web 小屋卡片现优先直接消费后端投影好的 `display_status`。
- 紧接着，Web 小屋前端这轮也把最后几处散落状态 fallback 收成了单点 helper：抽屉内链路项状态、再读取提示、hero 焦点 badge 与卡片 badge 现在统一先走 `observationDisplayStatus()`，优先消费 `display_status / status_label`，只有在后端投影缺位时才回退到原始 `status`。前端不再在多个面板里各自解释一遍状态协议。
- 再往前走一步后，前端连 `queued -> planned` 这层历史别名吸收也已经退场：`normalizeObservationStatus()` 现在只保留大小写/空白归一化，不再承担旧状态翻译；`queued` 只允许在 Supervisor 共享归一化入口里被吸收，Web 小屋自身正式退回纯展示角色。
- `gap_coverage / autonomous_chain_store` 这类长尾测试里的样例主题、证据标题与局部变量名这一轮也继续清了一批：高频 `"queue scheduling fairness" / "weak queue evidence" / queued` 现已回到 `backlog scheduling / weak backlog evidence / planned/backlog_snapshot`，进一步降低后续搜索结果把旧队列语义当主路径的概率。
- phase1 / executor / autonomous gate 这批长尾测试里的旧 `queued / dispatched / Learn queue *` 噪音也继续清掉一轮；同时删掉了一个已经失真的 `_dispatch_self_learning_followup` 测试假路径，避免后续会话把不存在的 supervisor 分发分支误读成当前主逻辑。
- 主 CLI 与自主链路桥接壳这一轮也继续向“后台缓存、前台只读”收口：`autonomous_observation_summary_sections()` 不再在用户链路前台同步拉 supervisor / gateway 活动快照，而是触发后台刷新并直接消费 `_supervisor_state_cache / _autonomous_gateway_activity_cache`；因此 `/status` 一类主 CLI 读操作不再为了展示自主链路摘要而同步阻塞用户链路。
- 同一轮里，`/auto` 控制路径本地收尾也从多处分支收回到单点 helper：`autonomous_gate.py` 新增 `_enter_autonomous_gate_locally()` 与 `_exit_autonomous_gate_locally()`，把 `/auto`、`/auto-q`、fast-path 退出、force-quit` 里重复的“激活本地执行面 / 标记 gate 关闭 / 发布 idle scene / 记录退出事件”统一到同一组桥接逻辑，减少主 CLI 未来再长回一堆分支型自主控制脚本。
- 再往前一步后，`/auto` 激活成功后的即时摘要也不再同步拉一次 supervisor snapshot：控制路径现在只触发 `refresh_supervisor_status()` 后台刷新，并优先读取 `_supervisor_state_cache`；如果缓存还没到位，则明确提示“监督者快照将在后台刷新后进入观测面”。这样激活命令与自主观测读取又拆开了一层。
- 紧接着，这一轮又把 `cli.py` 里 4 个只剩转发意义的 `/auto` 壳方法直接删掉：命令分发、fast-path 退出与 Ctrl+D 强退现在直接调用 `autonomous_gate` 模块函数，测试也同步改成以模块函数为入口；主 CLI 对 `/auto` 的耦合进一步从“方法级桥接”收成“少量调用点桥接”。
- 再往下收一层后，`_ensure_autonomous_executor_session / _trigger_autonomous_cycle` 这两个桥接壳也已从 `cli.py` 退场：`autonomous_gate.py` 现在直接调用 `autonomous_presence.ensure_autonomous_executor_session()` 与自身的 `trigger_autonomous_cycle()`，相关测试也不再经由主类 wrapper。主 CLI 与自主链路控制入口的边界继续收紧。
- 这一轮又继续把自主执行面展示壳从主类剥掉：`_autonomous_execution_panel_height / _build_autonomous_execution_panel_rows / _get_autonomous_execution_panel_fragments` 已从 `cli.py` 退场，TUI 面板容器直接调用 `autonomous_panel / autonomous_events` 模块函数；相关 panel 测试也改成以模块函数为入口，避免主类继续充当展示层转发表。
- CLI 自主观察摘要里的治理统计这一轮也不再外显 `direct_lm_actions / shadow_recommendations` 一类实现字段，现统一回到“裁定 / 建议 / 重排”人类可读主语；自主交接事件标签也同步和 Web/trace 收口。
- API-A 本地诊断这一轮也继续做了语义收口：本地阶段码已从 `approved_waiting_claim / claimed_* / running_elsewhere` 收为 `waiting_api_a_claim / local_claimed_waiting_first_turn / local_claimed_waiting_writeback / local_claimed_active / running_on_other_api_a`，更贴近“待认领 / 首回合 / 写回 / 他处在途”而不是实现过程。
- Web 小屋这一轮继续把“总任务台”内味抽掉：`ui_runtime` 内部给 Web 观测面准备的统计不再暴露 `observed_task_total / autonomous_task_total / api_b_task_total / pending / active` 这类混合总量，而是改成围绕 `chain_projection.governance_backlog / api_a_running / api_a_ready / candidate_signals / writeback_history / body_improvement` 组织；Supervisor scene 也开始直接从 `autonomous_observation.chain.segments` 读候选形成段，而不是再用一份原始候选列表单独判相。
- 自主判断本体这一轮也继续从旧英文实验层收口：`planning_runtime` 里由 drive judgement 合成的 canonical cognitive assessment 已改回中文链路口径，`endogenous_drive` 里的近期认知评估记忆 / 任务后效记忆 / 元认知画像空态摘要，以及 self-model snapshot 主摘要也已同步改回中文；同时一批仍在调用 `self_evolution_plan=` 的判断测试入口已统一收回 `autonomous_improvement_plan=`，避免历史参数名继续反向污染当前主语。
- prompt shell 与长尾判断样例这一轮也继续跟进：`endogenous_drive_prompts.py` 中 posture block、任务排序说明、`cognitive_assessment` 输出指令和示例 `posture_alignment` 文案已尽量改成“中文说明 + canonical 值保留”的形式；高频 `switch_reason / self_iteration_hypothesis / rationale / priority_basis` 测试样例也继续改回中文，减少“判断链主语已收口，但 prompt 与长尾 fixture 还像旧英文实验层”的割裂感。
- 同一轮里，`proposal_cognition.summary` 这类压缩摘要也不再回吐 `LM cognition status=...; proposal drift=...` 旧英文短句，而是收回 `LM 认知状态=...；提案漂移=...。`；对应的高频测试标题、链路投影样例标题和 prompt 断言也已同步改回当前主语，进一步降低后续搜索结果把旧断言口吻误认成正式协议的概率。
- 内生驱动候选标题与多处高频快照样例这一轮也继续改回中文：`维持长期记忆连续性 / 复核近期不确定性与纠偏信号 / 复核治理在途卫生 / 改进 shell 替身` 等标题开始成为主路径主语，减少 Web/CLI/测试搜索结果里的英文历史回声。
- gateway / supervisor 里围绕 owner 校验、孤儿恢复和晚到写回补偿的一批失败原因与恢复摘要这一轮也继续收成中文：CLI 会话未知、owner 不匹配、归属未知拒绝写回、恢复后接纳晚到写回等分支现在都能直接站在自主链路排障语义上阅读。
- 主 CLI 这一轮继续收薄：`cli.py` 不再通过一组逐个转发的 runtime host wrapper 调自主执行逻辑，而是统一通过单个 autonomous runtime 实例桥接；`autonomous_runtime_host.py` 也已清掉那组重复转发函数。随后又去掉了一层状态格式化薄壳，测试直接回到 observation helper；`/auto` 激活时用到的 supervisor 快照获取与格式化也已下沉回 gate/status host。进一步地，`/auto-q / fast-exit / force-quit` 的退出路径也开始直接调用 host 上既有方法，不再由 `cli.py` 手工传一长串回调，本地 CLI 主类进一步回到“用户链路入口 + 薄桥接壳”。
- 主 CLI 已把自主观察 helper、panel 壳层、`/auto` gate、presence、events、runtime host、status host 下沉为独立模块；监督者快照摘要 wrapper 与 panel 侧状态读取 wrapper 已继续删除，autonomous panel 直接读取 status host 缓存，`/auto` gate 改成显式 callback 协作 runtime/status host，不再静默依赖已删除的 host 方法，`cli.py` 进一步收口为“用户链路入口 + 自主链路薄桥接壳”。
- 紧接着，这一轮又把 `cli.py` 身上最后那层“自主事件转发表”也削掉了：`_append_autonomous_execution_event / _sync_autonomous_supervisor_event` 已退场，executor、gate、status host 与测试直接调用 `autonomous_events` 模块函数；主 CLI 不再假装自己是自主链路事件总线。
- 同一轮里，主 CLI 身上的一小组 presence / 观测转发表也继续退场：`_current_cli_agent_role / _current_gateway_presence_snapshot / _refresh_autonomous_observation_surfaces` 已从 `cli.py` 删除，相关判断测试直接回到 `autonomous_presence` 模块；空闲态后台刷新现在也改成在 `process_loop` 里直接组合 `status_host + presence` helper，而不是再经由主类薄壳转发一次。
- 再往前走一步后，scene 上报主函数也正式从 `cli.py` 迁出了：`_push_cli_agent_scene` 已下沉到 `autonomous_presence.push_cli_agent_scene()`，`autonomous_gate` 的激活/退出路径改成显式接收 `refresh_gateway_cli_presence_callback / push_cli_agent_scene_callback`，spinner keepalive 与相关测试也直接调用模块函数；主类里的 `_publish_cli_agent_scene / _refresh_gateway_cli_presence` 这两层 presence 壳随之退场。
- 紧接着，自主执行面这侧的一小束实现味 helper 也继续从主类收回模块：`_autonomous_task_execution_kind / _autonomous_task_label / _build_autonomous_task_prompt / _autonomous_task_run_id_for_chat_message / _clear_current_autonomous_task_state` 已从 `cli.py` 退场；`autonomous_executor` 现在直接使用模块级 `autonomous_task_execution_kind / autonomous_task_label / build_autonomous_task_prompt / autonomous_task_run_id_for_message`，runtime host 只补传 `git_head_commit`，测试也改成直接验证 executor 模块函数。
- 同一轮里，runtime 协作这组薄壳也继续清掉了一层：`_poll_autonomous_workflow / _interrupt_current_autonomous_task / _report_current_autonomous_task_timeout_if_needed / _post_autonomous_task_decision / _submit_body_improvement_report` 已从 `cli.py` 退场；`process_loop` 的 timeout 检查和空闲态自主拉取直接走 runtime，`autonomous_gate` 的 `poll/interrupt` 改成显式 callback 协作，相关测试也统一回到 `cli._autonomous_runtime()`。
- Web 小屋读模型这一轮也继续往“闭环投影管理”收口：`ui_runtime` 里围绕 API-B 治理段的内部拼装命名已从 `api_b_pending / supervisor_active / agent_active` 收回 `governance_backlog_cards / api_b_focus_task / api_a_running_task`，降低后续维护把观测层重新读成旧“队列台 + 分发器”的概率；`内生驱动核心设计` 也同步把“任务列表治理”主语改回“治理在途 / 待认领窗口 / Mem 写回”。
- 最小 ops dashboard 这一轮也继续从“本地再解释一份协议”退回“Supervisor 投影直读”：`_build_autonomous_chain_snapshot()` 已拆成 `_project_chain_stage / _project_chain_segment` 两个净化 helper，只负责清洗 `board / chain / loop` 已有投影，不再按 `segment key` 本地补一套 `治理在途 / 待认领窗口` 等解释；对应测试也已改成显式断言 dashboard 优先信任 Supervisor 已投影的 label / stage_label / status_label。
- 同一轮里，最小 ops dashboard 也开始把 `api_a_running` 作为正式计数单独抬出，与 `api_a_ready` 分开展示“执行中 / 待认领”两段，进一步防止前台把 API-A 在途态重新并回旧待处理总量。
- `planning_runtime` 这一轮也把正式协议位开始并行投影到 `drive_input`：`evaluate_endogenous_drive()`、`_run_endogenous_drive_cycle()`、`decide_autonomous_chain_task()` 与 `review_autonomous_chain_tasks()` 现在都会在返回体或决策上下文里并行带出 `drive_input`，而旧 `activity_guards` 先仅作为兼容字段保留，避免 runtime / review / execution request 继续把旧主语喂回新链路。
- 接着这一轮又把 Web 小屋 / 最小 dashboard 里一块最容易重新长回“旧 gate 面板”的残影直接拿掉了：dashboard 不再本地读取 gateway 活动时间戳去拼 `countdowns / observation_context / autonomous_chain_policy` 这层前台协议，也不再展示“规划空闲 / 执行空闲”一类旧 countdown 面板；现在前台只保留一块更窄的 `observation_input`，明确表达“用户链路软感知 + 快照来源 + 作用边界”这组 API-B 判断输入。与此同时，Web 小屋统计面板里对应小节也已从内部 `gateSec` 收回 `API-B 判断输入` 主语，减少后续实现继续把观测面写成队列门控台的概率。
- 再往下压一层后，`review` 这条正式治理链也开始把 `drive_input` 当主协议消费了：`_review_task_governance_with_supervisor()` 现在以 `drive_input` 为主输入构造 LM prompt，`review_autonomous_chain_tasks()` 的默认决策也优先读取 `drive_input` 中的 family / governance decisions；同时 execution request 里虽然还保留 `activity_guard_evidence` 这个历史字段名，但实际已优先写入 `decision_context["drive_input"]`，把“旧字段名 / 新内容体”的兼容层进一步往底部压。
- 紧接着，`decide` 这条自动裁决链也一起开始切主：`_build_autonomous_chain_auto_decision()` 已改为优先读取 `drive_input`，`decide_autonomous_chain_task()` 和 `review_autonomous_chain_tasks()` 在调用时都会把对应 task 的 `drive_input` 显式传入；相关 formal handoff 测试也已补上断言，确认 execution request 虽仍沿用 `activity_guard_evidence` 历史字段名，但其中写入的已是 `drive_input` 内容而非旧 guard 镜像。
- 这一轮又把这层 formal handoff 兼容壳再往底部压了一格：`AutonomousChainExecutionRequest` 现在已经新增 `drive_input_evidence` 正式字段，builder 也开始直接写它；旧 `activity_guard_evidence` 不再是唯一来源，而是由模型在加载/序列化时自动与 `drive_input_evidence` 对齐，专门留给旧消费端过渡使用。
- 随后连 supervisor 读侧透传链也补上了同样的护栏：`_serialize_autonomous_chain_task()` 在输出 `execution_request` 时，会优先显露 `drive_input_evidence`，并把只带旧 `activity_guard_evidence` 的历史请求补齐成双字段形态；因此 trace / list / get 这类仍以“原样透传 execution_request”为主的读侧，不需要先改一大轮消费代码，就能先看到新字段。
- 再往前推一步后，`planning_runtime` 的外部请求入口也开始认 `drive_input` 正式协议了：`evaluate_endogenous_drive()`、`decide_autonomous_chain_task()` 与 `review_autonomous_chain_tasks()` 现在都会先尝试读取 `request["drive_input"]`，只有缺席时才回退到 `request["activity_guards"] -> evaluate_activity_guards()` 这条旧路径。对应回归测试也已补上，明确验证“传 `drive_input` 时不会再去触发 guard probe”。
- 紧接着，runtime trace 这条排障读侧也补上了 execution request 归一化入口：`trace_runtime` 在收集 `execution_request` 记录时，会显式把 payload 归一成 `drive_input_evidence + activity_guard_evidence` 双字段形态，因此即便遇到只带旧字段的历史请求，时间线与 trace 详情里也能直接看见 `drive_input_evidence`，不用再靠上游恰好做过一次序列化补齐。
- 这一轮又继续把 Web 小屋里最容易长回“旧队列管理台”的心智往下压了一层：`ui_runtime` 的链路项排序不再依赖 `scheduled_for` 这类旧排队槽位，而是按“执行中 / 待认领 / 候选 / 治理在途 / 已完成”阶段优先级组织 API-B 观测投影；`service_runtime` 与 ops `serve` 里的 countdown 文案也一起退场，避免后续实现再把 Web 小屋理解成旧调度面板。
- 同一轮里，测试心智层也继续收口：`test_supervisor_autonomous_chain_store` 的高频样例构造器已从 `_build_activity_guards()` 改回 `_build_drive_input()`，两条 auto/review 核心回归测试名和决策上下文主断言也开始优先围绕 `drive_input`，旧 `activity_guards` 只再保留兼容镜像校验。
- 再往前收一层后，Web 小屋 board 里那层“创造类 backlog”二次脑补也退场了：前台 `chain_waiting` 与 `observation_notes` 不再单独推断“创造类链路项卡在治理段”，而是直接围绕正式 `api_b_backlog / api_a_running / api_a_ready` 阶段计数组织说明；对应的旧内部 helper 命名也同步收回 `API-A lane family` 语义，避免把 Web 观测面继续绑定到内部候选分类。
- 紧接着，Web 小屋前端里按 `group.key` 本地猜“候选/治理/回流”展示 copy 的那层逻辑也开始下沉到后端读模型：Supervisor 现在会在每个 `chain.segment` 上直接投影 `display_copy`，前端优先直读 `item_label / event_label / trace_label / footer_label / drill_label`，减少后续前端再次长成一套本地协议解释器。
- 同一轮里，review 长尾回归又往 `drive_input` 正式入口推进了一步：治理预览、优先级重排和 LM override 这几条核心样例已开始直接用 `request["drive_input"]` 触发 review，只把 `activity_guards` 留作 LM review 上下文里的兼容镜像校验。
- 再往前压一层后，连分段装饰这层前端 key 映射也开始退场：Supervisor 现在会在每个 `chain.segment` 上并行投影 `display_decor.cls/icon`，前端优先直读后端给出的装饰元数据，不再把 `api_b_candidates / api_a_ready / mem_recent` 这组 key 到样式的映射长期留在前台。
- 同一轮里，几条“保留 API-A pull 放行态 / body_improvement 预热放行”回归样例也开始直接走 `drive_input` 请求体；`_endogenous_drive_input_payload()` 现已补齐 `active_sessions / quiet_after_seconds` 等参数，便于后续继续把 review 测试从旧 guard request 切到正式输入协议。
- 再往前推一格后，loop 与 metrics 这两层前端展示推断也开始让位给后端投影：`board.metric_cards` 已由 Supervisor 直接生成，`loop.stages` 也开始并行带出 `lane / observation_role`，前端不再必须靠固定 stage key 自己反推展示身份。
- 同一轮里，`test_supervisor_autonomous_chain_store` 里“shadow governance / reprioritize”这两条 review 样例也已改走 `drive_input` 正式请求体，进一步缩小 `activity_guards` 还停留在请求层的测试面。
- 紧接着，loop rail 这一层也开始跟着后退到兼容位：`loop.stages` 现在还会并行带出 `rail_state / rail_note / is_focus`，前端优先消费这些后端投影，只在缺字段时才回退到本地 key/status 推断。
- 同一轮里，auto decision、批量 defer，以及一组 API-A pull / body-improvement / schedule-conflict 相关 review 样例也继续切到了 `drive_input` 请求体；测试 helper 的 `_endogenous_drive_input_payload()` 现已补上顶层 `decisions`，后续再迁移 review 样例时不需要继续用旧 guard request 兜底。
- 再往前收一层后，前端里一个已经失去语义价值的 `creativityGovernanceBacklogCount()` fallback helper 也被删除；当前请求层里剩余显式 `activity_guards.now` 的样例，基本只落在已 `skip` 的旧 body boundary 路径上，不再污染自主链路主回归。

## 5. 当前架构判断

按最新语义，VoidCube 应被理解为“双链路共享底座”：

- 用户链路保证用户当前交互稳定、直接、优先。
- 自主链路负责 API-B 内生判断、API-A 自主学习/改进、Mem 闭环和 Web 可观测。
- 两者共享 API-A 能力域和 Mem，但必须通过 `user_chat / supervisor_task`、主 CLI / 最小迷你 CLI、用户输出 / API-B Web 观测这三层边界隔离。

因此，当前设计在目标方向上是成立的。当前主要风险已经不再是“缺少组件”，而是某些残留命名、残留文案或旧活动文档仍可能把自主链路误读成“主 CLI 的另一种运行模式”。

## 6. 迁移历史入口

2026-07-03 到 2026-07-06 的连续收口过程，已经拆出到 [全链条迁移日志.md](./全链条迁移日志.md)。

如果需要追溯 `queue / watch_groups / presentation / self-evolution / auto-mode` 这些术语是如何退场的，请查看迁移日志；主报告不再保留长历史段。

## 7. 当前剩余全链路问题清单

以下问题仍然存在，但边界已经比较清楚，适合下一轮继续破坏性收口：

| 优先级 | 问题 | 当前状态 | 建议下一步 |
| --- | --- | --- | --- |
| P1 | 主 CLI 内仍保留自主链路桥接壳，但已继续变薄 | 观察 helper、panel、gate、presence、events、status host 已独立模块化；监督者快照摘要 wrapper、panel 侧状态读取 wrapper、gate 对旧 host 方法的隐式依赖已删除；`cli.py` 已统一通过单个 autonomous runtime 实例桥接，runtime host 里的逐个转发函数已清掉；这一轮连自主观测摘要也不再在前台同步拉 supervisor / gateway，而是改成后台刷新缓存、前台只读；`/auto` 激活与退出的本地状态收尾也已集中到 `autonomous_gate.py` 的单点 helper；激活成功后的即时摘要也不再同步请求 supervisor，而是只读缓存并等待后台刷新；`cli.py` 里 4 个纯转发 `/auto` 壳方法、`_ensure_autonomous_executor_session / _trigger_autonomous_cycle` 这两个桥接壳、自主执行面 panel height/rows/fragments 这 3 个展示壳、`_append_autonomous_execution_event / _sync_autonomous_supervisor_event` 这两个事件壳、`_current_cli_agent_role / _current_gateway_presence_snapshot / _refresh_autonomous_observation_surfaces` 这组 presence/观测壳、`_publish_cli_agent_scene / _refresh_gateway_cli_presence` 这组 scene/presence 壳、`_autonomous_task_execution_kind / _autonomous_task_label / _build_autonomous_task_prompt / _autonomous_task_run_id_for_chat_message / _clear_current_autonomous_task_state` 这组执行 helper 壳，以及 `_poll_autonomous_workflow / _interrupt_current_autonomous_task / _report_current_autonomous_task_timeout_if_needed / _post_autonomous_task_decision / _submit_body_improvement_report` 这组 runtime 协作壳都已移除；scene 上报主函数已下沉到 `autonomous_presence`，prompt/kind/run-id 判定与 poll/interrupt/timeout 则回到 `autonomous_executor`。命令分发、空闲刷新、事件记录与 TUI 面板容器开始直接调模块函数。主类仍承担 `/auto` 门控、状态栏读取和少量自主链路协作入口 | 继续按“用户链路入口 / 自主链路桥接壳”重组主类，把剩余 `/auto` 协作入口和状态读取进一步下沉，避免主 CLI 再吸回自主执行职责 |
| P1 | Web 小屋主视角已基本收口，但旧兼容壳与“队列管理台”心智还没彻底拔干净 | 这一轮已进一步去掉前台对“临时门控 / 驱动可用”的突出展示，hero pill、观测说明、抽屉底部辅助信息与 stats 面板都收回到“边界说明 / 观测快照 / 当前焦点”口径；前端也开始优先消费 `runtime.user_chain_signal` 正式分支，不再围绕 `activity_guards` 自己拼主视图；同时 Web 小屋内部链路排序也不再按 `scheduled_for` 这类旧排队槽位组织，而是按闭环阶段优先级组织观测投影，连 board 里按“创造类 backlog”做的本地二次推断也已退回正式 `api_b_backlog / api_a_running / api_a_ready` 阶段计数，分段 copy 也开始由后端 `display_copy` 直接投影给前端。但仍有少量长尾 helper、专题 gap 文档和说明段会把 Web 小屋重新读成旧控制台或旧队列台 | 下一轮直接按“API-B 观测投影”重设计剩余 Web helper：删掉不再需要的旧兼容读取、本地二次排队/二次解释逻辑，只保留 `board / chain / loop / runtime.user_chain_signal` 这组正式读模型 |
| P1 | Web 小屋的链路分段虽然已经不是旧任务台，但“待认领窗口 / 执行中阶段 / 回流写回”三段边界还需要更强护栏 | `api_a_ready` 已只保留放行待认领项；运行中任务只留在 `loop.stages.api_a_execution` 与事件/轨迹；`queued` 也已从 Web/trace 主展示枚举退回到统一归一化入口，其中 Python 侧吸收已下沉到共享 `observation_status.py`，前端侧也已停止直接吸收 `queued`，只再通过 `observationDisplayStatus()` 单点消费后端投影；同时 phase1 长尾注释与步骤说明也已不再把 API-A 执行态混回 API-B 治理段。剩余问题主要集中在少量 helper 命名与专题文档 | 继续按“待认领窗口 / 执行中阶段 / Mem 回流”重命名剩余 helper、测试和专题文档，并评估是否还能继续缩掉前端通用状态 helper，避免以后再次把 API-A 运行中状态并回待认领段 |
| P1 | 自主判断层虽然已开始按 `API-B backlog / API-A execution lane` 分开计数，但还没有把这组边界彻底推到所有历史 helper 与专题文档 | 这一轮内生驱动已不再把 API-A `approved/running/retry` 项误算成治理拥堵，`governance_backlog_count / stale_backlog_count / pending_review_count` 口径已经回到 API-B 治理段；同时学习/替身改进去重仍保留整链 live task 视角，避免重复候选反弹。剩余问题主要集中在少量历史 helper、专题 gap 文档和部分长尾统计标题 | 继续把剩余 `queue/dispatched` 结果键、专题 gap 文档和测试命名统一到 `backlog / handoff / writeback`，并评估是否要把 API-A lane 计数显式纳入认知快照，进一步提升 Supervisor 对“待认领 / 执行中 / 治理在途”三段的判断分辨率 |
| P1 | planning/runtime 协议层虽然已开始转向 `drive_input`，但外部兼容位还没有完全切干净 | 当前 `evaluate_endogenous_drive / cycle / decide / review` 已并行带出 `drive_input`，测试也开始断言新字段；这一轮 `review_autonomous_chain_tasks()` 已不再在 family 解析里回退读取 `activity_guards`，但 LM review、少量历史接口与长尾测试里仍保留兼容镜像读取，避免一次性扯乱现网协议 | 继续按“先并行、后切主”的节奏，把 runtime / review / trace / 测试里的正式消费位逐步改读 `drive_input`，等下游都稳定后再删除 `activity_guards` 协议壳 |
| P1 | API-A 子执行面已经回到 pull/claim 语义，但本地运行时和少量测试命名仍能看出“启动提示器”一类实现味 | 这一轮已把 executor route/status 与迷你 CLI 的 `enqueue/prompt_queue` 主语继续收口成“自主执行已起跑 / 首个执行回合 / 等待回写”这组链路阶段说明；但少量函数名、历史测试名和运行时注释仍会暴露实现机制 | 继续把迷你 CLI runtime、gateway activity 和剩余测试名统一到 `claim / execution start / writeback` 语义，进一步压低实现机制存在感 |
| P2 | CLI 自主执行面仍保留一部分会话本地诊断文案 | 这一轮已把本地阶段码继续收回链路语义：`waiting_api_a_claim / running_on_other_api_a / local_claimed_waiting_first_turn / local_claimed_waiting_writeback / local_claimed_active` 已比旧 `approved_waiting_claim / claimed_* / running_elsewhere` 更贴近“待认领 / 首回合 / 写回 / 他处在途”；但它们仍依赖本地 prompt 注入与回合时序 | 评估 gateway / supervisor 是否需要补更细执行事件读模型，或明确把它们永久定义为 CLI 本地诊断 |
| P2 | 仍有少数活动文档和专题 gap 文档保留较长历史段 | 主报告已拆出迁移日志，但其他专题文档搜索时仍可能命中 `presentation / queue / watch_groups` 等退场术语 | 按主题继续把长历史段迁到单独日志或附录，只保留当前基线判断 |

## 8. 历史别名集中说明

以下术语只作为迁移历史保留，不代表当前主协议。后续新会话、实现和文档应优先按右侧现行主语理解：

| 历史别名 | 当前主语 | 说明 |
| --- | --- | --- |
| `task_queue` | `autonomous_chain_store` | 底层治理存储已完成破坏性改名；UI / runtime / trace 不应再把旧名当主语 |
| `idle-window` | `activity_guards` / `user_chain_signal` | 用户状态只作为软感知信号；执行门控不再按“等用户空闲”组织 |
| `watch_groups` | `autonomous_observation.chain.segments` | Web 前端与 CLI/dashboard 均已不再消费旧分组别名 |
| `queue.sections` / `queue.headline` | `chain.segments` / `chain.headline` | Web 小屋和 dashboard 已完成破坏性改名，不再把自主链路展示理解成队列管理 |
| `queue_preview` / `queue_headline` | `chain.segments` / `chain.headline` | 旧缩略镜像已删除；展示层直接围绕真实链路分段输出 |
| `board.current_cards` | `loop.stages` + `board.primary_focus` | Web 小屋已不再维护“当前卡片板”投影；闭环当前态直接来自链路阶段本体 |
| `api_a_note` | `observation_notes` / `loop.stages.api_a_execution` | API-A 阶段解释已回到链路阶段本体 |
| `presentation` | `board / chain / loop / runtime / counts` | 阶段性展示镜像层已删除；现由消费端直接基于主读模型推导展示 |
| `/self-evolution/*` 对外任务路由 | `/autonomous-chain/*` | 当前外部治理与单次触发入口已统一收口到 autonomous-chain |
