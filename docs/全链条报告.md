# 全链条报告：自主链路与用户链路边界审查

更新日期：2026-07-06

> 本报告只保留当前有效基线、当前问题清单和历史别名对照；不再把长迁移历史留在主文，避免退场术语反向污染后续判断。

## 1. 审查基准

本报告按最新《voidcube架构基线》审查全执行链路。当前最高优先级语义如下：

- **自主链路**：API-B 内生驱动自主感知、判断并生成治理投影/任务；Web 小屋只展示 API-B 的状态、反馈、任务治理和自主任务回报；API-A 自主执行面（当前仍由 `/auto` 临时门控的最小迷你 CLI / 子代理通道承载）拉取并执行 `self_learning` 与 `body_improvement`；成果写入 Mem；下一轮 API-B 再读取并继续判断。
- **用户链路**：用户通过主 CLI 调用 API-A 主 Agent，走 `user_chat` 泳道，服务当前用户请求。
- **硬边界**：自主链路与用户链路必须互不抢占、互不覆盖、互不被解释成同一个 CLI 模式。`/auto` 与 `/auto-q` 只是当前自主链路的临时启停开关，不是主 CLI 的用户交互模式。Web 小屋不是用户聊天入口，只是 API-B 主视角的观测面。

## 2. 当前结论

当前设计方向基本成立，且本轮已经完成以下关键收口：

1. **双链路主语已固定**：架构讨论优先使用“自主链路 / 用户链路”，不再把 `/auto` 误写成主 CLI 模式。
2. **执行者边界已固定**：任务生产者归 API-B / Supervisor；任务执行者归 API-A 自主执行面；成果回流归 Mem。
3. **Web 小屋职责已固定**：Web 小屋只观察 API-B 判断、治理反馈、Mem 回流，以及 API-A 回报给 API-B 的自主任务状态。
4. **主读模型已固定**：`autonomous_observation` 当前有效主树为 `board / chain / loop / runtime / counts`。
5. **链路分段已固定**：Web / dashboard / CLI 自主观察 helper 统一围绕 `autonomous_observation.chain.segments`，顺序为 `api_b_candidates -> api_b_backlog -> api_a_ready -> mem_recent`。
6. **创造类任务归属已固定**：未被 API-B 放行的 `self_learning / body_improvement` 仍属于 `api_b_backlog` 治理段，不会因为类型属于 API-A 能力域就自动出现在 `api_a_ready`。
7. **API-A 待拉取窗口语义已进一步收口**：`api_a_ready` 现在只承载真正“已放行、待 API-A 拉取”的链路项；已经进入 `running` 的 API-A 任务只留在 `loop.stages.api_a_execution` 与最近事件/轨迹里观察，不再混回待拉取窗口。

## 3. 当前链路对齐

### 3.1 自主链路

```text
API-B / Supervisor 内生驱动
  → 自主感知用户、系统、记忆、身体、治理状态
  → 形成 judgement / need / intent / governance output
  → 生成候选并管理治理在途项
  → API-A 自主执行面 pull 已放行任务
  → 执行 self_learning / body_improvement
  → 将结果、替身改进报告、任务 outcome 写入 Mem
  → 下一轮 API-B 读取 Mem 与任务历史继续判断
```

符合点：

- 任务生产归 API-B。
- 任务执行归 API-A 自主执行面。
- Mem 是结果回流位，不是执行槽。
- Web 小屋只展示这条闭环的 API-B 主视角观测。

### 3.2 用户链路

```text
用户
  → 主 CLI
  → API-A 主 Agent (`user_chat`)
  → 工具 / 临时上下文 / 必要记忆访问
  → 主 CLI 输出
```

符合点：

- 用户链路不依赖自主链路治理存储。
- 用户主 CLI 不承担自主任务执行槽。
- `/auto` 开关不得阻断用户输入或主 Agent 交互。

## 4. 当前协议与实现对齐点

- `read_model_version=8`。
- Web / 最小 dashboard / 主 CLI 自主观察 helper 统一消费 `autonomous_observation.chain.segments`。
- `loop.stages.api_a_execution` 直接承载 `status_label / chain_reason / activity_text / reason_style / focus_task`。
- `api_a_ready` 只统计 `approved / retry` 的放行待拉取项；`running` 任务属于执行中阶段，不再算作“待拉取窗口”。
- `board` 只保留 `headline / summary / primary_focus`；Web 小屋不再把 `board.current_cards` 作为主投影层。
- `board.protocol_notes`、segment `read_rule / next_step` 与 stage `read_rule / transition_hint` 已加入读模型，Web 小屋开始直接消费这些语义提示，而不是继续把旧技术字段硬拼成“任务台解释”。
- 主 CLI 自主执行面、监督事件镜像、最近执行摘要与 `/auto` 入口提示，已统一回到“链路项 / 链路裁决 / 执行焦点 / 自主链路”语义。
- Web 小屋已删除旧 `autonomous_observation.queue`、顶层 `api_b/api_a/mem/reread` 兼容镜像，以及 `presentation` 展示镜像层。
- Web 小屋这一轮继续去掉旧“队列台 / dispatch / LM 观察 / Perception → Intent”展示味：面板主语改回“API-B 判断输入 / API-B 当前判断 / 自主链路分段观察”，用户链路信号只再作为“会话数 / 让路参考阈值”一类软感知说明出现。
- `ui_runtime` 内部围绕 API-A 段的旧 `dispatch` 命名也已继续收口到“execution lane / 待拉取窗口”语义，避免后续会话把自主链路观测层重新理解成旧分发队列。
- 内生驱动候选载荷入口与 backlog 计数主语已继续收口：候选载荷入口已统一到 `to_backlog_item()`，高频 backlog 压力/恢复/计数字段也已回到 `backlog` 语义，减少旧“总队列管理器”心智继续反向污染 Web 小屋与自主链路。
- 执行服务对外入口已进一步收口到自主链路主语：executor 侧正式执行入口改为 `/executor/autonomous-chain/execute`，返回状态改为 `autonomous_chain_execution_executed / autonomous_chain_execution_recorded`，不再继续把执行面叫成 `formal_self_evolution`。
- 最小迷你 CLI 中 API-A 子执行面的本地事件主语也已继续收口：`enqueue/prompt_queue` 一类命名已改成“执行提示注入 / execution prompt injected”，避免把 API-A pull/claim 执行面重新理解成旧本地消息队列。
- 监督者 scene 与循环摘要这一轮进一步统一为 `handoff / handed_off`，CLI、gateway、dashboard、Web 小屋不再同时混用“执行交接”文案与 `dispatch/dispatched` 旧键名。
- supervisor runtime / config 中最后一组还在充当正式主语的 `self_evolution_review_*` 已完成破坏性收口：现统一为 `autonomous_chain_review_*`，Web 调度视图同步读 `autonomous_chain_review_interval_seconds`，环境变量也切到 `SUPERVISOR_AUTONOMOUS_CHAIN_REVIEW_INTERVAL`。
- self-learning 结论回推 Supervisor 的入口已从 `submit_to_supervisor_queue()` 收口到 `submit_to_supervisor_backlog()`，避免把“学习结论转治理任务”继续误读成旧总队列写入。
- gateway / ops dashboard 这一轮继续削掉一层旧兼容：dashboard 对 API-A 观测现在只读取 `supervisor_task` 泳道，不再回退到顶层 agent 场景；gateway 活动状态恢复时也不再迁就旧 `self_evolution_*` 持久化键。
- Web 小屋抽屉与回合细节这一轮继续去掉“trace/source/execution family”实现味：前端现优先展示“回合 / 记录侧 / 治理动作 / 执行动作”语义，后端 trace 聚合也补上了对应的人类可读标签，房间舞台与动画骨架保持不变。
- `governance_preview` 这一轮也不再只是原始上下文字段透传：Supervisor 序列化阶段已补上 `summary / action_label / priority_label / merge_into_title` 等可读投影，Web 卡片与抽屉直接消费这些监督者说明，不再自行硬猜 shadow review 结构。
- Web 小屋的认知/溯源抽屉这一轮继续从“原始 cognition 快照直出”改成稳定投影协议：UI state 已新增 `cognition.judgement / cognition.uncertainty` 两层，先把 `dominant_constraint / why_not_improvement_now / recommended_probe / observation_target` 等内部字段折成 Supervisor 可解释中文语义，再交给前端展示。
- `renderProvenanceDrawer` 与认知面板也因此不再把不确定性账本当原始诊断对象硬摊开：现在主语回到“当前判断 / 为什么暂不直接改替身 / 当前最该补哪侧证据 / 建议探针是什么”，候选卡片同时补齐“候选类型 / 信号来源 / 学习分支 / 浮出原因”。
- Web 小屋的面板标题与空状态文案这一轮也继续从实现视角收口：`LM 输入 / 认知状态 · 感知到意图 / Prompt 预览` 已改成 `API-B 判断输入 / API-B 当前判断 / 判断输入预览`，同时把 `LLM 健康` 一类外显文案收薄为更中性的“判断模型健康 / 记忆模型健康”。
- 测试与专题 gap 文档这一轮也继续清掉一批容易误导新会话的旧主语：`queue item / task queue / dispatch` 一类测试标题、skip reason 与专题说明，已收回 `backlog / execution handoff / 自主链路分段投影` 语义。
- supervisor runtime 这一轮也继续把正式配置与测试名往当前主语收口：`self_evolution_handoff_limit_per_cycle` 已改成 `autonomous_chain_handoff_limit_per_cycle`，相关 review-cycle 测试名和结论回推测试名也不再沿用 `self_evolution_cycle / into_queue` 旧说法。
- 同一轮里，planning runtime 的 cycle lock 内部属性也已改成 `autonomous_chain_cycle_lock`，dashboard 的 `handoff` 场景外显文案收成“自主交接”，继续减少代码搜索结果与状态栏里的旧 `self_evolution / 执行交接` 回声。
- gateway activity 这一轮开始补“可读标签层”而不是只暴露 canonical runtime 值：`recent_metadata` 与 `task_identity` 现在会附带 `*_label / display_label / label_texts`，把 `self_evolution / body_switch / body_improvement / memory_maintenance` 等运行时值同步投影成人可读的“自主改进 / 身体切换 / 替身改进 / 记忆维护”语义，便于 dashboard、日志和排障视角直接站在自主链路主语上。
- 最小 ops dashboard 这一轮也开始真正消费这层 label：在保持房间骨架不变的前提下，dashboard 现会从 gateway `recent_metadata` 推导“最近自主动作”摘要，并把原先 `API-B 24x7 self-governance` 一类英文旧说法收口成“API-B 持续运行自主链路；用户链路只作为软感知信号”。
- Web 小屋前端的长尾动作标签也继续和 gateway label 层对齐：卡片类型与治理/执行动作提示现在优先使用 `task_identity.display_label`，`body_switch` 外显语义收回“身体切换”，`self_evolution` 则回到“自主改进”，减少前后端同一动作双翻译的情况。
- 主 CLI 这一轮继续收薄：`cli.py` 不再通过一组逐个转发的 runtime host wrapper 调自主执行逻辑，而是统一通过单个 autonomous runtime 实例桥接；`autonomous_runtime_host.py` 也已清掉那组重复转发函数。随后又去掉了一层状态格式化薄壳，测试直接回到 observation helper；`/auto` 激活时用到的 supervisor 快照获取与格式化也已下沉回 gate/status host。进一步地，`/auto-q / fast-exit / force-quit` 的退出路径也开始直接调用 host 上既有方法，不再由 `cli.py` 手工传一长串回调，本地 CLI 主类进一步回到“用户链路入口 + 薄桥接壳”。
- 主 CLI 已把自主观察 helper、panel 壳层、`/auto` gate、presence、events、runtime host、status host 下沉为独立模块；监督者快照摘要 wrapper 与 panel 侧状态读取 wrapper 已继续删除，autonomous panel 直接读取 status host 缓存，`/auto` gate 改成显式 callback 协作 runtime/status host，不再静默依赖已删除的 host 方法，`cli.py` 进一步收口为“用户链路入口 + 自主链路薄桥接壳”。

## 5. 当前架构判断

按最新语义，VoidCube 应被理解为“双链路共享底座”：

- 用户链路保证用户当前交互稳定、直接、优先。
- 自主链路负责 API-B 内生判断、API-A 自主学习/改进、Mem 闭环和 Web 可观测。
- 两者共享 API-A 能力域和 Mem，但必须通过 `user_chat / supervisor_task`、主 CLI / 最小迷你 CLI、用户输出 / API-B Web 观测这三层边界隔离。

因此，当前设计在目标方向上是成立的。当前主要风险已经不再是“缺少组件”，而是某些残留命名、残留文案或旧活动文档仍可能把自主链路误读成“主 CLI 的另一种运行模式”。

## 6. 迁移历史入口

2026-07-03 到 2026-07-06 的连续收口过程，已经拆出到 [全链条迁移日志.md](./全链条迁移日志.md)。

如果需要追溯 `queue / watch_groups / presentation / self-evolution / auto-mode` 这些术语是如何退场的，请查看迁移日志；主报告不再保留长历史段。

## 7. 当前剩余全链路问题清单

以下问题仍然存在，但边界已经比较清楚，适合下一轮继续破坏性收口：

| 优先级 | 问题 | 当前状态 | 建议下一步 |
| --- | --- | --- | --- |
| P1 | 主 CLI 内仍保留自主链路桥接壳，但已继续变薄 | 观察 helper、panel、gate、presence、events、status host 已独立模块化；监督者快照摘要 wrapper、panel 侧状态读取 wrapper、gate 对旧 host 方法的隐式依赖已删除；这一轮 `cli.py` 已改成统一通过单个 autonomous runtime 实例桥接，runtime host 里的逐个转发函数已清掉，但主类仍承担 `/auto` 门控、状态栏读取和少量自主链路协作入口 | 继续按“用户链路入口 / 自主链路桥接壳”重组主类，把剩余 `/auto` 协作入口和状态读取进一步下沉，避免主 CLI 再吸回自主执行职责 |
| P1 | Web 小屋虽然已经切回 API-B 主视角，但更深层专题诊断和少数 preview 子字段仍有零散实现味 | 这一轮已把抽屉里的 `trace/source/execution family` 主语收成人类可读的“回合 / 记录侧 / 治理动作 / 执行动作”，把 `governance_preview` 收口成 Supervisor 可读摘要，并新增 `cognition.judgement / cognition.uncertainty` 投影承接“当前判断 / 暂不直接改替身 / 建议探针”；dashboard 也开始直接消费 gateway 的 `display_label` / `task_identity` 做“最近自主动作”摘要。剩余问题主要集中在长尾未映射枚举、少数英文 fallback 和专题 gap 文档 | 继续把候选专题诊断、长尾 preview 子字段、gateway activity 长尾摘要和剩余 fallback 英文句式统一到稳定的人类可读展示字典 |
| P1 | Web 小屋的链路分段虽然已经不是旧任务台，但“待拉取窗口 / 执行中阶段 / 回流写回”三段边界还需要更强护栏 | `api_a_ready` 已只保留放行待拉取项；运行中任务只留在 `loop.stages.api_a_execution` 与事件/轨迹；store 层已开始把 `execution_dispatch` 收口为 `api_a_execution_lane`，但 planning / 更大范围测试与专题文档仍有历史术语残留 | 继续按“待拉取窗口 / 执行中阶段 / Mem 回流”重命名剩余 helper、测试和专题文档，避免以后再次把 API-A 运行中状态并回待拉取段 |
| P1 | Web 小屋虽然已脱离旧任务板，但底层治理存储的读取协议还需要从“总队列”思维彻底转向三投影读模型 | 当前 `governance_backlog / api_a_execution_lane / writeback_history` 已开始成为主投影，且这一轮已把 review loop、结论回推入口、planning 摘要和部分 gateway/dashboard 旧兼容一起收回 `backlog / autonomous_chain` 语义；但专题文档、长尾测试和部分历史标题仍残留少量 queue/dispatch 主语 | 继续把剩余 `queue/dispatched` 结果键、专题 gap 文档和测试命名统一到 `backlog / handoff / writeback`，彻底去掉旧队列管理心智 |
| P1 | API-A 子执行面已经回到 pull/claim 语义，但本地运行时和少量测试命名仍能看出“前台提示注入器”的实现味 | 这一轮已把 executor route/status 与迷你 CLI 的 `enqueue/prompt_queue` 主语收口为 `autonomous_chain_execution / execution_prompt_injected`；但少量函数名、历史测试名和运行时注释仍会暴露实现机制 | 继续把迷你 CLI runtime、gateway activity 和剩余测试名统一到 `claim / execution prompt / writeback` 语义，进一步压低“本地提示注入器”存在感 |
| P2 | CLI 自主执行面仍保留一部分会话本地诊断文案 | `approved_waiting_claim / running_elsewhere` 已回到 `loop.stages.api_a_execution`；`claimed_waiting_start / claimed_waiting_writeback / claimed_running` 仍依赖本地 prompt 注入与回合时序 | 评估 gateway / supervisor 是否需要补更细执行事件读模型，或明确把它们永久定义为 CLI 本地诊断 |
| P2 | 仍有少数活动文档和专题 gap 文档保留较长历史段 | 主报告已拆出迁移日志，但其他专题文档搜索时仍可能命中 `presentation / queue / watch_groups` 等退场术语 | 按主题继续把长历史段迁到单独日志或附录，只保留当前基线判断 |

## 8. 历史别名集中说明

以下术语只作为迁移历史保留，不代表当前主协议。后续新会话、实现和文档应优先按右侧现行主语理解：

| 历史别名 | 当前主语 | 说明 |
| --- | --- | --- |
| `task_queue` | `autonomous_chain_store` | 底层治理存储已完成破坏性改名；UI / runtime / trace 不应再把旧名当主语 |
| `idle-window` | `activity_guards` / `user_chain_signal` | 用户状态只作为软感知信号；执行门控不再按“等用户空闲”组织 |
| `watch_groups` | `autonomous_observation.chain.segments` | Web 前端与 CLI/dashboard 均已不再消费旧分组别名 |
| `queue.sections` / `queue.headline` | `chain.segments` / `chain.headline` | Web 小屋和 dashboard 已完成破坏性改名，不再把自主链路展示理解成队列管理 |
| `queue_preview` / `queue_headline` | `chain.segments` / `chain.headline` | 旧缩略镜像已删除；展示层直接围绕真实链路分段输出 |
| `board.current_cards` | `loop.stages` + `board.primary_focus` | Web 小屋已不再维护“当前卡片板”投影；闭环当前态直接来自链路阶段本体 |
| `api_a_note` | `observation_notes` / `loop.stages.api_a_execution` | API-A 阶段解释已回到链路阶段本体 |
| `presentation` | `board / chain / loop / runtime / counts` | 阶段性展示镜像层已删除；现由消费端直接基于主读模型推导展示 |
| `/self-evolution/*` 对外任务路由 | `/autonomous-chain/*` | 当前外部治理与单次触发入口已统一收口到 autonomous-chain |
