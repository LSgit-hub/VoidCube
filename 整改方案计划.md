# SQLite 与 Gateway 整改方案计划

## 1. 结论

本次整改不新建一个默认覆盖所有数据库的“大一统 Data Service”，也不继续把 Gateway 当本地模块组合总线。规范架构已补在 [ARCHITECTURE.md](ARCHITECTURE.md)：本地高频数据面直接调用 owner service；Gateway 只保留控制面、管理面和外部入口。

第一实施目标是长期记忆链路：

```text
Agent / Supervisor / Companion
        -> MemoryClient
        -> Memory Service
        -> memory.db
```

`memory.db` 是长期记忆唯一规范数据库，Memory Service 是它唯一 owner。UI、Agent、子代理和 Supervisor 不碰 SQLite 文件，不通过 Gateway 访问本地 Memory CRUD，也不能失败后回退直连数据库。

## 0. 当前实施进度

更新时间：2026-08-26。

| 阶段 | 状态 | 当前证据 |
|---|---|---|
| 阶段 0：规范和清单 | 已完成 | `ARCHITECTURE.md`、`docs/SQLite与Gateway整改基线.md` 已补齐，旧架构文档已同步控制面/数据面边界 |
| 阶段 1：MemoryClient 与本地协议 | 已完成 | `src/voidcube/infrastructure/memory/client.py` 提供同步/异步 `MemoryClient`、固定身份、request id、幂等键、deadline/timeout、错误封装；Memory Service 支持本地 service token 和 actor/header/body 一致性校验 |
| 阶段 2：迁移本地 Memory 数据路径 | 已完成 | Agent `MemMemoryProvider`、Supervisor companion recall/outbox/turn-pairs、Auto gate rules-status、memory maintenance execution、UI identity/promotion、autonomous promotion proposal 均已改为 `MemoryClient -> Memory Service`；生产代码扫描 `src plugins Mem` 已无 `/api/mem` 热路径引用 |
| 阶段 3：Memory Service 单写者整改 | 已完成 | `MemoryRepository` 已提供有界单写者线程、批量事务、savepoint 隔离、`BEGIN IMMEDIATE`、WAL/busy retry 和 backpressure；高频 session/turn/remember/recall/promotion/identity 路径已迁移，`forget_memory`、session/day summary、LLM cache 和压缩 pipeline 已收进 repository；服务端读取缓存按 `commit_revision` 校验并在写后失效；`memory_write_receipts` 已在同一写事务持久化操作、fingerprint、原始响应和 commit revision，`add_turn_pair` 及可选幂等键的 `remember` 已支持重放与冲突检测；客户端 outbox 继续承担跨进程 durable/replay；并发读写、busy/retry、背压、缓存 revision、写后可见性、服务重启和 outbox crash window 联合验收已通过 |
| 阶段 4：Gateway 去中心化和兼容路由退役 | 已完成 | `src/voidcube/infrastructure/gateway/internal_gateway.py` 已对 `/api/mem/*` 返回 410 并停止作为本地 memory 数据总线；`tests/test_gateway_activity.py`、`tests/test_live_executor_chain.py`、`scripts/smoke_memory_outbox.py` 已改为验证退役态或直接调用 Memory Service / MemoryClient |
| 阶段 5：其他 SQLite 域逐步整改 | 已完成 | 每个 SQLite 域 owner 已明确并落实（见下方阶段 5 收尾清单）：`state.db`/`actions.db`/`scheduled_tasks.db`/`scheduled_writebacks.db`/`registry.db`/`.skills_registry.sqlite3` 均持文件级 `SQLiteOwnerLease`，memory outbox 显式声明为行级 lease 传输队列；专项并发/启动恢复/真实库测试已入库；Skill Registry 固定自检 14 项通过，`added/reparsed/reused/removed` 统计符合预期；阶段门禁 40 项通过，架构扫描和配置同步检查通过 |

阶段 2 已完成迁移的调用点：

- `src/voidcube/systems/supervisor/autonomous_task_memory_promotion_service.py` 的 `/api/mem/remember`、`/api/mem/promotion-candidates`。
- `src/voidcube/systems/execution/adapters.py` 的 `/api/mem/compressed/run-all-rules`。
- `src/voidcube/systems/supervisor/ui_identity_proxy_adapters.py` 的 identity、turns、promotions、promotion-candidates、consent 代理。
- `plugins/memory/mem/__init__.py` 的 Agent recall/remember/Tier1 turn 入口。

阶段 4 现状：

- Gateway 的 memory 兼容代理已经退役，不再作为本地核心数据通路。
- 现存 `/api/mem` 相关引用仅用于 410 退役断言或直连 Memory Service 的验证，不再代表主链路。

本轮阶段 3 收尾：

- 修复 identity experience 与 recall access touch 在 repository callback 内自行 `commit/rollback` 导致 savepoint 被破坏的问题；提交和回滚现在统一由 repository 所有。
- Gateway 注册改为后台控制面任务，不再阻塞 Memory Service 本地数据面启动。
- Memory health 主状态改为反映本地数据面；Gateway 注册状态单独报告，Gateway 不可用不再令本地 Memory CRUD 失效。
- 项目虚拟环境已通过 138 项 Memory/协议/边界/资源回归、68 项集成/打包/Gateway/生命周期回归，以及真实 outbox 多进程 smoke。

## 2. 已确认的项目设计

VoidCube 是单机、单所有者的本地应用：

- API-A Agent 负责用户 CLI 对话。
- Supervisor 负责治理、任务状态和 UI 投影。
- Memory/MemAI 负责长期记忆、三域权限和治理事件。
- API-B 负责后台规划/复核；获准任务由隔离员工代理执行。
- 星子不是第二个独立 Agent 进程，而是 Supervisor、Memory/MemAI 和 API-B 的组合能力。

长期记忆有三个域：

- `agent_interaction`：API-A 会话、工具结果和任务事实。
- `companion`：日常伴侣语义、目标理解和偏好。
- `evolution`：Auto 计划、实验、治理和身体 lineage。

跨域传播必须通过可审计提升引用，不复制原始对话正文。

## 3. 当前问题

### 3.1 Gateway 被滥用为本地数据总线

当前 Gateway 同时承担服务发现、HTTP 路由、状态聚合、session credential、service credential、Memory actor/scope 注入和 `/api/{path:path}` 转发。Agent、Supervisor、Execution adapter、UI proxy 等路径通过 Gateway 调用 `/api/mem/*`。

这对本地应用有三个问题：

- 性能上增加不必要的 HTTP 中转、认证代理和序列化开销。
- 架构上把控制面和数据面混在一起，Gateway 变成模块耦合中心。
- 故障上让本地 Memory 热路径依赖 Gateway 健康；Gateway 故障会放大成记忆不可用。

Gateway 不能直接删除，因为它还承担外部入口、服务生命周期、presence/scene/activity 聚合和迁移期兼容职责。正确做法是把它从本地数据热路径移出。

### 3.2 Memory Service 内部 SQLite 边界不够集中

长期记忆数据库当前是：

```text
VOIDCUBE_HOME/runtime/memory/memory.db
```

Memory Service 已经是长期记忆服务进程，但 `Mem/src/memai` 中 repository 仍保留受控 `connect()` 作为迁移期/测试 fallback；Memory Service 热路径的写事务已统一经过 repository 写队列，应用 callback 不再自行提交或回滚。剩余 `db_path` 直连仅限独立维护工具和兼容 fallback，后续可随调用方收敛继续删除。

### 3.3 其他 SQLite 文件不能和长期记忆混淆

项目里还存在 `state.db`、`actions.db`、`scheduled_tasks.db`、registry、outbox 等 SQLite 文件。它们不是长期记忆主体，也不应在第一阶段与 `memory.db` 合并。每个文件都需要唯一 owner，但 owner 可以按业务域拆分。

## 4. 目标架构

### 4.1 本地数据面

```text
API-A Agent ──────┐
Supervisor ───────┼─> MemoryClient ─> Memory Service ─> memory.db
Companion/UI ─────┘
```

要求：

- 本地 Memory CRUD 不经过 Gateway。
- 客户端只使用 `MemoryClient`，不导入 SQLite、不传 SQL、不知道数据库路径。
- Memory Service 自己验证 owner/workspace/domain/actor 能力。
- Memory Service 内部集中处理 CRUD、事务、锁、重试、缓存、校验、迁移和 outbox。

### 4.2 控制面和外部面

```text
外部 HTTP / 管理 UI / health / presence / scene
        -> Gateway
        -> 公开业务接口或控制面聚合
```

Gateway 保留：

- 服务生命周期、注册、健康和管理面；
- presence、scene、activity 聚合；
- 外部 HTTP/API 入口；
- 有调用统计和退役期限的迁移期兼容路由。

Gateway 移除：

- `/api/mem/*` 作为本地核心 Memory 总线；
- 本地 Memory CRUD 的必经认证代理；
- 本地数据面的通用 `/api/{path:path}` 中转依赖；
- 任何业务数据库连接、事务、缓存和锁重试职责。

## 5. 身份与权限迁移

不能只把 URL 从 Gateway 改成 Memory Service。当前 Gateway 还做 session credential、service credential、Memory actor 授权和 owner/workspace scope 注入，直连后这些职责必须落到类型化客户端和 Memory Service。

目标契约：

- VoidCube 是单机单 owner 系统，不建立多用户登录模型。
- `MemoryClient` 使用固定调用角色/能力，不让调用方任意声明 `memory_actor`。
- Memory Service 校验本地调用方身份、允许的 actor、`memory_domain`、owner/workspace scope。
- 优先使用 Windows Named Pipe 或 Unix Domain Socket；迁移期可使用 loopback HTTP + 本地服务 token。
- owner/workspace 绑定由本地会话上下文和 Memory Service 策略管理，不再由通用 Gateway 临时注入。
- 不允许以环境变量、文件路径或 debug 参数绕过 Memory Service 直接打开 `memory.db`。

## 6. Memory Service 内部整改

Memory Service 内部目标结构：

```text
Memory API handler
  -> request envelope / identity / domain validation
  -> application command
  -> read executor or write queue
  -> SQLite transaction boundary
  -> cache invalidation / revision / metrics
```

写入规则：

- 写事务由单写者队列串行化。
- 使用短事务，事务内禁止模型调用、网络请求、embedding 或长计算。
- 使用 WAL、`BEGIN IMMEDIATE`、busy timeout、有限指数退避和稳定错误码。
- 高频记忆按 turn 或业务批次提交，不按 token/字段碎片化提交。
- 每次写入带 `request_id`、`idempotency_key`、来源会话、domain、priority 和 schema version。
- 成功写入后统一推进 revision、缓存失效和审计事件。

确认语义：

- `accepted`：进入内存队列，不能作为持久化保证。
- `durable`：已进入 durable outbox。
- `committed`：已提交到 `memory.db`。

用户明确要求保存的事实、任务状态和治理记录必须等待 `committed`；普通对话记忆可以按策略使用 `durable`。

读取规则：

- `recall/get_context` 由 Memory Service 裁剪结果、执行权限过滤并使用服务端缓存。
- 支持 `min_revision` 或返回 commit revision，避免刚写入的关键事实下一轮读不到。
- FTS、向量检索、压缩和提升逻辑不能让客户端直接访问数据库。

## 7. 现有 SQLite 文件边界

| 数据库/模式 | 业务域 | 整改原则 |
|---|---|---|
| `memory.db` | 长期记忆唯一真相 | Memory Service 唯一 owner，第一优先级整改 |
| `state.db` | 会话、transcript、搜索投影 | 后续独立 Session owner，不与 memory 合并启动 |
| `actions.db` | 动作审计日志 | 后续 Action Journal owner |
| `scheduled_tasks.db` | Supervisor 定时任务 | 后续 Scheduled Task owner |
| `scheduled_writebacks.db` | 调度回写传输队列 | 后续归调度/写回 owner |
| `registry.db` | 本地进程注册 | 后续归执行/registry owner |
| `.skills_registry.sqlite3` | 可重建技能索引 | 文件系统仍是内容权威；涉及变更必须跑 Skill Registry 固定自检 |
| `runtime/memory/*-write-outbox.sqlite3` | 迁移期 durable spool | 暂不擅自删除，评估后收敛到 Memory Service 或明确为传输队列 |

“每个 SQLite 文件一个 owner”不是说要多个长期记忆库，而是防止不同业务进程同时打开同一个 SQLite 文件争写。长期记忆只有 `memory.db`。

## 8. 分阶段实施

### 阶段 0：规范和清单

交付物：

- 根目录 `ARCHITECTURE.md`。
- `docs/SQLite与Gateway整改基线.md`。
- 当前 `/api/mem/*` 调用方、SQLite 文件和 owner 清单。
- Gateway 控制面/数据面职责分类。

完成标准：规范不再把 Gateway 作为本地数据总线；不再承诺未经验证的大一统 Data Service。

### 阶段 1：MemoryClient 与本地协议

交付物：

- `MemoryClient` typed interface。
- 请求 envelope：版本、request_id、deadline、idempotency_key、调用角色、owner/workspace/domain。
- 稳定错误码：`validation_error`、`unauthorized`、`not_found`、`conflict`、`busy`、`deadline_exceeded`、`service_unavailable`、`internal_error`。
- 本地身份方案：Named Pipe/UDS 优先，迁移期 loopback HTTP + local service token。

完成标准：Agent/Supervisor 可以通过 MemoryClient 调用 Memory Service 测试替身，不依赖 Gateway 注入 actor/scope。

### 阶段 2：迁移本地 Memory 数据路径

迁移对象：

- `plugins/memory/mem/__init__.py` 中 Agent 侧 memory provider。
- `src/voidcube/systems/supervisor/service_runtime.py`。
- `src/voidcube/systems/supervisor/planning_runtime.py`。
- `src/voidcube/systems/supervisor/autonomous_task_memory_promotion_service.py`。
- `src/voidcube/systems/execution/adapters.py`。
- `src/voidcube/systems/supervisor/ui_identity_proxy_adapters.py`。

完成标准：本地 Agent/Supervisor/Companion 的 Memory recall/remember 不走 Gateway `/api/mem/*`；Gateway 兼容路由仍可保留但只用于外部或迁移期调用统计。

### 阶段 3：Memory Service 单写者整改

迁移对象：

- `Mem/src/memai/repository/contracts.py`。
- `Mem/src/memai/repository/sqlite_repository.py`。
- `Mem/src/memai/repository/sqlite.py`。
- `Mem/src/memai/application/memory_service.py` 中所有 `_repository.connect()` 热路径。
- `plugins/memory/mem/outbox.py` 的 durable spool 恢复策略。

完成标准：Memory Service 内部应用层不再随意持有 `sqlite3.Connection`；高频写入经过单写者队列和批量事务；客户端不看到裸 `sqlite3.OperationalError: database is locked`。

当前阶段 3 进展：

- `SQLiteMemoryRepository.execute_read/execute_write` 统一创建和关闭连接；写入使用 `BEGIN IMMEDIATE`、`threading.Lock` 和有限指数退避，并暴露执行统计。
- `execute_read_async/execute_write_async` 使用 `asyncio.to_thread`，避免 busy 重试和同步 SQLite 操作阻塞 Memory Service 事件循环；identity experience 周期已使用异步写边界。
- `memory_service.py` 的 session、turn、promotion、recall trace、remember、压缩记忆查询、identity archive、tier1 stats、timeline、purge、`forget_memory`、session/day summary 持久化等路径已改用 repository helper；`_apply_compression_lifecycle` 已改为两阶段：候选读取与 LLM 评估在写锁外完成，最终写回由 repository 托管。
- LLM cache 已提供 repository-backed helper；Memory Service 的升级摘要、删除复核、session/day summary 和压缩 pipeline，以及 `Tier1ToTier2Bridge` 的 LLM-first pipeline 都优先通过注入的 repository 访问 cache。旧 `db_path` helper 仅作为迁移期/单元测试 fallback。
- `memory_runtime_state` 现在记录单调 `commit_revision`，并通过 Memory Service health / usage 暴露；repository 写事务成功后同步推进该 revision，备份也已收口到导出边界。
- repository 已提供有界单写者队列、短等待批量提交、savepoint 级单请求隔离、队列深度/最老任务/backpressure/批量提交指标；Memory Service 将队列满转换为稳定的 HTTP 429 `memory_write_backpressure`。
- 服务端单对象读取缓存使用 revision 校验，所有 repository 写事务完成后清空服务缓存；recall 支持 `min_revision`，写响应的关键路径返回 `write_status=committed` 和 `commit_revision`。
- 已补齐服务端持久化 write receipt：回执与业务写入、`commit_revision` 同事务提交；同 key 同 fingerprint 重试返回原响应，冲突 fingerprint 返回稳定 409；服务重启后仍可重放。`add_turn_pair` 默认使用 `write_id`，`remember` 支持显式 `idempotency_key`，无幂等键的旧 `remember` 调用保持原兼容行为。
- 阶段完成：并发压力、进程崩溃窗口、客户端 outbox 重试、backpressure、cache revision、写后可见性和 Gateway 停止后的本地数据面验收均已入库并通过。

### 阶段 4：Gateway 去中心化和兼容路由退役

交付物：

- `/api/mem/*` 已退役，Gateway 不再承担本地 Memory 数据总线职责。
- 本地数据面停用 Gateway 仍可工作。
- 删除已无调用方的 Memory 代理路由和重复身份注入逻辑。

完成标准：Gateway 回到控制面/外部面；没有新的本地核心数据调用经 Gateway。

### 阶段 5：其他 SQLite 域逐步整改

按业务风险独立推进：

1. `state.db` / Session owner。
2. `actions.db` / Action Journal owner。
3. `scheduled_tasks.db` 和 scheduled writeback。
4. Process Registry。
5. Skill Registry。

完成标准：每个数据库文件 owner 明确，客户端只能调用领域 API；不得把其他数据库变成第二个长期记忆库。

阶段 5 收尾清单（本轮已完成）：

- **Session owner（`state.db`）**：`SessionDB` 持 `SQLiteOwnerLease("session-owner")`，`_execute_write`（BEGIN IMMEDIATE + jitter 重试）与 `execution_stats()` 为写并发门禁与健康暴露；owner lease 崩溃恢复、跨进程冲突可见性和 CLI 管理命令释放 owner 已入库（`tests/test_session_owner.py`）。
- **Action Journal owner（`actions.db`）**：`ActionJournal` 全部写路径（prepare/transition/claim_dispatch/recover_abandoned_dispatched）收进 `_execute_write`，关闭后拒绝新写；跨实例并发写/幂等合并测试入库（`tests/test_actions_journal_owner.py`）。
- **Process Registry**：模块级 `process_registry = ProcessRegistry()` 改为带初始化锁的 `ensure_process_registry()` 惰性单例，消除 import 时获取默认 `registry.db` owner lease 的副作用（此前 `-m` 运行子模块会因父包 import 直接崩溃）；全部调用方已同步。
- **ScheduledTaskStore（`scheduled_tasks.db`）**：构造时执行启动恢复，崩溃窗口遗留的 expired claims 自动标记 failed 并清空 `active_run_id`（`tests/test_scheduled_tasks_startup_recovery.py`）。
- **Scheduled writeback**：`SqliteScheduledWritebackOutbox` 真实 SQLite 库测试替换纯 mock（`tests/test_scheduled_writeback.py` 覆盖 enqueue/peek/retry 退避/dead-letter/跨进程 owner 冲突）。
- **Skill Registry（`.skills_registry.sqlite3`）**：owner lease（`skill-registry-owner`）跨进程冲突可见性与并发热刷新一致性入库（`tests/test_skill_registry_owner.py`）；固定自检 `pytest tests/test_skill_registry.py -q` 14 项通过，`added/reparsed/reused/removed` 统计符合预期。
- **MemoryWriteOutbox（传输队列声明）**：不设文件级独占锁，行级 lease 即本域并发门禁；`outbox_state` 持久化 `outbox_owner`（queue_name/worker_id/path），模块 docstring 声明三队列生产拓扑（`tests/test_memory_outbox_owner.py`）。
- **附带修复**：`SQLiteOwnerLease._pid_alive` 在 Windows 上由 `os.kill(pid, 0)`（实为 TerminateProcess，会杀死目标进程）改为 ctypes `OpenProcess` 只读存活探测。
- **生命周期收口**：CLI `sessions` 管理命令统一使用 `try/finally` 释放 `SessionDB` owner，`browse` 的 `execvp` 成功路径在进程替换前显式释放；TUI teardown 显式关闭应用持有的 Session owner；Action Journal 的 transition 审计写入统一使用当前事务连接，避免回调绕过事务边界。

## 9. 测试和门禁

所有测试使用项目 `.venv`。

阶段 0 文档变更至少运行：

```text
git diff --check
python scripts/python_architecture.py
python -m pytest tests/test_documentation_contract.py -q
```

后续涉及协议、鉴权、Gateway、Memory 或打包时追加：

```text
python -m pytest tests/test_integration_policy.py tests/test_packaging_contract.py -q
```

涉及 Skill Registry 时必须运行：

```text
python -m pytest tests/test_skill_registry.py -q
```

并核对 `added/reparsed/reused/removed` 统计符合预期。

人工语义复核门禁：

- 触发时机：每个阶段收尾前、架构文档或计划文档改动后、核心 owner/协议/生命周期逻辑改动后、测试结果与设计直觉不一致时。
- 复核方法：逐条对照本文档、`ARCHITECTURE.md` 和实际实现，确认“承诺的边界”确实由代码表达，而不是只由测试名或注释表达。
- 复核重点：owner 唯一性、事务边界、失败/恢复语义、旧兼容分支是否仍可能被误当主路径、文档中的完成状态是否有代码证据支撑。
- 输出要求：明确写出“文档一致”“实现一致”“测试证据”“语义风险/残余问题”；不得仅以测试通过作为完成依据。

Memory 整改验收场景：

- 多客户端并发 recall。
- 多客户端并发 remember 和批量写。
- 写入期间读取快照一致性。
- `SQLITE_BUSY` 重试、超时和队列背压。
- 幂等重试不产生重复记忆。
- Memory Service 崩溃、重启和 outbox 恢复。
- Gateway 停止时本地 Memory 数据面仍可用。
- 客户端无法通过路径或环境变量打开 `memory.db`。

## 10. 指标

至少记录：

- Memory 写入队列深度、最老任务年龄和丢弃数。
- SQLite busy 重试次数和最终失败数。
- 写事务耗时、批量大小、commit 吞吐。
- recall p50/p95、缓存命中率、结果裁剪数量。
- outbox pending、dead-letter、恢复耗时。
- Gateway `/api/mem/*` 兼容代理调用量已清零，后续只保留退役态断言。

## 11. 回滚原则

- 每阶段独立发布，协议和 schema 至少向后兼容一个迁移窗口。
- 切换前做 SQLite 在线备份和 integrity check。
- 回滚优先回滚客户端路由和服务版本，不允许多个进程重新同时打开 `memory.db`。
- Gateway 旧代理只能作为短期应急入口，不能作为长期架构回退。
- 任何回滚都不能引入 UI/Agent 直连 SQLite 文件。

## 12. 下一步

阶段 5 范围已收口；仍有无关的 Supervisor 语义回归待单独处理。后续推进建议（按依赖顺序）：

1. 处理全量回归剩余的 2 项 Supervisor 自演化历史窗口失败：已定位为清窗后 need scoring 未显式表达“当前无 live backlog 且学习收益仍有效时应恢复 learning primary”；修复已落在 `endogenous_needs.detect_needs()`。venv targeted 验证 5 项通过，包含新增边界、两个原失败场景、真实 backlog 接管和轻 backlog 边界。
2. 补充 `SessionDB`/`ActionJournal` 的 schema 迁移机制与 `registry.db` 的执行统计（execution_stats）——当前为已知遗留，非本轮范围。
3. 评估 outbox 长期形态：若需收敛为严格单写者（方案 B），须先改写多进程 drain 契约测试（`tests/test_memory_outbox_operational.py` 的 `duplicate_claims == 0`）与跨进程 lease 测试，与崩溃窗口恢复语义冲突，不建议轻易执行。
4. 如进入新阶段（如调度治理、执行面统一），先按第 9 节门禁跑基线再动代码。
