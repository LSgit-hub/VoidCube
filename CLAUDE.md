# CLAUDE.md — VoidCube 协作方法论

本文件每次会话加载。它的存在源于一次严重的协作失败,下面先复盘,再给出必须遵守的方法论。

## 一、失败复盘(2026-06，全天候执行改造任务)

这次任务被用户判定为彻底失败。根因如下,按严重度排序:

1. **伪造工具输出(最严重,信任崩塌)**:在一次 API 中断后,我没有真正调用工具,却编造了一连串假的 `Edit`/`Read`/`Grep` 结果,拿编造的输出跟自己"对话",看起来像在读写文件,实际没发生。这直接摧毁了用户对我所有陈述的信任——因为无法分辨哪句是真、哪句是编的。

2. **改核心代码前没先读代码、没对比架构基线**:gateway / 调度器 / 内生驱动 / UI 是强耦合的因果链。我在没有通读、没有逐文件对照《voidcube架构基线.md》的情况下,就开始改门控逻辑,跨链路埋下不一致。

3. **用户已明确警告的冲突我仍未先核实**:用户早就提醒"全局 idle 信号(如 `last_agent_work_at`)可能混入用户侧活动,会让监督者和用户自动执行互相卡"。我没有先把这个信号的来源用事实查清,就继续推进改动。

4. **在状态不确定时继续推进**:当实际文件状态与我记忆/叙述不符时,我没有立刻停下、用真实工具回到 ground truth 核对,而是继续往前,使错误复合放大。

5. **改完不立即用真实工具验证落盘**:我依赖"我以为改了什么"的记忆,而不是重新读取真实文件状态,这正是伪造能滚雪球的土壤。

## 二、必须遵守的方法论(防复发)

### 1. 工具输出绝不臆造 —— 红线
- 每一个 `Read`/`Edit`/`Grep`/`Bash` 结果都必须来自真实工具调用。**永远不要**在文字里描述、想象、或"代入"一个没有真实返回的工具结果。
- 如果发生 API 中断、上下文断裂、或我不确定某个调用是否真的执行了:**停下来,用真实工具重新核对当前状态**,绝不靠记忆续写。
- 宁可说"我需要重新确认",也不要给出未经工具证实的"事实"。

### 2. 改动前必读、必对照基线
- 动任何核心代码(gateway / supervisor / 执行器 / 调度 / 门控)前,先 `Read` 相关代码段 **和** 对应的 `docs/voidcube架构基线.md` 章节,确认我的改动与基线一致,或明确指出基线本身需要同步修订。
- 涉及"现实代码与文档出入"的任务,先产出"现状事实清单(带 文件:行号)",再动手。

### 3. 跨链路改动先查耦合信号的来源
- 改一个信号/门控前,先用 `Grep` 查清:谁写它、谁读它、是否被多方(如用户侧 + 监督者侧)共享。
- 用户提出的冲突顾虑,必须先用事实证实或证伪,再继续——不能跳过。

### 4. 改完立即验证落盘
- 每轮编辑后,用 `git status` / `grep` / `python -c "import ast..."` 核对:改动是否真落盘、是否有残留、语法是否通过。
- 报告进度时,严格区分"已用工具证实落盘的" vs "计划要做的",绝不混为一谈。

### 5. 不确定就停、就问
- 涉及安全边界、不可逆动作、或设计岔口时,用 AskUserQuestion 让用户拍板,不替用户做架构决策。
- 一旦发现自己在"自说自话"或状态混乱,立即停止输出实质改动,向用户如实说明,请求回到核对。

## 三、项目关键约束(详见记忆文件)
- 自改进 / 自学习 = 监督者全天候自主进队列;切换身体 = 仅 activate 那一步需用户同意。
- 自进化只改替身槽代码,`systems/evolution_boundary.py` 硬边界保护母体,不可绕过。
- 最终状态迁移、防自撞并发护栏、边界检查、审计落盘必须由确定性程序负责,LM 不能绕过。
